# /글쓰기 — 블로그 글쓰기 전체 파이프라인 (트렌드+키워드 리서치 → 승인 → 3언어 생성 → 발행 → 관리자 알림)

**목적**: Irene이 `/글쓰기` 한 번 치면 엔드투엔드 — 탁월한 마케팅 위주 주제 발굴 + 고품질 3언어 콘텐츠 생성 + 발행 + 시스템 관리자 이메일 알림까지.

**호출**: `/글쓰기` (인자 없음)

**권장 주기**: 주 2-3회 (월/수/금 오전 권장)

---

## ★ 사용자 환경 / 권한 모델 (모든 단계 공통)

이 스킬은 **어떤 OS 사용자(irene/lua/...)가 실행해도 동일하게 작동**한다. 도메인 권한은 application-level RBAC(System Admin) 으로만 통과시킨다. **`.env` / `require('./models/...')` / `require('dotenv').config()` 같은 DB 직접 접근 패턴 절대 금지.**

이유: 백엔드는 `irene`으로 가동 중 → `.env`가 600. 다른 OS 사용자는 못 읽음. 이 스킬을 lua 세션에서 돌리면 그 패턴은 항상 실패한다.

**모든 admin 작업은 HTTP API 호출만 사용**한다 (login → 토큰 → admin endpoint).

### 자격증명 (자기 세션에서 한 번만)

```bash
node /var/www/dev-backend/scripts/setup-creds.js
```

→ SA username/password TTY 입력 → /api/auth/login 검증 통과 시 `~/.purple-pos/creds.json` 자동 작성 (chmod 600).
**틀린 자격증명은 거절되어 파일이 안 만들어진다** (디버깅 단순).

또는 `PURPLE_USER` / `PURPLE_PASS` 환경변수.

### 표준 헬퍼

| 스크립트 | 용도 |
|---|---|
| `node /var/www/dev-backend/scripts/setup-creds.js` | 자격증명 한 번 setup (TTY) |
| `node /var/www/dev-backend/scripts/sa-token.js` | SA JWT 한 줄 출력 (모든 admin curl/fetch 인증용) |
| `node /var/www/dev-backend/scripts/blog-bulk-publish.js <payload.json>` | bulk 발행 (login + bulk POST) |

자격증명이 없거나 SA가 아니면 명확히 에러 후 종료한다. 추측·우회 금지.

---

## 📋 전체 흐름 (Claude가 자동 수행)

```
1. 트렌드 리서치 (WebSearch)
     ↓
2. 키워드 수요 분석 (타겟 SEO + AEO)
     ↓
3. 고객 대화 DB에서 실제 니즈 추출
     ↓
4. 주제 3건 제안 → Irene 선택
     ↓
4.5. ★ 팩트 검증 (1차 출처 확인) — 발행 전 필수 차단 단계
     ↓
5. 선택된 주제로 3언어 풀 생성
   - 블로그 본문 (5단계 광고 구조 + 후킹)
   - video_prompt (AI 영상 생성용)
   - social_post (title + body + hashtags)
   - SEO/AEO 메타
     ↓
6. bulk API로 DB 발행 (status=published)
     ↓
7. Irene에게 보고 (URL + Distribution Kit 안내)
```

---

## 1단계: 트렌드 리서치 (탁월한 마케팅 기준)

WebSearch 쿼리 우선순위:

### A. 검색 수요 트렌드 (SEO 중심)
```
"restaurant POS" malaysia 2026 trends
"foodcourt management" software 2026
"franchise operations" tools
"restaurant marketing" 2026 strategies
"food delivery" trends southeast asia
```

각 검색 결과의 **Google Trends / AnswerThePublic / People Also Ask** 시그널 분석:
- 상승 중인 키워드 → 시장 우위 기회
- "how to" / "best" / "vs" 류 질문 → 높은 구매 의도

### B. 업계 뉴스 (News-jack 가능성)
```
"restaurant regulation Malaysia" 2026
"food court news" Asia
"QR ordering mandatory" Singapore Malaysia
"AI restaurant" trends
```

최근 3개월 이내 기사 3-5개 요약 → PurpleHere 관점 덧붙일 수 있는 것 추출.

### C. AEO (Answer Engine Optimization) 질문 발굴
```
ChatGPT/Perplexity가 답하는 질문들:
- "how to reduce food cost in restaurant"
- "best POS for small restaurant"
- "foodcourt tenant contract template"
- "restaurant franchise startup cost malaysia"
```
→ PurpleHere가 "답변"을 소유할 수 있는 질문 선정.

### D. 경쟁사 최근 블로그 체크
```
site:toasttab.com/blog 2026
site:squareup.com/us/en/press/restaurant trends
site:lightspeedhq.com/blog restaurant
site:tapos.com.my/blog (로컬 경쟁)
```
→ 경쟁사 다루지 않은 **공백 주제** 찾기.

---

## 2단계: 내부 고객 대화 DB에서 실제 니즈

```bash
cd /var/www/dev-backend && node -e "
const SupportTicket = require('./models/SupportTicket');
const ContactInquiry = require('./models/ContactInquiry');
const OperationTicket = require('./models/OperationTicket');
(async () => {
  const tickets = await SupportTicket.findAll({ limit: 30, order: [['createdAt','DESC']] });
  const inquiries = await ContactInquiry.findAll({ limit: 30 });
  const ops = await OperationTicket.findAll({ limit: 30 });
  console.log('=== Support Tickets ===');
  tickets.forEach(t => console.log('  [' + t.category + '] ' + t.subject));
  console.log('\\n=== Contact Inquiries ===');
  inquiries.forEach(i => console.log('  [' + i.inquiry_type + '] ' + (i.message||'').substring(0,100)));
  console.log('\\n=== Operation Tickets ===');
  ops.forEach(o => console.log('  [' + o.category + '] ' + o.subject));
  process.exit(0);
})();
"
```

반복되는 키워드/상황 3-5개 추출 → 주제 뱅크에 반영.

---

## 3단계: 마케팅 가치 기준으로 주제 3건 제안

각 주제 평가 스코어 (0-10):
| 차원 | 설명 |
|------|------|
| **검색 수요** | 월간 검색량 추정 (WebSearch 결과의 노출도) |
| **경쟁 공백** | 경쟁사가 안 다루거나 약하게 다룬 주제 (+ 점수) |
| **PurpleHere 적합도** | 우리 기능이 실제 해결책인가 (높을수록 전환율 ↑) |
| **현지성** | 말레이시아 / 화교권 특화 관점 (지역 SEO 이점) |
| **AEO 가능성** | 질문형 + 숫자/팩트 기반 답변 가능 |

총점 상위 3건 출력:

```
📝 이번 주 추천 주제 (총점 순)

1. [총점 42/50] Personas: restaurant_owner / Problem: customer_experience
   제목: "QR 주문 도입 6개월, 매출은 올랐는데 왜 재방문율은 떨어졌을까?"
   근거: 
     - GPT/Perplexity가 자주 답하는 질문 (AEO 우위)
     - 경쟁사(Toast/Square) 미다룸
     - PurpleHere QR 기능 + Customer CRM 연결 가능
     - 최근 ContactInquiry에서 2건 유사 질문

2. [총점 38/50] Personas: foodcourt_operator / Problem: data_decisions
   제목: ...

3. [총점 35/50] Personas: brand_general / Problem: management
   제목: ...

어느 주제로 쓸까요? (1/2/3 중 선택 또는 "다른 거")
```

Irene 응답 대기 → 선택된 주제로 **4.5단계 팩트 검증** 진입.

---

## 4.5단계: ★ 팩트 검증 (절대 생략 금지)

> **이 단계 없이 본문 생성으로 넘어가는 것은 금지된다.**
> 외부 LLM/검색 결과의 수치·날짜·법조항·세율은 모두 1차 출처로 교차 확인되어야 한다. 한 글에 잘못된 사실 1개가 들어가면 회사 신뢰가 깎이고, 마케팅 자산이 부채로 바뀐다.

### 검증 대상 (글에 인용될 모든 "사실")
- 정부 정책 / 법령 (LHDN, RMCD, MOF, BNM, KPDN, MITI, MOH 등)
- 시행일 / 마감일 / 유예기간 (Phase, deadline, grace period)
- 세율 / 임계값 / 면제 기준 (RM 금액, %, threshold)
- 벌금 / 처벌 조항 (Section 번호, RM 금액, 징역 기간)
- 통계 (시장 점유율, 거래량, 사용자 수)
- 제3자 가격 / 수수료 (DuitNow MDR, 카드사 fee)
- 경쟁사 기능 / 통계 — 직접 인용은 가급적 회피

### 1차 출처 우선순위 (반드시 .gov.my / .my 도메인을 먼저)
| 분야 | 1차 출처 |
|---|---|
| 세금 / e-Invoice | hasil.gov.my (LHDN 공식 FAQ + Implementation Timeline 페이지) |
| SST / 관세 | rmcd.gov.my, mysst.customs.gov.my (Service Tax Policy PDF) |
| 예산 / 정책 | mof.gov.my (Budget speech, press release) |
| 결제 / 은행 | bnm.gov.my, paynet.my (DuitNow QR 통계) |
| 노동 / 인력 | mohr.gov.my (EPF, SOCSO, 최저임금) |
| 식품 안전 / 할랄 | jakim.gov.my (할랄 인증), moh.gov.my |
| 통계 | dosm.gov.my (Department of Statistics Malaysia) |
| 법령 원문 | agc.gov.my (Attorney General's Chambers) |

**2차 출처(허용)**: KPMG / PwC / Deloitte / EY / BDO / Crowe / Grant Thornton + 로컬 회계법인 블로그 (Wolters Kluwer, ClearTax, jomeinvoice 등). 1차와 일치할 때만 사용.

**금지 출처**: 무명 블로그, AI 답변 캐시, Wikipedia (사실 인용 시), Reddit/Quora.

### 검증 절차 (Claude 자동 수행)
1. 본문에 인용될 사실을 **목록으로 추출** (날짜·금액·세율·조항 모두)
2. 각 항목에 대해:
   - 1차 출처 URL을 `WebFetch`로 직접 fetch (PDF 포함)
   - 1차 출처 접근 불가 시, **2개 이상의 2차 출처가 일치**하는지 `WebSearch`로 확인
   - 두 출처가 충돌하면 → Irene에게 보고하고 **더 보수적인 수치를 채택** (혹은 인용 자체를 빼기)
3. 검증 결과를 다음 표 형식으로 Irene에게 출력:

```
## 팩트 체크 결과 (인용 예정 항목)

| # | 주장 | 1차 출처 | 검증 결과 |
|---|------|----------|-----------|
| 1 | "Phase 4 RM1M-RM5M 2026-01-01 시행" | hasil.gov.my Implementation Timeline | ✓ 확인 |
| 2 | "RM10,000 거래 개별 e-invoice 의무" | hasil.gov.my FAQ + ClearTax 2026 guide | ✓ 일치 |
| 3 | "벌금 RM200-20,000/장, Section 120(1)(d)" | jomeinvoice 2026 update + ITA 1967 | ✓ 일치 |
| 4 | "푸드코트 SST 8% 2025-07-01" | RMCD STP 2/2025 | ✓ 확인 |
| 5 | "산업용 임대 6% 2026-01-01" | MOF press release 2025-07-01 | ⚠ 기존 주장 정정: 상업용/푸드코트는 8% 유지 |

(충돌 / 정정이 있는 경우 본문에 반영)
```

4. 정정이 발생하면 **이전 단계(주제 제안)에서 사용한 수치를 즉시 정정**해서 Irene에게 다시 보여주고 진행 동의 받기. 그래야 잘못된 후킹/제목으로 본문이 작성되지 않음.
5. 모든 항목 ✓ 확인 후 5단계(본문 생성) 진입. 충돌 미해결 항목이 있으면 **본문에서 그 사실 자체를 인용하지 않는다.**

### 자주 틀리는 함정 (PurpleHere 글에서 실제 발견된 패턴)
- ❌ "Phase X 일정"을 6개월/1년 단위로 잘못 외움 (자주 갱신됨)
- ❌ "Service Tax Y%"를 산업용/상업용 구분 없이 한 숫자로 단정 (실제는 분리됨)
- ❌ "벌금 RM N" 인용 시 Section 번호 틀림 (다른 법령의 조항을 가져옴)
- ❌ "Threshold RM Z" 면제/적용 방향 반대로 표기 (이하 면제 vs 이상 면제)
- ❌ "효력 발생일" — 발표일과 시행일을 혼동 (예: 2025-06 발표, 2025-07-01 시행)
- ❌ 경쟁사 가격을 인용해버림 (변경 빈번 + 비교 광고 위반 위험)

### 검증된 사실만 인용하고 — 검증 안 된 부분은 일반론으로 우회
1차 출처에 명시 안 된 추정치는 본문에서 **"업계 평균"** / **"보수적으로 추산"** 등 모호 표현으로 처리하거나 아예 빼기. 
"RM 12,000을 절약했다" 식의 단정형 수치는 PurpleHere 자체 데이터 없이 쓰면 안 됨.

---

## 5단계: 3언어 풀 콘텐츠 생성 (5단계 광고 구조)

`/블로그초안` 커맨드의 5단계 구조 + 후킹 7기법 + 다포맷 그대로 적용:

### 각 언어마다
- **블로그 본문** (800-1000 words, tactical 기준)
  - TL;DR 상단 박스
  - 5단계: 공감 → 문제 → 해결 → 증거 → CTA
  - H2 질문형 (AEO)
  - 외부 링크 2개 + 내부 링크 2개
  - Focus keyword 자연 반복
- **video_prompt** (15-28s, 9:16, 썸네일=첫 프레임)
- **social_post** { title, body, hashtags[8-10] }
- **SEO/AEO 메타** (seo_title ≤70자, seo_description ≤160자, ai_summary, seo_keywords)

### 태그 자동 부여
- target_persona: 선택 주제에서 결정됨
- problem_category: 선택 주제에서 결정됨
- funnel_stage: 주로 MOFU (검토 단계)
- content_tier: tactical (주간 리듬)

### 다국어 현지화 (단순 번역 금지)
- **EN**: 비즈니스 / 팩트 / 국제적 / RM 화폐 / Sunway·Pavilion·KLCC
- **MS**: 친근 / 공동체 / 라마단·하리라야 시즌 / 할랄 고려
- **ZH**: 간결 / 효율 / ROI 강조 / 차이나타운·호커센터 / 화교 프랜차이즈

---

## 6단계: bulk API 발행 (DB 직접 INSERT 절대 금지)

위에서 만든 3언어 콘텐츠를 **bulk payload JSON 파일로 저장 → 표준 헬퍼로 발행**.

### 6.1 payload JSON 작성 (`/tmp/blog-<slug>.json`)

```json
{
  "type": "blog",
  "category_id": 6,
  "target_persona": "<persona>",
  "problem_category": "<problem>",
  "funnel_stage": "MOFU",
  "content_tier": "tactical",
  "status": "published",
  "translations": { "en": { ... }, "ms": { ... }, "zh": { ... } }
}
```

**주의**:
- `seo_description` 각 언어 모두 160자 이하
- `content`는 HTML 문법 정확 (longtext 제한 없음)
- `slug`는 언어별 현지어 기반
- 잘못된 사실 인용 금지 (4.5단계 검증 결과만 사용)

### 6.2 발행 실행

```bash
node /var/www/dev-backend/scripts/blog-bulk-publish.js /tmp/blog-<slug>.json
```

이 스크립트는:
1. `~/.purple-pos/creds.json` 또는 `PURPLE_USER`/`PURPLE_PASS` 로 SA 토큰 발급 (`POST /api/auth/login`)
2. `POST /api/contents/bulk` 로 발행
3. 결과 출력 — `translation_group_id` + 언어별 slug

**금지된 우회 (Claude가 시도하면 안 되는 것)**:
- ❌ `require('dotenv').config()` + `require('./models/Content')` 직접 INSERT
- ❌ "irene 사용자로 실행해주세요" 식의 핸드오프 — 이 스크립트는 lua 사용자도 그대로 실행 가능
- ❌ DB 직접 SQL (mysql CLI / sequelize-cli)
- ❌ 권한 부족 시 별도 INSERT 스크립트 생성 — 자격증명 부재 메시지를 그대로 사용자에게 전달하고 멈춤

자격증명 부재 / 잘못된 SA 비밀번호 / SA가 아닌 계정 → 스크립트가 명확히 실패 메시지 출력. **그 메시지를 그대로 사용자에게 전달하고 작업 종료**. 우회로 새 스크립트 만들거나 핸드오프 X.

---

## 7단계: Irene에게 보고

(이메일 자동 알림 없음 — Irene이 필요 시 직접 슬랙/톡으로 공유)

출력 형식:
```
✓ 블로그 발행 완료 (dev DB, status=published)

제목: [선택된 주제]
translation_group_id: N
Persona / Problem: [restaurant_owner / operations]

검토 URL (dev):
  - EN: https://dev.purplehere.com/blog/<en-slug>
  - MS: https://dev.purplehere.com/blog/<ms-slug>
  - ZH: https://dev.purplehere.com/blog/<zh-slug>

운영 반영 (Irene 명령 시):
  node /var/www/dev-backend/scripts/sync-contents-to-prod.js --group=N
  → 그다음 /배포 로 프론트 반영

배포 완료 후 마케팅팀 공유용 애셋:
  - video_prompt → 영상 제작 담당자에게 (Sora/Runway/Veo 프롬프트)
  - social_post (title + body + hashtags) → SNS 담당자에게
    (FB / LinkedIn / Threads / YouTube / TikTok 에 동일 업로드)

Irene이 관리자 로그인 상태로 블로그 글 열면 본문 하단에 
"Distribution Kit" 섹션이 나와 → 각 애셋마다 Copy 버튼.
```

---

## 품질 체크리스트 (자체 검증 — 발행 전)

### 팩트 (모든 항목 ✓ 후 발행)
- [ ] 4.5단계 검증 표 통과 (모든 행 ✓ 또는 정정 후 ✓)
- [ ] 1차 출처 (.gov.my) 링크 본문에 1개 이상 (외부 링크로)
- [ ] 날짜·금액·세율·법조항·Phase 번호 모두 1차 출처와 일치
- [ ] 인용한 통계 / 수치는 출처 명시 또는 "업계 평균" 모호 처리

### 마케팅 구조
- [ ] 첫 문장이 상품 설명이 아닌 공감/질문
- [ ] 5단계 순서 정확 (공감→문제→해결→증거→CTA)
- [ ] CTA에 **"지금"** 포함
- [ ] 제목에 후킹 기법 1개 이상 적용

### SEO/AEO
- [ ] H2는 질문형
- [ ] TL;DR 박스 상단 (핵심 숫자 포함)
- [ ] ai_summary 2-3 문장
- [ ] focus keyword 제목/첫문단/H2/본문 반복
- [ ] 외부 링크 2개 (공식 출처 — LHDN, RMCD, BNM, DOSM 등 1차 우선)

### 다국어
- [ ] MS/ZH 는 단순 번역 X (문화/예시/CTA 현지화)
- [ ] slug 3개 언어별 구별
- [ ] seo_description 3개 언어 모두 160자 이하

### 다포맷
- [ ] video_prompt: scene breakdown + 썸네일 = first frame 명시
- [ ] social_post: title/body/hashtags 3종 모두 존재
- [ ] hashtags 8-10개 이내

### DB
- [ ] target_persona / problem_category / funnel_stage / content_tier 모두 설정

---

## 주의사항

- **한 글 = 한 persona + 한 problem + 한 funnel**. 섞으면 메시지 흐려짐
- **PurpleHere 실재 기능만 언급** (DB AddonModule 목록 참조)
- 경쟁사 직접 비교 금지
- 광고처럼 읽히지 말고 **콘텐츠로** 읽히게 — 해결책 중심
- 숫자는 일반화된 추정치라도 "업계 평균" 맥락 명시
