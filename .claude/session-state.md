## 현재 작업 상태
**마지막 업데이트:** 2026-08-25
**버전:** v3.78 (코드 변경 없음 — 이번 세션은 **운영 데이터 작업만**)
**작업 상태:** 완료

### 진행 중인 작업
- 없음

### 완료된 작업 (2026-08-25 · 운영 데이터, 배포 아님)

**GIT Consulting(BG, owner 23) 재고 실사 반영 + 상품 이름 표기 통일.**
코드는 한 줄도 안 바꿨다. 쓰기는 전부 앱 자신의 API(`localhost:3002`) 경로로 했다.
백업 `/var/www/backups/git_stock_naming_20260825134523.sql` (435행).

1. **재고 8건 조정** — Irene 이 준 21건 실사표와 대조해 어긋난 8건만 고쳤다(나머지 13건은 이미 일치).
   합계 313 → **306**, 재고 있는 품목 18 → 19건. `stock_take` 이력 8건 기록.
   R1000·R12 는 0 이던 것을 16 으로 올렸고, SC-450 17→1, SC-800 18→0(Irene: "없는데 잘못 잡혀 있다").
   박스 환산: 850cc·PP-Lid 12팩/bx, A1000 5팩/bx, R-12 10팩/bx.
2. **cc ↔ S/M/L 매핑을 추측으로 하지 않았다** — 삭제 예정이던 옛 상품에 용량이 남아 있어
   판매가로 교차 확인했다(850CC=RM16.50=(M), 780CC=RM20.50=(S)). 현재 재고값과도 일치.
3. **옛 표기 중복 상품 21건 삭제** — 같은 물건이 옛 영문 대문자 표기와 한글 병기 표기 **두 벌**로
   등록돼 있었다(발주 화면에 두 줄로 보이던 상태). 한글 쪽을 남겼다. 상품 109 → **90건**.
   지우기 전 **카테고리 19건을 남는 행으로 이관** — 카테고리가 삭제될 옛 행에만 있었다.
4. **신규 2건 등록** — MTP 360 (12OZ) PP Plastic Cup / MTP 12/16/22 FL5 PET Flat Lid.
   상품(판매가 RM14.70 / RM5.25 = 원가+40%) + 재고(원가 RM10.50 / RM3.75, 수량 0) 한 쌍씩.
5. **일부러 손대지 않은 것** — `RECLOSABLE LID 12OZ BLACK`(bp44, 짝이 둘이고 단서가 엇갈림),
   `K-Yukgaejang Sauce` 쌍(레시피가 `1kg` 표기 없는 쪽에 붙어 있어 표기를 맞추면 레시피가 끊김),
   비활성 `OMIJA/PEACH/STRAWBERRY BASE 1btl` 3건, 한글명이 아예 없는 34건(이름을 지어낼 수 없다).

**⚠️ 내가 만들고 고친 것 1건:** 상품 SKU·재고 코드 채번이 **건수 기반**(`PRD-{count+1}`)이라,
상품 20건을 지운 뒤 신규 등록하니 이미 쓰인 번호가 다시 나왔다(PRD-094/095 중복).
PRD-114/115 로 정정. 재고 코드도 같은 이유로 PI-288/289 로 정정.
**삭제 뒤 신규 등록은 채번 충돌을 반드시 확인할 것.** 단일 진실 = 메모리 [[reference_count_based_code_numbering]].

**추가 1건 (Irene 지시 "레시피 연결 안된 걸 지우면 되잖아")** — `K-Yukgaejang Sauce 1kg`(bp17,
레시피 없음) 삭제, `K-Yukgaejang Sauce`(bp11, 레시피 3번 · 재료 11건) 유지. 상품 91 → **90건**,
SKU 중복 0, 레시피 재료 11건 그대로. 매장분배·발주 참조 0 확인 후 삭제.

**[코드 변경 · 미배포] 발주 수량 화살표가 0.01 씩 오르던 것 — 3곳 전부** (Irene 보고 후 "같이 수정해줘")
카트·수령·반품 수량 입력이 전부 `step="0.01"` 로 고정돼 팩 상품에서 화살표 한 번에 0.01 씩 올랐다.
**단위가 step 을 정하도록** 공용 헬퍼로 통일: `utils/unitConversion.ts` 의 `qtyStepForUnit()` —
기존 `STANDARD_UNITS.category` 를 재사용해 count·serving 은 1, weight·volume·cooking 은 0.01,
모르는 단위는 1(발주 수량 대부분이 팩·개). 대소문자·공백 느슨하게 본다(`'L'`, `'KG'`).
- 적용 3곳: `NewPurchaseOrderPage.tsx`(카트) · `PurchaseOrderDetailPage.tsx`(수령 `line.unit`,
  반품 `item?.ingredient_unit`). 담기·＋버튼은 원래 1씩이라 무관.
- **건드리지 않은 것**: 단가(`:1323`)와 단위환산 비율(`NewPO:2063`) — 소수가 맞다.
- 검증: 헬퍼 단위테스트 **21/21** · 실브라우저(개 `step=1` 화살표 1→2, kg `step=0.01` 유지) ·
  상세 페이지 mount 크래시 0 · **3회 연속 2/2 통과** · 빌드 경고 증가 0(3건 그대로, PO 파일 무관)
- **고장주입**: 전부 0.01 로 되돌린 빌드에서 테스트가 실제 실패(`Received "0.01"`) → 원복 후 통과
- print-guard 8/8 · design-guard 신규 0 · health-check 171/171 · 민감도 판정 비대상
- 새 스펙 `dev-frontend/e2e/po-qty-step.spec.js` (dev 전용, 쓰기 없음). 8/20 보안 가드로
  `demo-login test_brand_general` 이 403 이라 dev 토큰을 직접 발급해 주입한다.
- ⚠️ **확인 불가 1건**: dev 에 수령 완료된 발주가 없어 **수령·반품 화면의 화살표 클릭은 못 봤다.**
  같은 헬퍼를 쓰고 단위 필드 존재는 확인했으나(`line.unit` = `it.ingredient_unit` 매핑,
  `PurchaseOrderDetailPage.tsx:657`), 화면 클릭 증명은 아니다.
- ⚠️ **운영 미배포** — Irene `/배포` 지시 대기.

**신규 2건 초기 재고 18팩씩 입력** (Irene 지시) — PI-288 투명컵 18, PI-289 뚜껑 18.
`initial` 이력으로 기록. 재고 19건 306개 → **21건 342개**. 세 화면 검증 4/4 PASS.

**발주 가능 상태 마무리 (Irene "발주해도 되지?" 점검 결과 2건 걸림)**
1. ⚠️ **내가 깨뜨린 것** — 상품 삭제 라우트가 `ingredient_seller_products` 를 안 치운다.
   실매장 2곳(K-DINE IPC Branch 3건 · with MIN Cafe 1건)의 발주 연결 4건이 없어진 상품을
   가리킨 채 남았다. 남아 있는 한글 표기 상품(127/129/131/123)으로 되돌렸다.
   **매장별 계약가는 손대지 않았다**(RM12.90/64.90/19.90/8.90 — 상품 정가와 다르다).
2. **신규 2건은 발주 연결이 없었다** — 기존 21건은 전부 공급업체 **UGS**(회사 35)에서 사온다.
   UGS 카탈로그에 2건 등록(SP-35-0054/0055, RM10.50·RM3.75) + 재고와 연결. 카탈로그 55 → 57건.
   **재검증 9/9 PASS** — 끊긴 연결 0 · 오늘 다룬 23건 전부 공급처 있음 · 재고 19건 306개 유지.

**검증 12/12 PASS** — 세 화면(재고/Stock Items/발주) 전부 19건 306개 · 주신 21건 값 전부 일치 ·
신규 2건 원가·판매가·카테고리 · SKU 중복 0 · 실사 이력 8건. 운영 임시 스크립트 전부 삭제 확인.
(재고 코드 중복 12종은 **이번 작업 전부터 있던 것** — 이원화 문제와 같은 뿌리, 손대지 않았다.)

### 완료된 작업 (2026-08-24 세션)

#### 운영 배포 4건
| 배포 | 내용 | 백업 |
|---|---|---|
| 1 | 발주 Stock Items 가 통째로 안 보이던 것 (`track_stock` 고정값) | `20260824_133050` |
| 2 | 카테고리 필터가 비어 있던 것 (`category` 미전달) | `20260824_141732` |
| 3 | 재고관리 필터 2종 + 연결 구분 · 발주 목록 제목/카테고리/재고 표시 | `20260824_165723` |
| 4 | `pending_approval` ENUM 마이그 + SW 버전 올림 | `20260824_181613` |

#### 1. 운영 발주 내역 전체 초기화 (배포 아님 · 데이터)
Irene "발주기능은 쓴 적이 없어" → 7건 전부 시드/테스트. `purchase_orders` 7→0, items 30→0.
**재고 행은 안 지웠다** — PO#2 에 붙어 있던 재고배치 4·재고거래 4 는 행 유지하고 `purchase_order_id` 만
NULL 로 끊음. 백업 `/var/www/backups/po_reset_20260824104138.sql`. 채번은 날짜별 조회라 충돌 없음.

#### 2. 발주·재고 화면 결함 3건 (배포 1~3)
세 결함 모두 **같은 매핑 블록 한 곳**에서 나왔다 — 브랜드 재고아이템을 화면 행으로 만들 때
`track_stock`·`category`·`current_stock` 을 안 받아가고 있었다.
- `track_stock: false` 고정 → 운영 286개 전부 "untracked" 로 분류돼 기본 필터가 통째로 숨김
- `category_id`/`category` 미전달 → 전부 미분류(운영은 미분류 0개, 카테고리 18개)
- 재고관리: 카테고리 필터 + **Zero Stock 필터**(기존 `Out of Stock` 은 `min_stock>0` 인 것만 잡아
  286개 전부 0건이었다. **알림 정책은 무변경**) + 연결 안 된 품목 구분(283 연결 / 3 미연결)
- 발주 목록: 제목 2줄(최장 89자), 카테고리가 컨테이너 900px 이하에서 `display:none` 되던 것을
  이름 아래 줄로 이동(카트 패널 때문에 사실상 항상 숨김이었다), 현재 재고 표시. 리스트·카드 공통

#### 3. `pending_approval` ENUM (배포 4)
운영 ENUM 에 값이 없어 **오너 연결 매장의 발주 제출이 실패**하던 상태. 이 값을 넣는 마이그가
애초에 없었다(sync-database 는 `--alter` 없이 돌아 ENUM 을 안 바꾼다).
고장주입 **7/7**: 값 제거 → 저장 실제 거부(`Data truncated`) → 마이그 → 저장 성공 → 재실행 skip.
중간 삽입이 기존 행을 밀지 않는지 dev 100행 7종 상태로 별도 증명(전부 보존). 운영 사후 4/4,
dev↔운영 스키마 `Schemas are identical`.

#### 4. GIT Consulting 재고 정정 (데이터)
**패키지 재고 18건 313개는 BG 재고인데 8/20 에 with MIN Cafe(매장10) 재고칸에 잘못 입력**돼 있었다.
Irene 지시 "BG 재고에 넣어, 위드민에서 빼고" → `product_ingredients` 이관 + 매장칸 0 + 브랜드행 사본 0.
이름 다른 1건(휴지)은 짝 수동 확정(ing#384→pi#286). 양쪽 이력 기록.
백업 `bg_stock_correction_20260824192743.sql`. **세 화면 API 검증 6/6**(18건 313개, 매장 0건).

#### 5. ⚠️ 내가 저지른 것 (재발 방지)
1. 18:55 에 매장 수량을 BG 재고칸에 17건 잘못 입력 → 되돌림. 근거로 삼은 게 **코드 주석**
   (`brand-inventory.js:320-322` "브랜드 제너럴은 자기 재고를 스스로 관리한다")이었는데 사실과 달랐다.
   **코드 주석은 이전 세션의 해석일 뿐 도메인 사실이 아니다.**
2. 매장 보유분을 브랜드 화면에 끌어오려 함(`linked_ingredient_id` 연결 + 배지) → **전면 철회.**
   Irene: "매장 재고가 브랜드제너럴이랑 무슨 상관이냐고. 각자 자기 재고를 관리하는 건데."
3. 선택지를 물은 것 자체가 이해 부족의 신호였다 — 도메인은 물어서 고르게 할 게 아니라 파악할 것.

#### 6. 검증
verify-all **16/16** ×3(mount sweep 655~685초 실수행) · 🔒 인쇄 **8/8 무접촉** · 스모크 **10/10** ×4 ·
고장주입 3건(track_stock 0→8 / 카테고리 0→전체 / ENUM 거부→성공) · 운영 실데이터 판정 3건.
**게이트가 실제로 잡은 것 2건**: ①타임존 가드가 내 `toLocaleString` 적발 → 로케일 호출 제거
②빌드가 메모리 게이트에 막혀 시작조차 안 됨 → 그대로 검증했으면 옛 번들을 검증할 뻔.

### 다음 확정 작업
- 없음 — 지시 대기
  (Irene 언급: Fable 은 **목요일(8/27~28)** 사용 가능 → 아래 구조 사안을 그때 설계)

### 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- **[Fable 설계 대기] 브랜드 재고 목록 이원화** — `ingredients`(브랜드 88) vs `product_ingredients`(286).
  상품 등록 시 양쪽에 하나씩 생긴다. 단일 진실 = 메모리 [[project_brand_stock_two_lists_split]]
- **[Fable 설계 대기] `linked_ingredient_id` 반쪽 구현** — 읽기만 연결을 따르고 쓰기는 전부 무시.
  **채우면 숫자가 안 움직인다** — 쓰기도 따르게 할지, 링크 개념을 버릴지 결정 필요
- **[Fable 설계 대기] FG(푸드코트) 같은 어긋남** — 입고는 `ingredients`, 화면은 `foodcourt_products`.
  사오면 안 보이고 팔면 음수로 내려간다(역할 전수 조사 결과)
- **미배포 코드 1건** — `useInventoryData.ts` 가 브랜드 수량에 매장 값을 더하던 계산 제거.
  현재 연결이 비어 있어 운영 영향 없으나 누가 연결을 채우면 두 배로 보인다. 다음 배포에 포함
- **이름 표기 통일 남은 것 — Irene 이 나중에 직접 본다(8/25 지시, 개발 대상 아님).**
  다시 물어보지 말 것. ①한글명 없는 34건(K-소스·K-DINE 의류 등) ②가격이 다른 옛 상품
  `OMIJA/PEACH/STRAWBERRY BASE 1btl` 3건(비활성 그대로 둔다) ③`RECLOSABLE LID 12OZ BLACK`
  레시피·소스 연결은 Irene 이 직접 한다.
  Irene 원문: **"한글 안보이는 건 두고 가격 다른 거 둬. 내가 나중에 볼게. 레시피나 소스랑
  연결하는 거나 알아서 할게. 개발문제 아니면 넘어가. 비활성 두고."**
- **[코드 결함] 상품 삭제가 발주 연결을 안 치운다** — `DELETE /api/brand-products/:id`
  (`routes/brand-products.js:1133`)가 식자재·분배·옵션그룹은 지우면서 `ingredient_seller_products`
  는 그대로 둔다. 오늘 실매장 4건이 끊겼다(데이터는 복구 완료). 코드 수정은 배포 필요 — 별건.
- **채번을 max 기반으로** — SKU/코드가 건수 기반이라 삭제 뒤 등록하면 번호가 재발급된다.
  운영 재고 코드에 이미 중복 12종. 메모리 [[reference_count_based_code_numbering]]
- 식자재 쪽 이름 중복(id 384 / 385) 정리
- Brand Manager 는 재고 목록이 전부 비어 보인다(읽기는 `owner_user_id`, 쓰기는 `brand_id` 기준)
- 데이터 작업(매장 몫): 재료 수량 입력 → 그 뒤 메뉴↔레시피 연결 84건. 순서 반대면 품절 알림 폭주
- 프로필·구독 통합 / POS 기기 고정 해제 옵션 / 흩어진 계정 통합

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```
