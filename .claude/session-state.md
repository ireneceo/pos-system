## 현재 작업 상태
**마지막 업데이트:** 2026-09-16 (카테고리 삭제 시 재료 이동 — **운영 배포 완료**)
**버전:** 변경 없음 (Irene 미지시)
**운영:** SW `5.30-category-reassign-delete-20260916` — **2026-09-16 09:31 UTC 배포**(안전 게이트 10/10 · 스모크 10/10 · 백업 `/var/www/backups/20260916_092523` · DB 덤프 `db_predeploy_20260916_092523.sql.gz`). 운영 실측 확인: 새 코드 3곳 · 새 문구가 운영 번들에 실림 · 익명 삭제 401.
**작업 상태:** 배포 완료. 배송비·카테고리 통합은 **Fable 판정 대기**(오늘 4회 모두 한도 429, 우회 안 함). 🔴 **막힌 곳: Fable 한도.** Irene 판단 필요 — 계속 기다릴지, 다른 방식으로 갈지.

> **오늘 한 일 3가지:** ①2026-09-15 오후 작업 2건이 운영 배포까지 끝났는데 기록이 비어 있던 것을 릴리즈 기록에서 복구하고 세 문서의 «미배포» 오표기를 정정 ②Irene 지시로 **재료 카테고리 삭제 시 «재료를 어디로 옮길지» 선택**을 구현·검증·**운영 배포** ③배송비·재료 카테고리 «두 벌» 실측을 `docs/TRADE_STRUCTURE.md` ⑦·⑧ 에 저장(Fable 판정 대기).
>
> ⚠ **Fable 은 오늘 4회 모두 한도(429)로 판정 없음** (배송비 1 · 배송비+카테고리 합본 1 · 카테고리 1 · 배송비 재요청 1). 4회차는 Irene 이 요구사항을 두 번 좁혀 «사안이 실제로 바뀐» 정당한 재호출이었다. 우회(`.claude/.fable-gate-skip`)는 하지 않았다 — 2026-09-14 Irene 이 「기다린다」를 선택.

---

## 👉 Irene 님이 하실 일 (Claude 가 대신 못 하는 것만 — 급한 순서)

> Claude 는 이 목록을 `/개발시작` 때 **그대로 안내**한다. 하나 끝내면 Claude 에게 «N번 했어»라고만 말하면 된다.

### 0-신번. (2026-09-15 15:21 배포분 · **운영** purplehere.com 에서 Ctrl+Shift+R) — 약 5분
1. 발주 → 내 재고아이템에서 **같은 물건이 한 줄**로 보이는지(연결된 것 기준)
2. 그 줄에 **공급업체 이름**이 제대로 나오는지(«undefined» 가 없는지)
3. 재고아이템 화면에서 이미 프로덕트인 항목에 **«판매 중: 이름»** 이 보이는지
4. 매장 관리자로 발주 → 카탈로그 버튼이 **«메뉴로 등록»**, 재료 화면 버튼이 **«메뉴로도 등록»** 인지
5. 발주·재고 목록의 **거래처 이름이 회사명**으로 나오는지(브랜드명이 아닌지)
6. 브랜드 프로덕트 창에서 «저장할 때 재고아이템도 같이 만들기» 를 켜고 저장 → 재고아이템이 생기고 붙는지 / 이미 있는 이름이면 **«있는 것에 연결» 확인창**이 뜨는지
7. 재고아이템이 붙은 프로덕트에 **«파는 기준 / 사는 기준 / 마진»** 줄이 보이는지
8. 사이드바 **Sales Orders 빨간 점** · 발주 알림 **메일 머리글 브랜드** · **Profile 이메일 인증 안내** (2026-09-15 오전분, 같은 배포에 포함)

### 0-A번. 운영 데이터 — 코드로는 안 되는 것 (배포 후 남은 일)
1. **이미 만들어진 «2중» 행 합치기** — 프로덕트 창에서 기존 재고아이템을 골라 연결하면 그때 한 줄이 된다(코드가 대신 못 합침)
2. **브랜드 회사명을 «GIT Consulting» 으로** — 비어 있으면 거래처 이름이 브랜드명으로 떨어진다(2026-09-15 Irene 「모든 브랜드 회사명은 GIT Consulting」)
3. `help@withmin.info` → `help@k-dine.com` **운영 실행 승인** — 스크립트는 준비됨(`scripts/migrate-replace-withmin-email.js`, 개발 적용 1건·재실행 0건 멱등 확인). 운영 DB 읽기 계정이 없어 현황 확인조차 못 한 상태
4. **운영 DB «읽기만 되는» 계정** — 위 2·3 과 아래 여러 건이 전부 이것 때문에 막혀 있다(약 5분)

### 0-B번. (2026-09-14 배포분) 청구서·발주 — 약 3분
1. 브랜드 청구서에서 **0원(Free) 건의 «Confirm»** 이 결제완료로 바뀌는지 (운영 10건 대기)
2. 발주 Staging 에서 **«Mark as Sent»** 후 위 «받을 발주» 에 **새로고침 없이** 올라오는지
3. 발주 결제 기록 창에 **결제일** 칸이 있고 **은행송금이 먼저** 나오는지
4. 인보이스 대조 안내에 **한글이 섞이지 않는지**(영어 화면)
5. `purplehere.com/pos/brand-menus` 에서 레시피 이름(보라색)·«레시피 없음»(회색) — ⚠ 로그인은 **`help@gitconsulting.group`**

### 1번. 운영 발주 32 손 수정 — 대조 화면에서 3줄 (손글씨라 자동으로 안 채워짐)
### 2번. 운영 «piece» 대기 발주 2건 — 담기 화면에서 줄 지우고 다시 담기
### 3번. 발주 33 의 단위 확인 — 말로 «맞아/아니야»만
### 4번. 운영서버 nginx 설정 폴더의 백업 파일 치우기 — 1분

---

### 진행 중인 작업
- 🟡 **K-DINE 브랜드 레시피 ↔ K-DINE IPC 연동 점검 (2026-09-16 Irene 요청, 조사 완료·조치 미착수).** Irene 원문 「브랜드제너럴인 GIT Consulting에서 K-DINE 브랜드레시피를 올렸거든. 이게 제대로 K-DINE IPC에 갔는지 알려줘. 연동 문제 없는지 체크 좀 해줘」
  - **도달은 정상**: 브랜드 2(K-DINE with MIN) 레시피 **72건 전부 활성** · 매장 8(K-DINE IPC Branch) `brand_id=2` · 카테고리 72/72 브랜드 소유 · 재료 줄 351개 전부 브랜드 2 재료(끊김 0) · 매장 상품 recipe_id 65건 전부 브랜드 2 레시피(끊김 0) · 요금제 문은 이 경로만 예외(`routes/recipes.js` 주석). 실호출(개발서버 매장 계정) 200·건수 일치, 익명 401.
  - **문제 6건(운영 데이터, 손대지 않음)**: ①재료 든 레시피 **21건이 어떤 메뉴에도 안 붙음**(9/1~9/10 업로드분 대부분) → 팔려도 차감 0 ②그중 **Add-on 10건은 옵션용인데 옵션은 레시피가 아니라 `option_ingredients` 로 차감** — 매장 8 옵션 12개 전부 재료 연결 0 ③**메뉴에 붙었는데 재료 0인 레시피 8건**(Cooked Rice·Fried Egg·Radish Kimchi·음료 5) → 매장 상품 13개가 «연결됨» 으로 보이나 차감 0 ④브랜드 메뉴 104개 중 **39개 레시피 없음**(ALA CARTE 5·세트 12·Take Away Charge 등, 이름 같은 레시피 0건) ⑤**상속 누락 1건** — 매장 상품 373 «2Pax Sundubu(S)» 가 브랜드 메뉴 354(recipe 13)에서 왔는데 `recipe_id` NULL ⑥**브랜드 메뉴 이름 중복 8쌍**(VEGE JAMPONG RAMEN 5·Ramen Tteokbokki 3·2Pax Sundubu(S) 2 — 한쪽만 레시피).
  - 별건: 매장 27 «K-dine with.MIN» 은 `brand_id` 가 비어 브랜드 레시피가 0건 도달(주문 0건).
  - **조치는 운영 레시피·메뉴 연결을 바꾸는 일이라 Fable 판정 대상**(파급 크고 길이 갈림). 아직 아무것도 고치지 않았다.
- 🔴 **재료 카테고리를 브랜드 것으로 통합 — Fable 판정 대기 (2026-09-16 Irene 지시).** Irene 원문 「브랜드에서 제대로 넘어온 카테고리로 보내려고 해. 이중으로 생기네」.
  - **삭제 시 «옮길 곳 선택» 은 오늘 배포로 끝났다**(SW 5.30). 남은 것은 **어느 벌을 정본으로 삼고 어떻게 합칠지** — 이건 길이 갈린다.
  - 걸림돌 실측: 브랜드 14개에 매장이 쓰는 **Staff Meal·Poultry·Alcohol·Beverage·Kitchen & Supplies 5개가 없고 거기 재료가 43건**. 매장 «Uncategorized» 에 81건.
  - 두 벌이 또 생기는 경로 2개(임포트 스크립트 · `brand-products.js` find-or-create)를 막을지도 같이 정해야 한다.
  - 갈리는 6지점 = `docs/TRADE_STRUCTURE.md` ⑧. **운영 재료 386건 분류를 바꾸는 일이라 스냅샷 없이 착수 금지.**
- 🔴 **배송비 — Fable 판정 대기 (2026-09-16 Irene 지시, 원문 5건).** 실측·규칙 지정 다 끝났고 **설계 판단만 남았다.** Fable 4회 전부 한도(429).
  - **Irene 지정으로 닫힌 갈래 4개**: 자동계산(사람 입력 아님) · 규칙 = 기준선 이상 무료 / 미만 고정액 · 적용 시점 = 품목 합계 확정 후 · **판매자별**.
  - **남은 갈래 7개** = `docs/TRADE_STRUCTURE.md` ⑦ (발주에 담는 형태·총액식 변경 / 값을 담을 그릇 / 원가 반영 / 4경로 범위 / 통화·매장 차등 / 미설정 기본값).
  - 핵심 실측: 발주 38건 전부 `total_amount = subtotal`·`tax_amount = 0` → **배송비가 총액식을 바꾸는 첫 값**(금액 공식은 게이트가 잠근 보호 대상) · 공급업체 42곳 전부 `min_order_amount`·`delivery_policy` **0건**(옮길 값 없음) · 브랜드 판매 실거래 **8건**인데 브랜드엔 칸 없음 · 판매자 4종 단일 소스 `utils/sellerNames.js resolveSellers` 가 **이미 있다**(새 경로 금지).
  - ✅ Irene 확답: 「외부업체 등록 = **레스토랑 공급업체 등록**」(Carrier 화면 아님). ✅ 「외부업체 등록 = **레스토랑 공급업체 등록**」으로 확답 받음(Carrier 화면 아님). 추가 실측 답: 발주 총액식이 `total_amount = subtotal, tax_amount = 0` 이라 **배송비는 무료로 정한 게 아니라 계산식에 자리가 없다**. 대조에서 적은 값은 `invoice_delivery` 로 저장만 되고 발주 총액엔 안 들어간다. 실측은 끝났고 설계 판단만 남았다. Fable 은 **한도(429)로 이번에도 판정 미수령**. Irene 이 2026-09-14 에 «기다린다» 를 고른 대로 **우회하지 않는다**.
  - Irene 원문 ① 「브랜드제너럴이랑 공급업체들 배송비 기준은 레스토랑관리자가 발주할 때 안나오네. 배송비 나와야 하는데 외부배송업체 등록할 때도 배송비 따로 기준 안나오고. 이거 어떻게 해야 해? 발주하고 인보이스 비교할 때 배송비항목 추가하게 할 수 있어?」
  - Irene 원문 ② 「브랜드제너럴이 공급업체에 주문할 때도 마찬가지지.」
  - **실측 사실 (2026-09-16):**
    - 발주(`models/PurchaseOrder.js:28~31`)는 `subtotal`/`tax_amount`/`total_amount`/`currency` 뿐 — **배송비 칸 없음**. 품목(`PurchaseOrderItem`)에도 비품목 라인 개념 없음.
    - **인보이스 대조에는 배송비가 이미 있다** — 컬럼 `PurchaseOrder.invoice_delivery` DECIMAL(12,2)(`:82`), 라우트 `routes/cost-reconciliation.js:151~152,204` 가 `invoice.delivery` 를 받아 `줄 합 + 세금 + 배송 − 할인` 으로 총액 대조(차액 허용치 초과 시 400 TOTAL_MISMATCH), 화면 `InvoiceReconcilePage.tsx:809~810` 에 «배송비» 입력칸. 화면 안내 원문 「세금·배송·할인은 품목 단가에 섞지 않습니다. 따로 기록해 두어야 원가가 부풀지 않습니다.」
    - 판매자 «배송비 기준» 칸은 `SupplierCompany.min_order_amount`(DECIMAL) · `delivery_policy`(**TEXT 자유 텍스트**) 둘뿐 — 계산에 쓸 배송비 금액·무료배송 기준 값이 없다.
    - 그 두 칸이 나오는 화면은 `SupplierDirectoryPage.tsx:341,516,542` 의 **외부 공급업체 등록 폼 한 곳뿐**. `SupplierProfilePage.tsx` 에는 없어 **가입한 공급업체는 자기 배송 정책을 화면에서 못 적는다**(백엔드 PUT 은 받음 — `routes/supplier-directory.js:1097,1102`).
    - **브랜드(판매자)에는 배송비·최소주문 칸이 아예 없다** — SupplierCompany 는 공급업체 전용 모델.
    - 발주 담는 화면(`NewPurchaseOrderPage.tsx`)은 판매자별 `subtotal` 합(`:1866`)만 보여준다 — min_order_amount·delivery_policy 를 읽는 코드 0건.
    - `RegisterExternalSupplierModal.tsx` 는 이름과 달리 **공급업체 상품** 등록 모달(업체 등록 아님).
  - **갈리는 지점(판정은 Fable):** 배송비를 발주 자체에 담을지(PO 헤더 칸 / 비품목 라인 / 안 담고 대조의 `invoice_delivery` 유지) · 판매자 «배송비 기준» 을 어디에(SupplierCompany 확장 / 계약 / 브랜드·푸드코트 포함 판매자 공통 자리) · 사람이 적는 값 vs 규칙 자동계산 · 배송비를 재고 원가에 태울지 · 4개 발주 경로에 같은 규칙을 쓸지.
- 🔴 **Fable 판정 대기 (이월 2건)** — 한도(429)로 계속 미수령. 한도가 풀리면 판정 요청 → 받으면 `node dev-backend/scripts/fable-gate.js pass --note "판정 요지"`.
  - (가) **2026-09-14 배포분(SW 5.24)** — 0원 청구서 확정 권한 3조건 · 결제일 입력 · 대조 거부 사유 4언어. **이미 운영 배포됨**, 마커만 미발급.
  - (나) **2026-09-15 배포분(SW 5.26 → 5.29)** — 배지 단일 소스 · 메일 머리글 추가형 · Profile 인증 안내 · 프로덕트↔재고아이템 연결 · 목록 «두 줄» 합치기 · 거래처 회사명. **이미 운영 배포됨**(15:21 UTC), 마커만 미발급. 기계 판정은 «일반 변경»(비대상).
- 🟡 **판정 필요(별건, 아직 안 물음)** — 매장 알림의 «소유주» 범위: **«감독»으로 연결된 브랜드·푸드코트 관리자를 수신자로 볼지**. 그 함수를 청구서·승인 알림도 함께 쓰므로 범위 판정이 필요하다. 실측: K-DINE IPC Branch 는 소속 계정 4명 전원 이메일이 없고 브랜드가 «감독»으로만 연결돼 **구매자 «발주 접수됨» 확인 메일이 아무에게도 안 간다**. 운영 전체로도 «감독» 19건 대 «소유» 3건인데 알림 코드는 «소유»만 본다.
- 🟣 **Fable 구조정리 대기 7건** — `docs/TRADE_STRUCTURE.md` 맨 끝 절(⑦ 배송비를 2026-09-16 에 추가).

### 완료된 작업 (이번 세션 — 2026-09-16)
- **2026-09-15 오후 작업 2건을 기록에 복구했다.** 릴리즈 기록 `2026-09-15-product-stock-link`(14:08 SW 5.26) · `2026-09-15-link-pairs`(15:16 SW 5.29)만 남아 있고 DEVELOPMENT_PLAN·CHANGELOG·session-state 어디에도 없던 것을 옮겨 적었다. 내용: 프로덕트 저장 시 재고아이템 동시 생성(체크 시에만·중복이면 409) · 프로덕트 창에서 공급업체 연결 · «파는 기준/사는 기준/마진» · 발주 검색 시 카탈로그 건수 안내 · 발주 목록 «두 줄» 합치기 · 재등록 차단 · «undefined» → 공급업체 이름 · 버튼 2개 분리 · 매장 어휘 4언어 · 거래처 회사명 단일 소스 · 반송 메일 교체 스크립트 · 타입 구멍 2건(442 → 410건).
- **«미배포» 오표기를 정정했다.** 세 문서 모두 SW 5.25 «미배포» 에서 멈춰 있었으나, 운영 `sw.js` 의 `SW_VERSION` 이 `5.29-seller-name-fix-20260915` 로 개발과 같아 **2026-09-15 15:21 UTC 운영 배포가 실제로 있었음**을 실측 확인했다.
- **배송비 현황 실측** — 위 «진행 중인 작업» 에 사실만 정리. Fable 판정 요청했으나 한도(429)로 미수령.
- 기계 판정: `check-sensitive-diff` **비대상**(운영 스냅샷 0건 + 워킹트리 0건). 코드 변경이 없어 빌드·verify-all 은 돌리지 않았다(돌릴 대상 없음) — **확인하지 않은 것은 확인하지 않았다고 적는다**.

### 다음 확정 작업
- **배송비 설계** — Irene 이 2026-09-16 에 지시. 단, **Fable 판정을 받은 뒤 착수**한다(돈·발주 금액 계약에 닿고 길이 갈린다). 한도가 풀리면 판정 요청이 첫 걸음.

### 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- **브랜드메뉴 역방향**(2026-09-15 Irene 요청) — 매장이 추가·수정한 메뉴를 브랜드가 확인하고 함께 고치는 **상시 흐름**. 현재는 2026-09-13 일회성 도구(`adopt-restaurant-menus-to-brand.js`)만 있다.
- 「모든 등록을 한 번에 · 층별 필수 단위 강제 · 아무렇게나 연결 금지」(2026-09-15 Irene) — 구조 변경이라 Fable 판정 대기.
- **purplehere.com 에 SPF·DMARC 레코드 없음**(실측). 보내는 주소는 `help@purplehere.com` 인데 그 도메인이 발송 권한을 선언한 적이 없다.
- **알림 발송 로그가 messageId 만 남기고 accepted/rejected 를 안 남긴다.**
- **기존 발주의 판매자를 바꾸는 경로가 코드에 없다**(발주 수정은 품목·납기일·주소·메모만). 옮기려면 취소 후 다시 담아야 하고, 취소는 초안·제출됨·승인대기에서만 된다.
- **매장 10 재료 카테고리 2중**(매장 자체 + 브랜드 내려준 것) 정리 — 운영 읽기 계정이 없어 검토표 작성 불가.
- **공급업체 상품 «Active» 더블클릭 시 서버에 쓰기 2회**(PUT 200 ×2) — 클라이언트 중복 가드 없음(실측).
- **저장이 네트워크에서 실패해도 화면 안내 없음**(콘솔에만) — 공급업체 상품·계약·결제설정.
- **요금제 모듈 문은 개발서버에서 검증 불가** — 개발 매장·브랜드 전부 plan_type 이 비어 fail-open. 검증용 계정 필요.
- **K-DINE 신메뉴 66건 적용** — 2026-09-01 지시로 보류(`docs/KDINE_MENU_AND_RECIPE_PLAN.md`).
- **버튼 전수검사 미완 구간** — 모달 일부·드롭다운·모바일 손님 주문 화면·Floor Plan 8건.
- 기존 대기: 상품링크 «배송·픽업 안내»(`docs/BUYER_FREE_TIER_DESIGN.md` §6-4) · 판매자 자체 재고 «재고 단위» 칸(§5-12) · 준비된 재고(1차 가공) 설계.

### Git 상태 (저장 시점)
- 브랜치 `deploy-isolation` (원격 `origin/deploy-isolation`)
- ⚠ 미커밋 상태에서 `git checkout`/`reset --hard` 금지(메모리 feedback_stash_before_deploy_isolation — 영구 소실 2회)

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```
