## 현재 작업 상태
**마지막 업데이트:** 2026-08-22
**버전:** v3.78 (운영 배포 완료 — Backup `20260822_084643`)
**작업 상태:** 완료

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

#### 운영 배포 3건
| 배포 | 내용 | 백업 |
|---|---|---|
| D1 | 소프트 중복 가드가 **정당한 변형 상품 등록을 막던 회귀** 해제 | `20260822_072850` |
| 표시 | 판매가/원가 빠진 상품 배지 + 요약 줄 · 브랜드 거래내역 출처 표시 | `20260822_081522` |
| 상품재고 | **레시피 없는 상품 = 그 자체가 재고 단위** (RA·BG 양쪽) | `20260822_084643` |

#### 1. D1 — 내가 만든 회귀를 되돌린 것
8/22 중복 가드로 소프트 중복(괄호 설명만 다른 변형)을 409 로 막았는데 **프론트가 `force` 를 안 보내**
종이볼 L/M/S 같은 진짜 변형을 등록할 수 없었다. 운영 번들 **234청크 전수** `SIMILAR_EXISTS` 0건으로
확인. 확인 모달 → "Register anyway" → `force:true`. **완전 중복은 force 로도 차단**(운영 실호출 확인).
※ 운영 측 근거 "main.js 0건"은 측정법이 틀렸다 — lazy 청크라 main.js 는 원래 0건. 결론만 맞았다.

#### 2. 빠진 값 표시
판매가 없음=빨강(빠뜨림) / 원가 근거 없음=회색(아직 안 이음). **원가를 경고색으로 안 한 이유**: 운영
754개 전부라 목록이 통째로 붉어져 진짜 문제가 묻힌다. 요약 줄 숫자 클릭 → 그것만 필터, 0 이면 줄 자체
미표시. 공용 `StatusBadge` 사용(로컬 배지 신규 0, 디자인 가드 신규 위반 0).

#### 3. 레시피 없는 상품 = 그 자체가 재고 (핵심)
**확정 모델(Irene): 상품은 둘 중 하나. RA 메뉴·BG 프로덕트 완전 대칭.**
①레시피 있음 → 재료 차감(기존) ②레시피 없음 → **그 상품이 재고 단위**(신설).
②가 없어 캔음료·완제품은 팔려도 재고가 안 줄었다 — `products` 는 칸만 있고 깎는 코드 없음(운영 772개
중 `track_stock=1` **0개**), `brand_products` 는 **칸 자체가 없었다**.
- 매장 주문완료 → `products.current_stock` / 브랜드 매장출고 → `brand_products.current_stock` 차감
- 음수 금지, 못 채운 몫 `[stock_shortfall N]`, 거래기록은 **재료와 같은 표**
  (`inventory_transactions.product_id`/`brand_product_id` 신설)
- 편집 화면 체크박스 + 수량·단위. **레시피 있으면 안 나타남**. 기본값 꺼짐

#### 4. 검증
verify-all **15/15** · 실브라우저 mount 통과 · 🔒 인쇄 **8/8 무접촉** · 인쇄 라우트 34/34 ·
health-check **재고 계약 8건**(각각 깨뜨려 검출력 증명) · 마이그 멱등(재실행 6/6 no-op) ·
운영 실동작 10→7, 과잉판매 0 정지, 거래기록 2건.
**hydration 게이트가 실제 버그 적발** — 메뉴 수정 저장 시 재고가 0 으로 소실(`setFormData` 누락).
배포 전 수정.

#### 5. 접수·검토만 한 것 (미착수)
- 운영 인수인계 D3(`stock_mode`)·D4(매입↔판매 연결)·D5(죽은 컬럼 정리)
- 브랜드 재료 동기화 3건 — 메모리 [[project_brand_ingredient_sync_overwrite]]
- 발주↔인보이스 원가 대조 — 메모리 [[project_po_invoice_cost_variance]]
- **철회**: `track_stock` 일괄 끄기 계획. 끄면 입고해도 재고가 안 잡히고, 내 기준에
  `product_recipe_ingredients` 구멍이 있어 실제 사용 중인 12개가 대상에 들어갔다(실측 확인).

### 다음 확정 작업
- 없음 — 지시 대기

### 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- **D2 적용 자리 확정** — `/api/brands/:brandId/inventory` 는 **부르는 화면이 0건**. 운영이 목격한
  증상("브랜드 행 0 정리 → 전 품목 Out of Stock")은 재료 관리 페이지(`IngredientsPage`,
  `/api/brands/:id/ingredients`)에서 났을 가능성이 크다. 그쪽 확인 후 배포. 코드는 커밋돼 있음.
- 데이터 작업(매장 몫): 재료 수량 입력 → 그 뒤 메뉴↔레시피 연결 84건(`link-product-recipes.js`).
  **순서 반대로 하면 품절 알림만 쏟아진다.**
- 프로필·구독 통합 — 구독이 RA 는 매장 행, 나머지는 유저 행으로 혼재. 유저 행은 칸이 1벌뿐이라
  한 사람이 두 역할 구독을 못 가진다. 로그인 상태에서 역할을 추가 구매하는 경로도 없다.
- POS 기기 고정 해제 옵션 / 흩어진 계정 통합(주 계정 지정 필요)

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```
