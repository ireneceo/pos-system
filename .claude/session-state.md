# Purple POS — 개발 세션 상태

<!-- AUTOSAVE-STALE-BANNER -->
> **[AUTO-SAVE STALE] (2026-07-14 09:05, idle 1784019901s)** — narrative 가 마지막 편집된 이후 작업 파일이 변경됐는데 narrative 가 미갱신 상태로 자동저장됨. /개발시작 진입 시 git HEAD 와 대조해 진행/완료를 정정하고 이 블록을 삭제할 것.
> 변경된 작업 파일: .v4.json
<!-- /AUTOSAVE-STALE-BANNER -->

## 현재 작업 상태
**마지막 업데이트:** 2026-07-13
**버전:** v3.68 (운영).
**작업 상태:** 운영 배포 **5회** 완료 — ①브랜드 재료 실사·발주제안·매장별 PAR ②발주 오너 승인 우회 봉인 + 판매자 유출 차단 ③반품 net 0 ④**FG 출고 차감 누락 + BG/FG 구매자 반품(B1·B2, 마이그 포함)** ⑤배포 스크립트 회귀(운영 health-check 실패시키던 옛 껍데기 파일) + 배포 게이트 fail-silent 수정.
**dev 완료·미배포:** 데스크탑 다운로드 CTA 버전 드리프트 수정(#6) · 안드로이드 V4 게이트 수정(#8, 앱 런타임 무변경).

### 진행 중인 작업
- **안드로이드 M2 (V4 폴러 E2E 게이트)** — 하니스·환경 결함 3건을 고쳤고 **고친 하니스로 완주(6/6)는 아직 못 봄**(Irene 이 세션 마감 지시 — 에뮬레이터가 서버를 굶기던 것 확인 후 내림).
  **다음 세션 첫 명령:** `cd /var/www/mobile-app && PURPLE_AVD=purplepos-ci node scripts/verify/run-v4.js`
  (에뮬레이터가 RSS 약 4.5GB 를 쓴다 — 실행 중엔 프론트 빌드 등 동시 실행 금지. 가용 3GB 미만이면 스크립트가 스스로 기동을 거부한다.)

### 다음 확정 작업
- 🔴 **B1 (윈도우, 🔒보호파일) — Fable 설계·검증 필수**: `billPrint.js` `printTicketHTML` 이 네이티브의 실패(`PRINTER_NOT_FOUND` 등)를 버리고 무조건 `true` 반환 → 폴러가 "인쇄됨" 도장 → **종이 없이 티켓 유실**. 인쇄 생명선이라 미착수. (단, 폴러의 주력 경로인 `method='qztray'` 는 `sendTicketAutoFormat`→`sendViaQZTray`/`sendHTMLViaQZTray` 로 **반환값이 정직함** — B1 은 `method='browser'` 매장에서 터진다. 이번에 코드로 재확인.)
- **안드로이드 V4 완주 → M3 매장 실기기 1회 → M4 배포**(서명·호스팅·CTA). M1(코드) + V3(바이트 게이트)는 완료.
- **V3 를 새 AVD(`purplepos-ci`)에서 1회 재확인** — V3 13/13 은 옛 google_apis AVD 에서 받은 결과다. 기본 AVD 는 아직 `purplepos`(옛것) 라 동작엔 영향 없지만, 옛 AVD 는 서버를 흔든다(아래).

### 완료된 작업 (2026-07-13 #8 — V4 게이트가 3회 실패한 진짜 이유: 앱이 아니라 게이트·환경이었다)
> 세션이 SSH 끊김으로 중단 → 이어받아 실측. **앱 코드는 한 줄도 안 고쳤다**(print-guard 8/8 무변경). 고친 것 = 하니스 + 에뮬레이터 환경.
- **① 픽스처 불일치 (V4-1/2/3 실패의 원인)**: 데모 매장(rid=5)에 **스테이션 프린터 2개가 'BAR' 주소**로 설정돼 있다. `billPrint.printKitchenTicketViaRawBT` 는 스테이션이 하나라도 있으면 **마스터(`kitchenPrinter.address`)를 아예 안 쓴다**(station 분기). 하니스는 태블릿에 'KITCHEN' 만 등록하고 마스터만 설정 → **앱은 'BAR' 로 인쇄 시도 → 태블릿 미등록 → 0바이트**. 게이트가 자기 픽스처 때문에 앱을 범인으로 몰았다. → 이제 **판정 대상 설정을 전부 명시**(마스터 + 스테이션)하고 **앱이 실제로 읽는 API GET 으로 되읽기 검증**.
- **② 에뮬레이터 환경 (마지막 실행이 "bridge timeout" 으로 죽은 원인)**: AVD 가 **google_apis(GMS 포함)** 이미지 + RAM 1.5GB. 메모리 빠듯한 서버에서 **GMS 가 ANR 로 죽으면서 앱을 같이 kill** (`Killing com.purplehere.pos.mobile: depends on provider ...FontsProvider in dying proc com.google.android.gms.persistent`). → **GMS 없는 AOSP AVD `purplepos-ci`**(RAM 2GB) 신설, V4 기본값으로. 게이트에 필요한 건 WebView 이지 Google Play 가 아니다.
- **③ V4-5 판정 오류 (마지막 실행의 "티켓 유실" 은 오탐)**: `needs_print` 스냅샷으로 판정했는데 **`print-claim` 이 인쇄 시작 시점에 이미 `needs_print=0`** 으로 만든다 → "인쇄 중"과 "인쇄됐다고 도장 찍음"을 구분 못 함. → **품목 `printed_at` 도장**(PATCH `/printed` 만 찍는다)으로 판정 교체 + 폴러가 **정착(재무장)할 때까지 대기** 후 판정.
- **게이트 강화**: **V4-6 스테이션 라우팅 신설** — 실매장 표준(KQ1/KQ2/BAR)이 이 경로다. 가짜 프린터 2대(9100=KITCHEN, 9101=KQ1)로 **"티켓이 나왔나"가 아니라 "옳은 프린터로 나왔나"** 를 판정(스테이션 1장 + 마스터 0장). 앱 사망 감지(죽은 앱을 90초 폴링 후 "브릿지 없음"으로 오보고하던 것).
- **🛡️ 서버 보호 (Irene 지적)**: 에뮬레이터는 **RSS 약 4.5GB** — 이 서버(7.9GB, dev-backend/MySQL/PlanQ 공존)에서 띄우면 여유가 200MB 까지 떨어진다. **SSH 가 끊긴 것도 이 자원 압박이 원인으로 보인다**(내리자마자 6.8GB→2.1GB 사용). → V3·V4 둘 다 **가용 3GB 미만이면 기동 거부**(fail-loud). 에뮬레이터 실행 중엔 프론트 빌드 등 동시 실행 금지.
- **🛡️ 중단돼도 매장 원복**: 게이트는 데모 매장 프린터를 **실제로 갈아끼운다**. 중단된 실행이 데모 매장 스테이션 설정을 지운 채 남긴 사고를 겪음 → **모든 종료 경로(크래시·SIGINT/TERM/HUP)에서 설정 원복 + 테스트 주문 정리**. (이번 사고분은 수동 복구 완료 — 25/26 → 'BAR' 확인.)
- **부수 확인 (제품 동작, 변경 안 함)**: 설정 API 는 **스테이션 프린터를 지울 수 없다** — 빈 맵/키 누락을 "미로드"로 보고 기존값 보존(`utils/settingsGuard`, thefire 설정 wipe 사고 대응 자물쇠). 의도된 동작이라 손대지 않았고, 하니스 픽스처만 DB 직접 쓰기로 우회.
- **남은 것**: 고친 하니스로 **V4 완주(6/6) 미확인** — 다음 세션 첫 작업. 변경 파일 = `scripts/verify/run-v4.js`, `scripts/verify/run-v3.js`(메모리 게이트만).

### 완료된 작업 (2026-07-13 #7 — 안드로이드 P0 구현 + 하드웨어 없는 인쇄 게이트, dev 완료 · 미배포)
> Irene "안드로이드 P0 구현 시작해" · 설계 = **Fable**(`mobile-app/docs/ANDROID_APP_DESIGN.md` §8)
- **구조 판정(Fable)**: 프린터 **레지스트리 데이터는 기기 로컬 유지가 정당**(윈도우도 printer_settings + OS 스풀러 2층 구조. 안드로이드엔 스풀러가 없어 그 자리를 레지스트리가 대신 — 두 저장소는 서로소라 드리프트 불성립). 단 **네이티브 설정 화면은 삭제** — 무인증으로 프린터를 바꿀 수 있어 "프린터 설정=RA 전용" **권한 게이트의 뒷문**이었다. 등록 UI = 웹 Settings 전담.
- **계약**: §4(`__NATIVE_PRINT`) **무확장**. 등록·BT권한은 Android 전용 `__NATIVE_PRINT_SETUP` 별도 객체 → **윈도우 동작 바이트 동일**, `billPrint.js` **0줄**.
- **고친 결함**: ①printHtml **레인 결함**(렌더+전송이 공용 html 레인 → 죽은 주방 프린터 1대가 **빌 인쇄까지 전부 차단**) → 렌더=html 레인, 전송=목적지 레인 ②LAN **연결 타임아웃 없음**(죽은 IP 가 레인 2분 점유) → 5s/10s ③BT 권한 콜백 없음 → `@PermissionCallback` + denied_forever 안내 ④diagnostics 계약 불일치 + **버전 하드코딩 2곳**(= 데스크탑 CTA 드리프트와 동일 클래스) → `BuildConfig.VERSION_NAME` 단일소스 ⑤릴리즈 URL 런타임 스왑(→ origin 변경 시 **플러그인 주입 소실 = 릴리즈 인쇄 전멸** 위험) → 빌드시점 확정(`PURPLE_APP_URL`) ⑥중복 플러그인 사본 삭제.
- 🔴 **V3 게이트가 잡은 진짜 버그**: `OffscreenHtmlRenderer` 가 **unattached WebView 에 `View.postDelayed`** 를 걸어 콜백이 **영원히 실행되지 않음**(뷰가 window 에 attach 될 때까지 큐에서 대기 → 오프스크린이라 영영). 결과 = **모든 HTML(한글) 티켓이 20초 타임아웃**, 로그 0줄. raw/드로어는 WebView 미경유라 멀쩡 → "APK 빌드됨"만 믿었으면 매장에서 "한글 주방티켓만 안 나옴"으로 터졌다. 수정 = 메인 루퍼 핸들러 + 모든 종료 경로 latch 해제 + 높이 0 시 재시도 후 명시적 실패(백지 금지).
- **V3 게이트 신설·완주 (하드웨어 0)**: `scripts/verify/run-v3.js` + `expr/v3.json` (설계엔 있었지만 입력 파일 부재로 **한 번도 안 돌던 하니스** 완성). 에뮬레이터+가짜 프린터 2대로 **실제 바이트 판정 13/13**: raw 바이트 ≡ base64 디코드 · 0x00~0xFF 무손실 · 드로어 정확히 `1B 70 00 64 64` · 미등록 이름 → PRINTER_NOT_FOUND **+ 유출 0바이트** · 빈 이름 조용한 폴백 없음 · 대소문자 무시 · 같은 프린터 도착순=호출순 · **한글 래스터 10,169B/576px/컷/잉크 6.7%(백지 아님)** · **죽은 프린터 5.1s 자기만 실패, 정상 프린터 233ms 인쇄**(전엔 40초 블록) · 레지스트리 왕복.
- 프론트: Settings **"이 태블릿의 프린터" 카드**(네트워크 프린터 등록/삭제, 페어링 BT 목록, 권한 요청·앱설정 안내) + 안드로이드에선 "기본 프린터" 옵션 숨김(안드로이드 browser 인쇄=PDF 다이얼로그) + **i18n 4언어**. 변경 = `SettingsPage.tsx` 1파일.
- 검증: print-guard **8/8 무접촉** · verify-all **13/13** · 프론트 빌드 경고 신규 0 · i18n 통과.
- **남은 것**: V4(폴러 E2E, 서버서 가능) → **매장 실기기 1회**(BT 실전·종이 품질·드로어 물리 킥) → 서명·호스팅·CTA 배포.

### 완료된 작업 (2026-07-13 #6 — 데스크탑 다운로드 CTA 버전 드리프트, dev 검증완료 · 미배포)
> Irene "윈도우 네이티브앱 제대로 구현됐는지 전체 검증" 중 적발 → "CTA 버전 고정 문제 고쳐줘"
- **결함**: 다운로드 CTA 가 프론트에 박힌 상수(`DESKTOP_APP_VERSION='0.1.6'`)로 URL 을 만들었다. 운영 피드는 **0.1.7** → **신규 매장이 구버전 설치본을 받았다**(운영 번들에서 실측 확인). 상수는 릴리즈마다 손으로 따라가야 해서 반드시 어긋난다.
- **수정(정석)**: 버전 복제를 없앴다. CTA 가 `/desktop/latest.yml`(**electron-updater 가 읽는 그 피드 = 단일 진실**)에서 파일명을 읽고, 못 읽으면 항상-최신 별칭 `/desktop/PurplePOS-Setup.exe` 로 폴백. 변경 = `PwaInstallContext.tsx` 1파일.
- **회귀 박제**: `scripts/check-desktop-feed.js`(verify-all 등록, fail-closed) — ①피드가 가리키는 exe 실재+sha512 일치 ②별칭이 최신본과 바이트 동일 ③프론트에 버전 하드코딩 없음. **게이트 자체를 고장 주입으로 실증**했고, 첫 판이 옛 회귀(템플릿 리터럴)를 못 잡아 규칙을 고쳤다.
- **검증**: 실브라우저(Windows UA, 로그인) CTA = `PurplePOS-Setup-0.1.7.exe` 링크 200·크래시 0 · **피드 강제 500 → 별칭 폴백** 실증 · verify-all **13/13** · print-guard 8/8 무접촉.

### 완료된 작업 (2026-07-13 #5 — 네이티브앱 전체 검증, 코드 무변경)
> Irene "윈도우 네이티브앱 제대로 구현된 건지 전체 검증 + 안드로이드는 어떤 단계인지"
- **윈도우(Electron 0.1.7)**: 브릿지 계약(§4)은 **적합**. 실행 검증 — 인쇄 유닛 6/6 · 헤드리스 런타임 스모크 **9/9**(한글 HTML 233KB 실렌더=백지 아님, 없는 프린터 → `PRINTER_NOT_FOUND` 명시 실패, raw LAN, 드로어) · 인쇄 라우트 가드 **34/34**(web QZ ↔ native 분기 각각) · print-guard 8/8. 운영 배포본 0.1.7 ↔ 소스 **드리프트 0**, 피드 sha512 **일치**.
- **미검증으로 남는 것**: ①**실프린터 종이 확인**(윈도우 드라이버 raster leg — 리눅스 서버에선 원천 불가, 매장 1회 필요) ②**실사용 흔적**(운영 nginx 로그는 sudo 비번 필요 — Irene 만 확인 가능) ③미서명(SmartScreen 경고) ④Win7 미지원(공존 유지).
- **적발(Fable 대상)**: B1(위) · 설계문서 §5 "8 절단면" 이 실제 13곳으로 어긋남(문서가 현실을 못 따라감) · §6-1 `pageSize/widthMm` 은 의도적으로 제거됐는데 문서엔 남아 있어 **다음 에이전트가 백지 버그를 "복원"할 위험**.
- **안드로이드**: **A2(debug APK 빌드)까지, A3 실기기 검증부터 공백.** Kotlin 플러그인(295줄)·DantSu 라이브러리·브릿지 등록은 진짜. 그러나 ①프린터 등록 UI 미노출 = 인쇄 사실상 불가(P0) ②설계된 하드웨어 없는 검증게이트 V3/V4 **한 번도 미실행**(cdp-eval 이 요구하는 표현식 파일이 리포에 없음 = 하니스 미완) ③릴리즈 서명·호스팅·CTA 전무, 릴리즈 빌드 시 `allowNavigation` 미설정으로 시스템 브라우저 튕김 위험 ④LAN 소켓 연결 타임아웃 없음(설계는 5초). **"APK 빌드"를 "동작"으로 착각한 상태 — 아무도 인쇄되는 걸 본 적 없음.**

### 완료된 작업 (2026-07-13 #4 — 배포 게이트 fail-silent 수정)
> 반품 배포 1차 시도가 **안전게이트 9/9 통과 후 조용히 exit 2** 로 죽은 것에서 출발. (운영 무피해 — 백업·rsync 이전 단계)
- **원인**: `deploy-to-production.sh` 가 `node compare-schema.js --export ... 2>/dev/null` 로 **stderr 를 버렸다.** compare-schema 는 export 실패 시 `exit 2` → `set -e` 가 **아무 말 없이** 스크립트를 종료. (단독 실행 5회는 전부 성공 — 일시적 DB 경합으로 추정)
- **더 위험한 것 발견**: **운영 백업 `cp` 가 `2>/dev/null || true`** 라 **실패해도 넘어가면서 "Backup created" 를 찍고 있었다** → 백업 없이 배포 = **롤백 불가**.
- **수정**: ①스키마 export = stderr 표시 + **3회 재시도** + 실패 시 원인 찍고 `error` ②백업 = 실패 시 `error` + **실제 생성 검증**(파일 수 확인) 후에만 진행. 나머지 `2>/dev/null` 은 정보성(존재 확인 등)이라 유지.
- **검증**: 일부러 깨뜨려 fail-loud 실증(사유 3회 출력 + `[ERROR]` exit 1) · 정상 경로 유지(152 tables) · 실제 운영 백업으로 검증 로직 확인(54 파일 인식) · **회귀 박제**(옛 패턴으로 되돌리면 health-check 가 정확히 그 1건을 실패 — 실증) · pos **38/38** · verify-all 12/12 · print-guard 8/8.
- **원칙**: 배포 게이트는 **fail-closed 는 맞지만 fail-silent 는 안 된다.** 왜 막혔는지 모르면 다음 사람이 `--skip-safety` 로 우회한다. 단일 진실 = `DEPLOYMENT.md`.

### 완료된 작업 (2026-07-13 #3 — 반품 net 0, **운영 배포 완료**)
> Irene "2번 반품 net 0 조사해줘" → 조사 결과 **진짜 버그로 실증** → "수정해" · 설계·검증 = Fable
- **결함**: `po-returns.js` 브랜드 판매자 반품 환원이 **판매자 재고가 아니라 구매자의 재료 행**을 올렸다(`ret.ingredient_id` = 매장 재료 id). 1단계에서 깎은 **같은 행**이라 −q 후 +q = **net 0**.
  - **실증**: 브랜드 판매자 PO 반품 5개 승인(200) → **매장 재고 20 → 20.00 (변화 0)**. 15가 되어야 했다.
  - 피해 ①매장 재고 과다계상(반품해도 안 줄어듦 → 발주제안·부족알림·실사 오염) ②브랜드 **본사 재고(ProductIngredient) 과소계상**(출고 때 BOM 으로 깎았는데 반품이 안 되돌림) ③브랜드 표준 재료면 **재료 마스터 행의 current_stock 오염**.
- **수정(Fable A안)**: 브랜드 분기 = **출고의 정확한 역방향** — `BrandProduct` → `product_recipe_id` → BOM → `ProductIngredient` 복원 + `return_in` 원장. 레시피 없으면 환원 없음(출고 대칭).
- **Fable 이 함께 잡은 3건**: ①**환산 누락**(입고는 `qty×conv` 더하는데 반품은 conv 없이 뺐다) ②**원장 구멍**(입고는 기록 남기는데 반품은 아무것도 안 남김 → `return_out`/`return_in` 2건) ③**이중 승인 레이스**(동시 승인 2발 → 재고 2회 환원 + CN 2장 → 행 락으로 직렬화) ④**누적 초과 반품**(5개 입고에 5개 반품 3번 요청해도 통과 → 생성·승인 합산검사).
- **Fable 이 내 구현에서 잡은 것**: 클램프 시 원장이 실재고와 어긋남(재고 3에 5 반품 → 재고 0인데 원장 −5) → **실제 이동량 기록**으로 수정(3 + (−3) = 0 정합 실증). Fable 본인 설계 오류라고 인정.
- 검증: 실호출 **11/11** + 클램프 케이스 + **Fable 독립 재현**(conv=2·동시승인 레이스·IDOR·레시피 없는 경우) · health-check `pos` **37/37**(반품 회귀 2건 신규) · verify-all 12/12 · print-guard 8/8 무접촉 · **변경 = po-returns.js 1파일**.
- **운영 영향 0** — 반품 이력 0건(오염 데이터 없음). 단 브랜드 판매자 PO 5건 존재 → **첫 반품에 터질 상황이었다**.
- **Fable 배포 전 최종 검토**: 독립 실호출 **42/42**(브랜드 공유 재료 오버레이 반품까지) + **운영 하류 전수 확인** — 새 원장 타입(`return_in`/`return_out`)이 재고 집계·거래내역·CN 하류 어디도 안 깨뜨림(재고는 원장 합산이 아니라 current_stock 단일 소스). 롤백 = 파일 되돌리기(스키마 부채 0).
- **배포 후 운영 확인**: 사이트 200 · API 200 · 기동 에러 0 · 스모크 9/9 · **운영 health-check pos 37/37** · 반품 이력 0 · CN 0 · 새 원장 0(정상) · **반품 대기 브랜드 발주 2건**(PO-R13-...001 received / ...003 delivered = 위험했던 바로 그 발주들).
- **첫 실반품 시 확인**: 매장 재고 감소 + 본사 재고 증가 + `return_out`/`return_in` 원장 쌍 + CN 1장(`CN-{PO}-R{id}`).

### 완료된 작업 (2026-07-13 #2 — 발주 오너 승인 우회 봉인)
> Irene "레스토랑 발주를 오너가 승인하는 거 확인 + 설정 토글 + 오너 연결이 프로필에 뜨는지" · Fable 전체 검토
- **요청 3건은 이미 다 있었다** (2026-06-21 구현): 오너 승인 흐름 정상(승인→submitted / 반려→draft+사유 / 크로스오너 404 / 셀프승인 403) · **설정 토글 = 설정→운영(Operations) 탭** "Require Owner approval for purchase orders"(오너 연결 시 기본 ON, OFF 면 매장 직접 발주) · **오너 연결 = 설정→Managers 탭**에 브랜드/푸드코트 매니저와 같은 목록으로 노출.
- **진짜 결함 = 승인 우회 3경로** (Fable 적발). 승인 판정이 submit 라우트 **안에만** 있어서:
  - 🔴 **일괄발주(재고관리 Bulk Order)** 가 게이트를 통째로 건너뛰고 바로 발주 — **직전에 브랜드 재료를 이 경로에 태웠으므로 브랜드 재료 발주에도 열려 있던 구멍**
  - 🔴 **외부업체 수동전송(Mark as Sent)** 도 draft→submitted 직행
  - 🔴 **승인 대기 발주를 입고 처리로 끝낼 수** 있었다(승인 무력화)
- 수정: `utils/poOwnerApproval.applySubmitGate` **단일 게이트**로 3경로 통일(복붙 금지 — 새 발주 경로가 생기면 또 뚫린다). mark-received 화이트리스트 · 승인대기 **철회 가능** · 승인대기 수량을 **입고예정(on-order)에 집계**(중복 발주 방지) · 오너 배지 색 · Bulk 모달 상태 라벨 i18n.
- 검증: 실호출 **10/10** · health-check `pos` **34/34**(승인 회귀 3건 신규) · verify-all **13/13** · 문서 `PURCHASE_ORDER_SYSTEM.md §G-5` 신설.
- **운영 영향 0** — 운영에 오너 연결된 실매장 0곳(테스트 매장만), `pending_approval` 방치 발주 0건. 잠재 경로 봉인.

### 완료된 작업 (2026-07-13 — 브랜드 재료 실사 · 발주 제안 · 매장별 PAR)
> Irene "브랜드 재료 실사랑 발주 제안도 해줘" · 설계·판정 = **Fable(B안)** · 문서 = `docs/BRAND_STOCK_SHARING_DESIGN.md` "후속 ①"
- **판정 B — PAR(발주점·리드타임·사용량)은 매장별.** 프랜차이즈 표준: 본사가 재료를 표준화하고 **PAR 은 지점이 정한다**(10석 매장과 100석 매장이 같은 발주점을 쓸 수 없다). 결정적으로 **`calculate-usage` 가 이미 브랜드 공유 행을 오염**시키고 있었다 — 형제 매장 입고까지 합산해 공유 행에 써서 **전 지점의 사용량을 덮었다** → 매장별 저장소가 어차피 필요.
- **스키마**: 새 테이블 없이 기존 오버레이(`restaurant_ingredient_stocks`)에 **nullable 컬럼 8개**(NULL=브랜드 기본값 상속). **0 은 유효한 오버라이드** → 상속 판정은 `!= null`(falsy 금지).
- **실사**: 대상에 브랜드 재료 포함(기대재고=매장 오버레이) · 완료 시 **오버레이만 갱신**(브랜드 행·형제 매장 불변).
- **발주 제안 — 두 곳 모두**: 대시보드(`/inventory/reorder-suggestions`) + **Bulk Order 체크박스(`/purchase-orders/suggestions`)**. 후자는 Fable 이 적발(한쪽만 고치면 "부족한데 담을 수 없는" 반쪽). ⚠ 브랜드 분기에 `min_stock>0` SQL 필터 금지(브랜드 행은 0, 임계치는 오버레이).
- **부족/품절·알림 임계치**도 매장 기준(`stockAlerts` + 주문 자동차감 2곳).
- **기존 IDOR 봉쇄**: 실사 detail·items·complete·cancel 4곳이 `stockTakeId` 매장 귀속 미검증 → **남의 매장 실사 열람·조작·완료 가능**했다. 교차 실사항목 id 주입도 차단.
- 프론트: 브랜드 재료도 Settings 열림(안내: 정의는 브랜드, PAR 은 이 매장) · 실사·발주제안에 `Brand` 표식. **재료 정의(이름·단위·공급처·삭제)는 여전히 브랜드 전용.**
- 검증: 실호출 **29/29**(내) + Fable 독립 **41/41 + 9/9** · health-check `pos` **31/31**(회귀 4건 신규+1건 갱신) · verify-all **13/13** · print-guard 8/8 무접촉.
- Fable 지적으로 수정: 내 회귀 테스트의 **파괴적 정리**(그 매장·재료의 실사 이력·알림 전부 DELETE — health-check 는 운영에서도 도는 스크립트) → 테스트 생성분만 id 스코프로 삭제.

### 완료된 작업 (2026-07-12 #2 — 운영 배포 완료)

#### 브랜드 재고 공유 (신규 기능, 운영 배포 · Backup 20260712_201511)
> Irene "공급업체·스톡아이템도 브랜드에 연결하면 레시피처럼 공유돼야 한다. K-DINE with MIN 기준으로." · 설계 = `docs/BRAND_STOCK_SHARING_DESIGN.md`
- **Fable 구조판정 C안** — 내가 제안한 `product_ingredients` 통합은 **기각**. 그건 *본사 자체 구매 재고*(current_stock/PAR/실사 = buyer 재고)라 브랜드 표준 재료와 **성격이 다르다**. 통합해도 K-DINE 은 계약 0건이라 발주 불가 → 전제가 무너짐. 정석 = **이미 있는 구조를 잇는다**.
- 브랜드 재료(`ingredients.owner_type='brand'`, 운영 270건) → 소속 매장에 **읽기전용** 공유. 매장은 발주·입고·레시피에 사용, **수정·삭제·공급처 연결은 브랜드 전용**.
- **신규 테이블 1개** `restaurant_ingredient_stocks`(매장별 실재고 오버레이) — 브랜드 공유 행의 current_stock 을 매장이 갱신하면 형제 매장 재고가 오염된다. 단가는 기존 `restaurant_ingredient_costs` 와 대칭. **데이터 이관 0**.
- 접근·재고 규칙 **단일 소스** = `utils/brandStockAccess.js`(readable/writable/stockFor/applyStock). brand_id 는 **항상 서버 조회**(형제 브랜드 누출 방지).
- **K-DINE 이 막혀 있던 진짜 원인** = `buyerScope` 가 BG 를 primary 브랜드(brand 1)에 고정 → **두 번째 브랜드(K-DINE=brand 2) 재료엔 공급처를 붙일 수조차 없었다**(그래서 운영 매핑 0건·계약 0건). → 소유 브랜드로 전환 허용(`?entity_type=brand&entity_id=N` + `Brand.owner_id` 검증, `isBrandManager` 와 동일 기준). 프론트도 와이어링(안 하면 **primary 브랜드로 잘못 귀속**되는 침묵 버그).

#### 함께 봉쇄한 기존 결함 (전부 이번 절단면 안)
- **IDOR 5개**: `/inventory/receive` · `/inventory/deduct` · PAR settings · 재료 `PUT` · 재료 `DELETE` 에 **소유권 검사가 아예 없었다** — 남의 매장 재료를 id 만으로 입고·차감·수정·삭제 가능.
- **`/inventory/deduct` 항상 500** — 라우트 분리 때 `checkAndCreateAlert` 헬퍼가 유실(호출 화면이 없어 미발견) → `utils/stockAlerts.js` 로 공용화(알림은 매장 스코프 — 안 그러면 A매장 입고가 B매장 알림을 지운다).
- **주문 차감·FIFO 배치가 브랜드 행을 깎던 것** (Fable P0 적발) — 입고=오버레이 / 차감=브랜드행 이면 **이중장부**가 된다. 폐기·PO 반품 환원도 동일 분기.
- **레시피 재료 소유권 검증 없음** — 운영에 **타 브랜드 재료 참조 1건 실재**.

#### 부록 (공급업체 혼동 정리)
- **Direct → External(외부업체)** 명칭 변경(4언어) · **Find Suppliers 에서 외부업체 제외**(= 가입 공급업체를 찾아 계약 신청하는 곳) · 외부업체 프로필의 **계약 UI 제거**(자동계약은 발주를 열어주는 내부 장치일 뿐) · 운영 테스트 잔재 `__EDIT_DELETE_TEST__` 삭제.
- 확인: **외부업체는 유저 계정이 생기지 않는다** (운영 39곳 전부 owner_id NULL, supplier 역할 유저는 데모 1개뿐).

#### 발주 리스트 보기 재작성
- 원인: **카드 마크업을 grid 행에 흘려넣어** 열과 자식이 어긋남 → 가운데 공백·정렬 붕괴. 행마다 별개 grid 라 `auto` 열은 행별 폭이 달라짐. 미디어쿼리가 **뷰포트**를 봐서(카트 패널 때문에 1100px 창에서도 리스트 실폭 490px) 이름 열이 35px 까지 찌그러짐.
- 수정: **5열 전용 마크업**(이름+배지/분류/공급처/가격/액션) + **컨테이너 쿼리**(리스트 실폭 기준) + 가격·액션 고정폭(행 간 정렬 보장) + **행높이 44px 균일**. 토글은 **POS Image/Compact 와 동일 디자인**(연회색 트랙+흰 활성칩) · 우측정렬.
- 실측: 5개 폭 전부 열정렬 ✓ · 행높이 44px · pageerror 0.

#### 검증
- 실호출 30/30(읽기전용·공급처 읽기/쓰기403·발주 생성/타브랜드 차단·입고 오버레이·**실주문 차감 오버레이**·IDOR 404·본사체인 무접촉) · health-check `pos` **27/27**(브랜드 재고 회귀 **6건 신규 박제**) · verify-all **13/13** · print-guard 8/8 무접촉.
- **Fable 최종 게이트: 승인** (C1 deduct 500 · C2 죽은 토글 수정 후).
- **운영 실검증 11/11** — 테스트 매장(rid=5)에서 주문 생성 → 단계이동(preparing→ready→served) → **현금 결제 completed** → 인쇄 계약(동시 claim 5개 중 **1개만 승리**, printed 후 재인쇄 0) → 주문 삭제·인쇄 플래그 정리(**종이 안 나감**).

### 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- **Find/Contracts 페이지의 BG 브랜드 선택 UI** — 두 번째 브랜드 명의로 *가입 공급업체* 계약을 신청하려면 필요(외부업체 경로는 이번에 해결됨).
- **B1 (Fable 적발) FG 출고 차감 누락** — 푸드코트 판매자는 출고(ship) 때 재고를 **아예 안 깎는데** 반품 승인은 `FoodcourtProduct.current_stock` 을 늘린다(반대 방향 비대칭 → FG 재고 인플레). 근본 = ship 에 foodcourt 분기 추가.
- **B2 BG/FG 가 구매자일 때의 반품** — ①반품 1단계가 `entity_type='restaurant'` 만 처리 ②`PurchaseOrderReturn.ingredient_id NOT NULL` 인데 BG 구매 라인은 `product_ingredient_id`(ingredient_id=null) → 반품 생성이 500. 스키마 변경 필요.
- **B3** 반품이 InventoryBatch(FIFO) 를 역전시키지 않음(원가 미세 오차) · **B4** 크레딧노트 tax=0.
- 반품 회귀 2건이 소스 grep 기반 → Fable 의 실호출 시나리오(conv≠1 + 동시승인 레이스)를 fixture 회귀로 승격.
- with MIN 매장 인쇄 1회 물리 테스트(사람 필요) · timezone(242)·design(310) baseline 부채 · E2E 시나리오 확장

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```

> 전체 완료 아카이브(과거 세션 상세)는 `DEVELOPMENT_PLAN.md`. 이 파일은 진행/다음/후속의 단일 소스.
