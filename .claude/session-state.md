## 현재 작업 상태
**마지막 업데이트:** 2026-09-14 #2 (레시피 태그 클릭 이동 — 개발서버 반영, 미배포)
**버전:** 변경 없음 (Irene 미지시) · SW 5.22-recipe-tag-20260914
**운영:** SW `5.22-recipe-tag-20260914` · 04:35 배포 · 스모크 10/10 · 직전 배포 2026-09-13 19:44(SW 5.21, 백업 `/var/www/backups/20260913_192703`)
**작업 상태:** 완료. ⚠ Fable 판정 미수령(한도 429 지속) — 릴리즈 기록 `fable_note` 에 명시. 민감 판정 «일반 변경»(비대상).

---

## 👉 Irene 님이 하실 일 (Claude 가 대신 못 하는 것만 — 급한 순서)

> Claude 는 이 목록을 `/개발시작` 때 **그대로 안내**한다. 하나 끝내면 Claude 에게 «N번 했어»라고만 말하면 된다.

### 0-A번. (오늘 배포됨 04:35) 브랜드 메뉴 레시피 표시 — 약 1분
1. `purplehere.com/pos/brand-menus` 에서 Ctrl+Shift+R
2. 레시피가 연결된 메뉴에 **레시피 이름**(보라색)이 보이는지 — K-DINE 기준 105개 중 65개
3. 연결 없는 메뉴에 **«레시피 없음»**(회색)이 보이는지
- ⚠ 로그인은 **`help@gitconsulting.group`**(표시 이름 gitconsulting). `irene@gitconsulting.group` 은 소유 계정이 아니라 비어 보입니다.

### 0-B번. (2026-09-13 배포분) 브랜드 화면 — 약 2분
1. **브랜드 레시피**·**재고원장** 메뉴가 열리는지(대시보드로 안 튕기는지)
2. 아무 주소나 틀리게 쳤을 때 흰 화면 대신 안내 화면이 나오는지

### 0-C번. (2026-09-13 배포분) 상품 종류 3갈래 + 포장단위 `ea` — 약 5분
1. 상품 창에 «재고 상품 / 주문제작 / 서비스·기타» 2. «서비스/기타» 고르면 단위에 `hour`
3. 4시간 서비스(`ea` · 용량 4 · `hour`) → «4 hour/ea» 4. 서비스만 담긴 주문에 «완료 처리» 버튼

### 0-D번. (2026-09-13 배포분) B2B 무료 발주 등급 — 약 10분
1. 주문용 상품 링크 만들기·QR 2. 로그인 안 된 브라우저에서 열기 3. Mobile Order 의 «온라인 메뉴판» 카드
4. 유료 매장 요금제 목록에 «발주 전용(무료)» 가 **안 보이는지** 5. 브랜드 계약 관리 탭

### 1번. 운영 발주 32 손 수정 — 대조 화면에서 3줄 (손글씨라 자동으로 안 채워짐)
### 2번. 운영 «piece» 대기 발주 2건 — 담기 화면에서 줄 지우고 다시 담기
### 3번. 발주 33 의 단위 확인 — 말로 «맞아/아니야»만
### 4번. (급하지 않음) 운영 DB «읽기만 되는» 계정 — 약 5분
### 5번. 운영서버 nginx 설정 폴더의 백업 파일 치우기 — 1분

---

### 진행 중인 작업
- 🔴 **Fable 판정 대기 (2026-09-14 Irene 지시 «2번 — 기다린다»)** — 2026-09-14 배포분(SW 5.24)이 돈·권한 경계라 정지 훅이 걸려 있다.
  - 한도(429)로 오늘 3회 모두 실패. **우회(`.claude/.fable-gate-skip`)는 하지 않았다 — Irene 이 «기다린다» 를 골랐다.**
  - 한도가 풀리면: Fable 에 판정 요청 → 받으면 `node dev-backend/scripts/fable-gate.js pass --note "판정 요지"`.
  - 판정에 넘길 사실: ①0원 청구서 확정 권한을 «0원 + 낼 사람 본인 + status=paid» 세 조건에서만 열었다(`routes/invoices-crud.js:549~561`), `payer_id` 의미가 종류마다 달라(restaurant=매장id / brand_manager·foodcourt_manager=사용자id) 종류별로 나눔 ②결제일 입력(`utils/paidAtInput.js` 단일 규칙, 미래 거부) ③대조 거부 사유를 코드+4언어로(거부 조건 자체는 무변경).
  - 검증: 권한 실호출 4/4(반증 2 포함) · 결제일 단위 6/6 + 고장주입 · 화면 5/5 · verify-all --full 18/19(deploy-ready 만).
  - ⚠ **이미 운영 배포됨**(17:03, 스모크 10/10). 마커만 미발급 상태.

### 완료된 작업 (이번 세션 — 2026-09-14)
- **레시피 태그를 누르면 그 레시피 화면으로 이동(개발서버 · 미배포).** `/pos/recipes?tab=recipes&brandId=..&search=<레시피명>`. 주소 규칙은 실측(`RecipeManagementPage` tab·brandId / `RecipesTab.tsx:803~808` search). 실클릭 검증 3/3 — 이동·주소 검색어·**검색칸에 값이 실제로 들어감**. 게이트 18/19(실패는 배포기록 부재).
- **브랜드 메뉴에 «연결된 레시피» 표시 + 운영 배포(SW 5.22).** 서버는 `linkedRecipe`(이름·id)를 목록·상세에 이미 내려주고 있었고 화면만 그리지 않던 것. 표시 전용 · 4언어. 실브라우저 렌더 2/2(양성·음성 대조) · 게이트 18/19 · mount sweep 크래시 0 · 스모크 10/10.
- **구조 이해 오류 2건 정정(기록까지).** (가) 「푸시 갱신 경로에 레시피 연결이 없다」 → 실제로는 `brandMenuSyncService.js:231` 에 «매장이 지정했으면 덮어쓰지 않는다» 규칙이 이미 있었음. (나) 「재료는 브랜드·가격은 매장은 담을 칸이 없다」 → 실제로는 이미 구현된 구조(매장 원가층 `restaurant_ingredient_costs` 운영 77행 · 재료 거울 158 · 공급업체 연결 831 · 재고 오버레이 159). 둘 다 **좁게 보고 단정**한 것이 원인이며, `docs/BRAND_MENU_SYSTEM.md`·이 파일에서 실측값으로 교체했다.
- **레시피 재푸시 규칙은 바꾸지 않기로 결정.** 브랜드 메뉴에 연결된 매장 상품 492건 전수 실측 — 어긋난 것 1건(다음 푸시 때 자동 정합) · 둘 다 있는데 다름 0건 · 덮어쓰기로 바꿔도 바뀔 데이터 0건. 매장 고유 레시피를 지울 위험만 있어 현행 유지.
- **Fable 구조정리 5건 접수** — `docs/TRADE_STRUCTURE.md` 맨 끝. **사실·파일/줄·측정일만** 적고 판단은 안 적었다.
- **운영 건강 점검(주문·결제·인쇄)** — 기계 게이트 17/18(실패는 배포기록 부재뿐) + 운영 실데이터: 7일 주문 completed 269/cancelled 46 · **이중결제 의심 0건** · 인쇄 대기 2,813건은 자동인쇄 미사용 매장의 알려진 패턴(매장8 2,587 · 그 매장은 주방프린터 설정 자체가 없음).
- **브랜드 메뉴 삭제 동작 확인** — 매장 메뉴는 지워지지 않고 **연결만 끊긴다**(`brand_menu_id=null` · `unlinked` · 잠금 해제, `brandMenuSyncService.js:467`). 확인창 문구도 이미 있음. Irene 「됐어」 → 변경 없음.

### 다음 확정 작업
- 없음 — 지시 대기

### 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- 🟣 **Fable 구조정리 대기 6건** — `docs/TRADE_STRUCTURE.md` 맨 끝 절. ①외부 공급업체 가격 PUT 이 브랜드 층 원가를 건드릴 수 있는 문 ②브랜드 메뉴가 레시피 2계통을 동시에 가짐 ③브랜드메뉴↔매장상품 1:1 보장 장치 없음 ④브랜드 다중 소유 불가 ⑤매장8 이월렛 결제 금액 미기록 ⑥브랜드가 넣어준 공급업체의 «가격관리 각자» 범위(2026-09-14 Irene 질문 — 재고·내 원가는 이미 각자, 공급업체 상품 정가만 등록 주체 소유)
- **③④ 확인 과제(설계 아님)**: 브랜드 레시피가 매장에 내려간 뒤 **매장 원가층 가격으로 계산되는지 실호출 증명**. 매장 화면에서 그 가격을 직접 넣는 흐름이 있는지는 **확인 불가(아직 안 봄)**.
- **공급업체 상품 «Active» 더블클릭 시 서버에 쓰기 2회**(PUT 200 ×2) — 클라이언트 중복 가드 없음(실측).
- **저장이 네트워크에서 실패해도 화면 안내 없음**(콘솔에만) — 공급업체 상품·계약·결제설정.
- **요금제 모듈 문은 개발서버에서 검증 불가** — 개발 매장·브랜드 전부 plan_type 이 비어 fail-open. 검증용 계정 필요.
- **K-DINE 신메뉴 66건 적용** — 2026-09-01 지시로 보류(`docs/KDINE_MENU_AND_RECIPE_PLAN.md`).
- **버튼 전수검사 미완 구간** — 모달 일부·드롭다운·모바일 손님 주문 화면·Floor Plan 8건.
- 기존 대기: 상품링크 «배송·픽업 안내»(`docs/BUYER_FREE_TIER_DESIGN.md` §6-4) · 판매자 자체 재고 «재고 단위» 칸(§5-12) · 준비된 재고(1차 가공) 설계.

### Git 상태 (저장 시점)
- 브랜치 `deploy-isolation`
- ⚠ 미커밋 상태에서 `git checkout`/`reset --hard` 금지(메모리 feedback_stash_before_deploy_isolation — 영구 소실 2회)

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```
