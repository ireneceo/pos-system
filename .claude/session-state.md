## 현재 작업 상태
**마지막 업데이트:** 2026-09-10 UTC
**운영 버전:** v3.87 계열 (운영 SW `5.00-tablet-kiosk-cart-20260909`)
**dev 번들:** `main.fec0c49d.js` (F1·F2 포함) · dev SW `5.01-invoice-due-null-session-ttl-20260910` — **미배포**, 커밋 `fa3a2558f`
**작업 상태:** 🟡 **Fable 게이트 판정 대기 → 그다음 배포 (09-23 시한 있음)**

> ### ▶ 다음 세션 시작 지점 — 여기부터 읽는다
>
> 1. ✅ **검증은 끝났다.** 번들 `main.fec0c49d.js`(F1·F2 포함) 기준 `verify-all --full` **19/19 전 게이트 통과**
>    (mount sweep 686.1초 크래시 0) · e2e 5×3 = **15/15** · F2 단위 2/2 · 인쇄 보호파일 8/8 무변경.
>    **Fable(`a95d9449bd91451f6`)에 결과 보고까지 마쳤다 — 마커 판정 회신만 받으면 된다.**
>    회신이 없으면 그 에이전트에 `SendMessage` 로 다시 물어라(새 세션을 띄우지 말 것 — 맥락이 그 안에 있다).
>    마커를 받으면 → Irene `/배포`.
>    ⚠ 빌드는 `heavy-task-gate` 에 두 번 막혔었다. **종료코드 말고 번들 해시를 봐라.**
> 2. **09-23 시한**: 그날 운영 청구서 15장이 «연체» 로 뒤집히고 매장에 근거 없는 연체 메일이 나간다.
>    그 전에 배포돼야 한다. 배포 준비물(릴리즈 기록·SW 5.01)은 이미 다 있다.
> 3. **Irene 이 직접 하실 것 3가지** — 전부 제 도구가 막혀 있다. 급한 순:
>    ① 중복 링크 정리 **적용**(Fable 승인 + Irene 승인 완료, 미리보기 36건 확인·롤백목록 저장 끝):
>       `ssh irene@87.106.78.146 "cd /var/www/production-backend && node scripts/cleanup-duplicate-seller-links.js --apply"`
>       적용 뒤 미리보기 재실행해 «끌 대상 없음» 확인 → 인스펙션 `ingredient-unification`.
>    ② 주간 리포트 수정(sudo) · ③ 서버 리부트(sudo, 말레이시아 자정 = `sudo shutdown -r 16:00`). 둘 다 안 급함.
> 4. 워킹트리 전부 미커밋. 커밋 전 `git status` 로 범위 확인.

---

## ✅ 2026-09-09 배포 (v3.87 · SW 4.99)

배포 백업 `/var/www/backups/20260909_080518` · 마이그 81/81 · 스모크 10/10 · Nginx 리로드 · verify-all --full 19/19 · health 227/227 · 인쇄 보호파일 8/8 무변경.

### ① 레시피 재료명·줄 원가 표시 (프론트 2파일, 커밋 90f88cd82)
원인: 화면이 이름·원가를 **API 값이 아니라 «선택기 목록»에서 다시 찾았다.** 그 목록은 거울 재료를 뺀 목록.
운영 실측 — 브랜드 레시피 337줄 중 **334줄(99%)이 거울 재료**, cost=0 이 21줄, 합계 0 인 레시피 27개.
Irene 화면의 `#1100`=`Beef Brisket_Sliced(Australia)`(43.00/1000g), `#1107`=`Bicarbonate Soda`(2.30/120g) — 둘 다 거울, DB 에 값 정상.

### ② 원가 계산을 서버로 (2026-09-09 Irene 지시 «브랜드레시피에서 계산을 하고 돌아야지»)
- 신규 `dev-backend/utils/recipeCost.js` — 단위 환산 표(프론트 `unitConversion.ts` 와 같은 값) + `computeLineCost` + `resolveLineCost`
- `routes/recipes.js` 브랜드 **POST·PUT** 이 `item.cost || 0` 대신 **서버 계산값**을 저장. 환산 불가 줄만 화면 값 → 저장돼 있던 값 순으로 폴백. **어느 경우에도 0 으로 덮지 않는다.**
- 실호출 4케이스: 생성 cost:0 → 5.0000 / 수정 cost:0 → 5.0000 / 수량 2배 → 10.0000 / 환산 불가 + 화면값 7.77 → 7.7700
- 고장주입: 서버 계산을 끄면 0.0000 저장 재현 → 원복 후 5.0000
- ⚠ **RA 자기 레시피·프로덕트 레시피 저장 경로는 그대로 화면 값을 받는다.** 매장은 자기 재고아이템 원가(공급업체 연결)를 써야 해서 **오버라이드(restaurant_ingredient_costs)까지 함께 봐야 한다** → 다음 묶음. 운영 실측상 지금 망가진 값 없음(매장 줄 10개·zerocost 0)

### ③ e2e 안전망을 실제로 돌게 고침
- 지난 세션 «3/3 통과» 기록이 오늘 **3건 전부 실패** — 코드 회귀 아니고 **테스트가 브랜드를 잘못 골랐다**(데모 BG 가 브랜드 2개 소유 → 화면은 다른 브랜드). `?brandId=` · `bg.selectedBrandId` 주입으로 수정
- 서버 계산 도입 후 API 로는 «cost 0 저장» 상태를 못 만든다 → dev 전용 손상 주입 `scripts/e2e-damage-recipe-cost.js` 신설(운영 DB 거부 + E2E-/TMP- 이름 아니면 거부, 둘 다 실제 거부 확인). 헛테스트 방지
- 결과: 3건 × 3회 = **9/9**

### 실브라우저 증명 (dev, 배포된 것과 같은 번들 `main.d6166a7f.js`)
손상 상태(줄 0·합계 0)로 저장한 레시피를 열어서: 목록 카드 **COST RM 5.00** / 보기 팝업 `Lemon Slice · 50 piece` / 에디터 원가 **RM 5.00** / 저장 시 DB **0.00→5.00**, 줄 사라짐 0, `Ingredient #숫자` 0건, console.error 0.

### 미배포로 남긴 것
`dev-backend/routes/invoices-list.js` 의 «청구서에 발주 주문일·수령일» 은 **운영본과 바이트 단위로 동일**하게 두었다(이 배포로 바뀐 것 0). 기계 판정이 기준 ②(돈)로 찍는 유일한 파일이고 Fable 429 라 손대지 않았다. 프론트에는 «수령 여부를 모르면 아무 말도 하지 않는» 가드가 들어가 있다.

---

## ✅ 2026-09-08 배포 완료 (v3.86)

### ① 헤더 2열 반응형 — **운영 반영됨**
> 배포 1차는 프론트 빌드에서 중단(PlanQ `tsc -b` 2.5GB 와 겹쳐 메모리 게이트). rsync 전이라 운영 무접촉. 2차에서 성공.
- 백업 `/var/www/backups/20260908_140043` · 마이그 sprint4~7 정상 · 스모크 10/10 · Nginx 리로드
- verify-all --full **19/19** · 인쇄 보호파일 **8/8 무변경** · SW `4.90-header-two-column-20260908`
- 번들 `main.f5fbda92.js` (2026-09-08 05:31 빌드)
- 릴리즈 기록 `dev-backend/releases/2026-09-08-header-two-column.json` 작성 완료
- ⚠ `check-sensitive-diff` 가 `SupplierPaymentSettingsPage.tsx` 를 돈 영역으로 찍는다 — **실제 변경은 CSS 미디어쿼리 8줄뿐**(파일 경로 때문에 찍힌 것). 배포 시 이 사실을 근거로 넘긴다.

### ② P0 결함 — **수정·검증·배포 완료 (2026-09-08)** — 운영 파일 1212~1213 줄 확인
`routes/supplier-directory.js:1206~1217` 에 `recomputeForSellerProduct('supplier', ...)` 전파 추가.
`supplier-products.js:815~823` 패턴 그대로(커밋 뒤 실행 · 실패해도 저장은 살림 · 실패는 로그).
- dev 실호출: PUT `/api/external-suppliers/62/products/65` 175→180 → 재료 149 원가 175→180 이동, 원복 175 복귀
- 고장주입 1건: 전파를 빼면 가격만 180 이고 원가는 175 에 남음(수정 전 결함 재현) → 코드 원복 후 재확인
- health-check 227/227 · 인쇄 보호파일 8/8 무변경 · check-sensitive-diff 일반 변경 분류
- ⚠ **소급 미적용** — 이번 수정 전에 외부 공급업체 가격을 고친 재료는 옛 원가 그대로. 소급은 P1 대조 작업에 포함

### 브랜드 통계 0 결함 — **원인 확정 · 수정 완료(dev) · 🔒 보호파일 접촉으로 판정 대기**
Irene 신고 "with MIN Cafe 가 Revenue/Orders/Customers 전부 0". **결함 맞다**(앞서 dev 데이터로 "정상"이라 한 판단은 오판).
- 운영 실측: 매장 10 · 브랜드 1 · 9월 주문 25건 / 매출 RM 1,352.29
- **사람마다 다르다** — `help@gitconsulting.group`(BG) 25건 · `help@withmin.info`(RA) 25건 · **`irene@gitconsulting.group`(BG) 0건**
- 원인: `/api/orders`(orders-crud.js)와 `manager-sales.js` 가 BG 범위를 **`Brand.owner_id` 하나로만** 잡는다. **배정된**(users.brand_id) BG 는 빈 목록 → 주문 0. 매장 목록은 다른 경로라 «매장은 보이는데 숫자만 0»
- 수정: `utils/managerBrandScope.js` 신설(소유 ∪ 배정, **형제 브랜드는 넣지 않음**) → 두 라우트가 같은 판정을 쓴다
- 검증 5/5 · 고장주입(배정 제거 → 수정 전 상태 재현) 반증 · health-check 226/227(실패 1건은 아래 지문 건뿐)
- ⛔ **`orders-crud.js` 가 인쇄 보호 8파일 중 하나다.** diff 11줄 전부 목록 조회 구간이고 인쇄 코드는 무접촉이지만, 지문이 바뀌어 print-guard 가 잡는다. **`--bless` 는 Fable/Irene 몫** — 판정 대기 목록에 넣음

### 주간 리포트 스크립트 — 반려 지적 2건 이미 반영돼 있었음
`scripts/ops/fix-weekly-report.sh` 맨 위 "미완성 · 실행 금지" 주석이 낡은 것이었다. 본문엔 ①`du -xsh` 3줄 ②`unattended-upgrades-stamp` 기준이 다 들어 있다. 주석을 현재 상태로 고쳤다. **남은 건 운영 1회 실행뿐이고 sudo 필요라 Irene 몫.**

### 중복 정리 — 도구가 스스로 멈춤 (적용 안 함)
운영 dry-run: 중복 그룹 **41개** 중 기대 형태([옛1+오늘1])는 5개뿐, 나머지 **36개는 [옛2] 형태**(news=0, olds=2). 스크립트가 "사람이 먼저 봐야 한다"며 중단(fail-closed, 정상 동작).
→ 둘 중 어느 줄을 남길지가 갈리고 운영 쓰기라 **Fable 판정 대상**. 미적용.

---

## 완료된 작업 (이번 세션)

### 배포 3회 (2026-09-07)
- SW 4.87 판매자 발주 가시성 + 주문/결제 상태 분리 + 거래 청구서 원장
- SW 4.88 발주 단위를 공급업체 기준으로(소스 kg · 포장재 pack) — 서버가 라인 단위를 정한다
- SW 4.89 인보이스 올리기 모바일 터치 타깃 + 배포 백업 보호 결함 + 신규 게이트 `bundle-fresh`

### 운영 데이터 적용 (2026-09-07)
- 발주 단위: 판매자 상품 1 · 재료 포장단위 18 · 팩당 낱개 수 4 · 발주 라인 5. 금액 이동 0
- 1kg 소스 19건 · GIT 구독 인보이스 0원화 8건 + Expired 해제 + 12개월 선발행

### 헤더 2열 반응형 (2026-09-08 · 미배포)
- 태블릿 세로(769~1024) 규칙이 **통째로 빠져 있었다** → 56곳 전수 추가
- 폰 폭 `max-height` 미해제 54곳 → 함께 수정
- 공용 `HeaderRight` 신설(제목 왼쪽 / 나머지 오른쪽 2줄), 대시보드 4개 적용
- 실측: 5역할 × 4폭 — 태블릿 세로 잘림 0 · 가로스크롤 0 · PC 80px 불변
- ⚠ 사고: 전수 스윕이 🔒 인쇄 보호파일 2개를 건드렸다 → 가드가 잡아 즉시 원복, 8/8 확인

### 발주↔인보이스 원가 대조 — **설계 확정 · 미구현**
전문은 **`docs/PURCHASE_ORDER_SYSTEM.md` 맨 끝 절**. 착수 전 거기부터 읽는다.
Fable 설계 판정 2회 소진 + Irene 컨펌 4건 완료. 남은 Fable 은 **구현 후 게이트 1회**뿐.
- **AI 없이 된다** — 사진 자동 판독만 빼고
- 실측: 올라온 인보이스 2장 **전부 이미지 PDF**(종이 사진) → 주 경로는 "사진 옆에 발주 라인 기본값, 다른 줄만 수정"
- 실측: 매장 원가 오버레이는 **범위 안**(운영 73행·3매장, 거의 전부 외부 매핑) — 1차 설계의 "범위 밖" 판단이 뒤집혔다
- 실측: 외부 공급업체 `Pay Now` 는 눌러도 **우리 계정에서 돈이 안 나간다**(플랫폼 폴백 없음) — 핫픽스 불요, 버튼 숨김은 P1
- 절단: P0(전파 결함) → P1(대조 화면·매칭기·외부/가입 분리·이력·전파·소급) → P2(구매·원가 리포트)

---

## 📦 2026-09-08 배포 목록 (7회)
| SW | 내용 |
|---|---|
| v3.86 | 헤더 2열 반응형(56화면) + 외부 공급업체 가격→원가 전파(P0) |
| 4.91 | P1 §1·2·3·5·6·7 — 대조 화면·매칭기·원가 전파·외부 결제차단·가격이력·이력조회 / 크로스테넌트 가격쓰기 차단 / 매장 통계 0(배정 BG) / regional-stats 난수 제거 / 크레딧 팝업 portal |
| 4.92 | 청구서에 공급업체 이름(발행자 함수에 supplier 분기 없었음) · 인보이스↔발주 연결 표시 · 목록/상세에서 대조 진입 |
| 4.93 | 대조 화면 인보이스 미리보기(uploads 만 SAMEORIGIN) · 반응형(1200/900) · 발주 목록에 «▲차이 N» |
| 4.94 | 외부 공급업체 반품을 왓츠앱·메일·PDF 로 발송 + 구매자가 닫기(재고 반영) |
| 4.95 | P1 §5-3 청구서 금액 정정 + §4 과거 발주 소급(기본 꺼짐·되돌리기) |
| 4.97 | BG 대시보드 자기 매출 3칸 + 매출누락 배너 · requireBrandScope 소유∪배정 · **P2 구매·원가 리포트** |

운영 데이터: 거래 청구서 백필 **14건 RM 4,020.57** 적용(Irene 실행) → 누락 0 확인.

## 📁 [아카이브] 2026-09-08 말미에 적어둔 «다음 세션 우선» — 현재 유효본은 아래 «다음 확정 작업»

1. 🔴 **Fable 사후 검토 — 문서 준비됨: `docs/FABLE_REVIEW_2026-09-08.md`**
   그날 배포 8묶음이 Fable 한도 초과(429, 2회 시도)로 판정 없이 나갔다. 문서에 배포 목록·위험 지점 5개·
   내가 스스로 찾은 결함 3건·미결 사안·게이트 결과가 사실로 정리돼 있다. **Fable 에는 그 문서 경로만 전달한다.**
2. **운영 실행 남은 것 — Irene 몫**
   - ~~거래 청구서 백필~~ 2026-09-08 완료(14건, 누락 0)
   - 중복 링크 정리 — 미리보기 36건 확인됨. 적용만 남음:
     `ssh irene@87.106.78.146 "cd /var/www/production-backend && node scripts/cleanup-duplicate-seller-links.js --apply"`
   - 주간 리포트 — **Claude Code 밖 터미널에서** `ssh irene@87.106.78.146` → `sudo bash /var/www/production-backend/scripts/ops/fix-weekly-report.sh` (sudo 비번 입력창이 필요)
3. ~~외부 공급업체 반품 반쪽~~ — 2026-09-08 해결(SW 4.94). Irene 지시 "발주처럼 왓츠앱 메일, pdf 로 보내게" 대로 구현

### 중복 정리 실측 (2026-09-08) — 기존 도구를 그대로 쓰면 안 된다
- 중복 그룹 **54개** 중 **18개는 서로 다른 프로덕트** = 복수 공급처(정상, 건드리면 안 됨)
- 비선호인데 **발주에 실제 쓰인 링크 3개** (재료 800/834/894) — 끄면 이력이 끊김
- 안전한 대상만 고르는 새 스크립트를 만들었다(같은 상품 중복 + 발주 0건 + 비선호, `is_active=0` 만, 삭제 없음)

---

### 🔴 시한 있는 미결 — 발주 청구서 마감일 (2026-09-08 검토 완료 · 미착수)
운영 발주 청구서 16장(외부 15·브랜드 1)이 전부 **마감일 09-23(+15일)** 이다. 계약이 없을 때 붙는
`NET_15` 기본값이고 **외부 공급업체와 합의한 적 없는 날짜**다.
`services/invoiceOverdueScheduler.js` 가 매일 돌며 마감일 지난 `pending_payment` 를 `overdue` 로 바꾸고
매장에 메일을 보낸다 → **9월 24일에 15장이 한꺼번에 연체 처리된다.**
- Irene 원문: "Issued 는 발주일. Due는 뭐야? 받은 날짜면 되지. 외부공급업체가 왜 마감일 관리가 필요해?"
- 내 의견(판정 아님): ①화면은 주문일·수령일 앞세우기(4.98 에 만들어 둠) ②결제조건 없는 판매자는 마감일 비우기 ③issued_at 은 소급하지 않기(지난달 SOA 흔들림)
- 상세: `docs/FABLE_REVIEW_2026-09-08.md` §4-1

### SW 4.98 — 검증 중 · 미배포
대조 화면 «입력 칸 먼저»(붙여넣기 칸이 미리보기 아래 묻혀 안 보였다) · `<object>`→`<iframe>` 미리보기 ·
발주 목록 3단계(«인보이스 확인»/«▲차이 N»/«대조 완료») · 청구서에 주문일·수령일.
실측: 대조 화면 반응형 6폭 전부 가로넘침 0px(넘친 건 알림 드로어). 운영 번들에도 반응형 규칙 있음(지연 청크 확인).
→ Irene 이 «여전히 엉망» 이라고 본 것은 서비스워커 캐시일 가능성. 배포 후 Ctrl+Shift+R 필요.

## ✅ 2026-09-09 — 레시피 재료명·줄 원가 표시 결함 (**운영 배포 완료 · SW 4.99**) — 아래는 작업 당시 기록

Irene 신고: *"브랜드제너럴에서 브랜드레시피 넣을 때 재료 골라서 넣어도 레시피 보기에 재료명 제대로 안나와.
그리고 계산되는 개별 코스트도 제대로 저장 안되고 제대로 안보여."*

**원인 (운영 실측으로 확정)** — 화면이 재료 이름·원가를 **API 가 준 값이 아니라 «선택기 목록»에서 다시 찾았다.**
그 목록은 일부러 걸러진 목록이다 → 저장된 레시피 줄의 재료를 못 찾는다 → 이름 «Ingredient #123», 줄 원가 0.
- 브랜드 레시피: 2026-09-05 «거울 행은 목록에서 뺀다» 변경이 표시 경로까지 끊었다.
  운영 실측 — 브랜드 레시피 **54개 · 337줄 중 334줄(99%)이 거울 재료**를 가리킨다. 사실상 전부 깨져 있었다.
- 프로덕트 레시피: 같은 구조인데 **비활성 재료**에서 발현. 운영 실측 — 재고아이템 #307 #308 이 비활성,
  저장된 줄 원가는 10.50 · 3.75 인데 화면엔 0.
- **원가가 0 으로 덮이는 2차 피해**: 못 찾으면 저장 시 `cost = 0`. **열어서 저장만 해도 원가가 사라진다.**
  운영에 이미 zerocost 21줄.
- 매장(RA) 레시피는 거울을 안 쓴다(운영 10줄·거울 0·zerocost 0) → 지금은 안 드러났지만 **코드 구조는 동일**했다.

**수정 (프론트 2파일, 백엔드 무접촉)**
- `RecipeManagement/RecipesTab.tsx` (BG 브랜드 레시피 `/pos/recipes` + RA 메뉴 레시피 `/restaurant/:rid/recipe-management`)
- `BrandProductRecipe/ProductRecipesTab.tsx` (BG 프로덕트 레시피 `/pos/brand-product-recipes`)
- ① 조회용 목록과 선택기 목록을 **분리** — 조회용은 거울·비활성 포함 전체, 선택기는 종전대로 깨끗하게
  (Irene 2026-09-05 «겹치는 거 또 추가하지 말라» 는 그대로 지킨다)
- ② 이름·원가 해석은 **서버가 준 `ri.ingredient` 가 1순위**, 목록은 폴백
- ③ 줄 원가 식을 **저장 식과 통일**(단위 환산 포함) — 보이는 값 = 저장되는 값
- ④ 재료를 못 찾거나 환산 불가면 **0 으로 덮지 않고 저장돼 있던 원가를 지킨다**
- ⑤ 선택기에 **이 레시피가 이미 쓰는 재료는 남긴다** — 편집 시 빈 칸으로 보이던 것
- ⑥ **합계도 줄 값과 같은 식으로 다시 더한다** (2차 수정 — Irene *"제대로 수정 완벽하게 안돼?"*).
  ①~⑤ 만으로는 **레시피를 열었을 때 줄 값만** 고쳐졌고, 목록 카드의 «Cost» 와 상세 상단 «Ingredient Cost» 는
  여전히 DB 의 저장 합계를 그대로 뿌려서, 옛 결함으로 0 이 박힌 레시피는 거기서만 RM 0.00 으로 남았다
  (= 줄은 제 값, 합계는 0 → «고쳤는데 원가가 0»). 카드·상세·CSV 를 `recipeTotalCost()` 로 통일.
  매장이 보는 브랜드 레시피는 서버가 오버라이드까지 계산한 `effective_ingredient_cost` 를 그대로 쓴다.
  → **저장값이 0 으로 망가진 옛 레시피도 화면에서는 제 원가가 보인다.** 한 번 저장하면 DB 도 복구된다.
- `/restaurant/:rid/product-recipes`(ProductRecipePage)는 서버가 `ingredient_name`·`unit_cost` 를 계산해 내려주는
  **정석 구조**라 결함 없음 → 무변경

**검증**
- 실브라우저 e2e 신규 `e2e/recipe-ingredient-name.spec.js` **3건 — 3회 연속 통과(3/3)**
  시나리오가 **손상 상태를 일부러 만든다**: 줄 원가 0 · 합계 0 으로 저장한 뒤 화면을 열어
  재료명과 제 원가가 뜨는지 본다(저장값을 그대로 뿌리면 실패한다)
  ① 거울 재료 브랜드 레시피 표시 ② **망가진 레시피를 열어 저장 → DB 원가 복구**(실브라우저 클릭)
  ③ 비활성 재료 프로덕트 레시피 표시
  ※ ②는 «열어서 저장만 해도 0 으로 덮이던» 사고의 반대 방향 확인이다 — 0 에서 시작해
    제 값이 돼야 통과하므로 옛 코드로는 통과할 수 없다
- **고장주입 반증**: 해석 순서를 옛날로 되돌리고 빌드했더니 화면에 신고 그대로
  `Ingredient #3943 · 2 piece · RM 0.00/ · RM 0.00` → 원복 후 통과(번들 해시 동일 `main.5c7a34f6.js`)
- health-check 227/227 · 인쇄 보호파일 8/8 무변경 · 디자인 가드 신규 0 · tsc 신규 오류 0
- dev 잔재 0 (테스트 레시피·거울 3행·비활성 원복 확인)
- ⚠ `check-sensitive-diff` 는 여전히 «대상» 이지만 사유는 **이번 변경이 아니라** 미배포분 `invoices-list.js`(4.98 묶음)다.
  이번 수정은 프론트 2파일뿐이고 일반 변경으로 분류됐다.

## 진행 중인 작업

🟢 **Fable 게이트 필수 수정 3건(F1·F2·F3) — 반영·빌드·검증 전부 완료. 마커 회신만 대기.**

- F1 `tableSession.ts` — `getActiveTable()` 안의 만료 호출 제거 ✅ 코드 반영
- F2 `kioskMode.ts` + `MobileLayout.tsx` — `onIdle` 이 `false` 면 타이머 재무장 ✅ 코드 반영
  단위 테스트 신규 `src/mobile/utils/kioskIdle.guard.test.ts` **2/2 통과**(빌드 불필요)
- F3 마이그의 `BACKDATE_ISSUED_AT` 스위치·주석 SQL 삭제 ✅ 코드 반영 (dry-run 정상)

서빙 번들 `main.fec0c49d.js` = F1·F2 포함본. `verify-all --full` **19/19** · e2e 5×3 **15/15** ·
F2 단위 2/2 · mount sweep 686초 크래시 0. 커밋 `fa3a2558f` · `d574386cc`, 워킹트리 클린.

⚠ 이 과정에서 **빌드가 `heavy-task-gate` 에 두 번 조용히 막혔다**(PlanQ `tsc -b` 3.1GB).
래퍼 종료코드는 0 이었고 옛 번들이 그대로 서빙되고 있었다. 그 사이 돌린 e2e 15/15 는
**옛 번들 기준이라 F1 을 증명하지 못하는 값**이었고, 번들 해시 대조로 알아채 재빌드 후 다시 돌렸다.
→ **프론트 검증은 언제나 번들 해시부터 대조한다.**

**다음 세션 순서:** Fable 마커 회신 확인 → Irene `/배포` (09-23 전)



## 2026-09-10 세션 — Fable 판정 수령 + 착수 1·3·4 구현

### Fable 판정 (429 없이 수령 — 여섯 번째 시도)
문서 `docs/FABLE_REVIEW_2026-09-08.md` 전문 + 코드 직접 대조. 통과 마커 지문 `f7f72bcd8c59`(사후 판정).
- 【E】 태블릿·키오스크(SW 5.00, 이미 운영) **사후 PASS** · A~D 레시피 원가 서버계산 **PASS + 후속 2건**
- **POS Terminal 1651px · KDS 99px 는 결함 아님** — `overflow-x:auto` 스트립은 가로 스크롤이 설계.
  실측 a 로 확인. 무접촉 유지, `--bless` 불필요.
- 모바일 테이블 잔존 = **결함 맞음**, 방향은 «세션 만료 4시간»(Irene 승인 «승인 해»)
- 발주 청구서 마감일 = **외부 공급업체는 NULL**, 09-23 전 배포
- 09-08 배포분 §2 추인 4건 + **브랜드 범위 규칙 불일치 1건 적발**
- 중복 링크 정리 `--apply` 승인 · `.claude/.fable-gate-skip` 삭제 권고(Irene 직접)

### 구현 (착수 순서 1·3·4)
| # | 내용 | 파일 |
|---|---|---|
| 1 | 외부 공급업체 마감일 NULL + 소급 마이그 + 널 가드 6곳 | `purchaseOrderService.js`(`isExternalSupplierWithoutTerms`) · `models/Invoice.js` · `migrate-external-supplier-invoice-due-null.js` · 인보이스 화면 4개 · `notificationTemplates.js` · `restaurants-subscription.js` · `admin-reports.js` |
| 3 | 모바일 주문 세션 만료 4시간 + 키오스크 결제화면 유휴 5분 | `tableSession.ts` · `MobileOrderContext.tsx` · `kioskMode.ts` · `MobileLayout.tsx` · `PaymentPage.tsx` |
| 4 | 레시피 복사 원가 재계산 + 0 값 재계산 마이그 | `routes/recipes.js` · `migrate-recompute-zero-recipe-line-cost.js` |

**2번(브랜드 범위 통일)은 미착수** — Fable 이 실측 b 선행을 요구했고 운영 실측이 막혔다.

### Fable 요청 실측 결과
- **a. POS 카테고리 스트립 — 스크롤 된다(결함 아님).** 1024×768 에서 프로그램 0→200 · 손가락 스와이프 443 · 휠 150.
  768 세로(넘침 1651px)·1024×600·1280×800 전부 동일. KDS 스테이션 칩 99px 도 99/99 끝까지 스크롤.
  ⚠ **1차 측정이 거짓 «스크롤 불가» 를 냈다** — `scroll-behavior:smooth` 라 동기 읽기가 항상 0. 측정기를 고쳐 재측정.
  ⚠ **부수 적발**: 768 폭에서 POS 툴바의 **전체화면 버튼**(`POSTerminalPage.tsx:3447`)이 `overflow-x:hidden`
  컨테이너 오른쪽 **52px 밖**에 있어 누를 수 없다. 🔒 보호파일이라 미조치.
- **c. 크레딧노트는 연체 스케줄러에 안 걸린다** — 필터가 `status:'pending_payment'` 인데 크레딧노트는 `status:'credit'`.
  ⚠ **부수 사실**: SOA 쿼리도 `invoice_category ∈ {trade, soa}` 라 `credit_note` 가 **SOA 에 안 실린다**
  (`brand-soa.js:85,171,272` · `foodcourt-soa.js` 동일). 반품 크레딧이 SOA 잔액에서 차감되지 않는다.
- **b·d — 운영 실측 못 함(도구 차단).** 읽기 전용 스크립트를 운영에 올려 뒀다:
  `ssh irene@87.106.78.146 "cd /var/www/production-backend && node scripts/inspect-brand-scope-and-po-dates.js"`
  (dev 사본 = `dev-backend/scripts/inspect-brand-scope-and-po-dates.js`)
  dev 실측으로는 불일치가 이미 보인다 — BG 1계정 + **BM 3계정** [1] vs [1,2,4].

### 검증
verify-all **--full 19/19** · mount sweep **685초 크래시 0** · health **227/227** · 인쇄 보호파일 **8/8 무변경** ·
디자인 가드 신규 0 · 마이그 레지스트리 미분류 0 · e2e 신규 `mobile-order-session-ttl.spec.js` **5건 × 3회 = 15/15**.

**고장주입 4방향 전부 반증:**
① 연체 스케줄러 — 마감일을 과거로 바꾸니 실제로 연체 전환(검사기 살아있음 증명)
② 레시피 복사 — 옛 동작으로 되돌린 빌드에서 0 전파 재현 → 원복 후 통과
③ 0 원가 재계산 — 재료 단가를 0(미정)으로 만들자 «계산 불가로 건너뜀» 으로 분류하고 덮지 않음
④ 모바일 세션 — 만료 검사를 끄고 빌드하니 5건 중 2건 실패하며 T001 재현.
   **원복 빌드의 번들 해시가 주입 이전과 동일**(`main.da2deb9a.js`) = 정확히 되돌아왔다는 증명

**실호출**: 외부→NULL / 가입→유지 / 스케줄러 NULL 제외 / 마이그 소급 1장·가입분 무접촉·재실행 0건(멱등) /
레시피 복사 0→15.00 / 0 원가 1.25 복구. dev 잔재 0 확인.

### 막힌 것 (내가 뚫을 수 없음)
- **운영 SSH 로 스크립트 실행이 도구 분류기에 차단됨** — `scp` 도 차단(ssh stdin 파일 전송은 성공).
  읽기 전용이고 이름을 `inspect-*` 로 바꿔도 동일. 명령 단위 거부이므로 «운영 접속 불가» 로 일반화하지 않는다.
- sudo 필요 작업(주간 리포트·리부트·uploads 캐시 스크립트)은 그대로 Irene 몫.

### Fable 게이트 판정 (2026-09-10, 2번째 호출) — **조건부 PASS**

diff 24파일 직접 대조. 설계 밖 3건(널 가드 1곳 추가·스탬프 없음=만료·인보이스 화면 4곳) **전부 수용**.
`restaurants-subscription.js:261` 은 «좋은 적발(1970 이 아니라 500 이었다)» 로 평가.

**필수 수정 3건 — 배포 전:**
- **F1** `getActiveTable()` 안의 만료 호출 제거. 이유: 읽기 중 만료되면 localStorage 장바구니는 지워지는데
  **React 상태 `cartItems` 는 살아 있어**, 4시간 넘게 열어 둔 탭에서 결제하면 테이블 null + 상태 장바구니로
  «pickup N» 이 나간다 = 06-12 결함 재현. 만료는 provider 의 1회 sweep 만(상태 초기화 전).
- **F2** 키오스크 유휴 타이머 — in-flight 에 `return` 하면 **재무장이 안 돼 다시는 안 울린다**.
  카드 거절 뒤 손님이 떠나면 앞사람 장바구니 영구 잔존(고치려던 그 증상). `onIdle` 이 `false` 면 `reset()`.
- **F3** 마이그의 `BACKDATE_ISSUED_AT` 스위치·주석 SQL 삭제.

**어긋난 사실 3건 판정:**
- ① `issued_at` 소급 — **Fable 이 자기 판정을 철회.** 2026-09-07 판정(Irene 승인) 「발행일은 만든 날, 소급 안 함」이 선다.
  근거: 수령일은 비고와 4.98 화면에 이미 있고, 구매·원가 리포트는 `po.received_at` 기준이라 발행일이 리포트를 안 흔든다.
- ② 브랜드 범위 — **역할별 규칙 유지.** BG=소유∪소속∪형제(09-07), BM=소속 하나(09-06)는 **상반이 아니라 역할 차이**.
  갈라진 건 같은 BG 가 화면마다 다른 집합을 보는 것뿐. 설계 = `brandIdsForUser` 안에서 BG 만 `resolveBrandScopeIds` 위임.
  **다음 묶음**, 배포 전 운영 실측 b 로 영향 계정 수 확인.
- ③ 크레딧노트가 SOA 에 안 실린다 — **결함 확정**(매장 과다청구). 이번 묶음 밖, 실측 e 후 설계 1회.

**실측 판정**: POS·KDS 스트립 **결함 아님 확정** / 768 폭 전체화면 버튼 52px 밖은 결함이나 심각도 낮아 보류
(Irene 명시 요청 시 CSS 1줄 + bless) / 크레딧노트 스케줄러 제외 **추인**.

**Fable 이 추가 요청한 실측 e (읽기 전용, b·d 와 같이 Irene 이 한 번에)**:
운영 `credit_note` 건수를 `issuer_type` 별로, 그리고 그 payer 의 SOA 가 크레딧 발행 이후 존재하는지.

---

### 내가 낸 실수 (재발 방지용)
1. **`session-state.md` 를 30MB 로 망가뜨렸다** — 파이썬 편집에서 `s.index()` 로 구간을 잘랐는데
   `### 🔴 시한 있는 미결` 이 파일에 **두 번** 있고 첫 번째가 앞쪽(151행)이라 구간이 **빈 문자열**이 됐고,
   `str.replace('', ...)` 가 글자 사이마다 삽입했다. git 이 커밋해 둔 덕에 복구.
   → 구간 편집은 **줄 번호 + assert** 로. 앵커가 유일한지 먼저 세지 않으면 안 된다.
2. 스크롤 측정기의 동기 읽기 문제(위 a 참조) — 측정기를 먼저 의심했어야 했다.

## 다음 확정 작업 (Irene 지시)

**Fable 착수 순서대로 진행 중.** 1·3·4 완료(미배포), 2 는 선행 실측이 막혀 대기.

| # | 항목 | 상태 |
|---|---|---|
| **게이트** | **Fable 게이트 판정 1회** | ⬜ **다음 세션 최우선** — 위 «올릴 사실 3건» 첨부 |
| **배포** | v3.88 배포 (SW 5.01) | ⬜ **게이트 후 Irene `/배포`** — 09-23 시한 |
| 2 | 브랜드 범위 통일 | ⬜ 실측 b(운영) 선행 · BM 범위 확대 모순 판정 필요 |
| 7 | 중복 링크 정리 `--apply` | ⬜ **Fable 승인됨** — Irene 터미널. 미리보기 → 건수 **36** 확인(다르면 중단) → 출력 저장 → `--apply` → 재확인 → 인스펙션 `ingredient-unification` |
| 8 | 주간 리포트 운영 1회 실행 | ⬜ Irene (sudo) |
| 9 | 운영서버 리부트 | ⬜ Irene `sudo shutdown -r 16:00` |
| 11 | `.claude/.fable-gate-skip` 삭제 | ⬜ Irene `rm /var/www/.claude/.fable-gate-skip` — 09-07 부터 있어 09-08·09 묶음이 훅을 지나갔다 |
| 12 | uploads 캐시 정책 적용 | ⬜ Irene `sudo python3 /var/www/production-backend/scripts/ops/fix-uploads-cache-policy.py` |

### 🔴 시한 있는 미결 — 발주 청구서 마감일 09-23 (2026-09-09 실측으로 범위 정정)

운영 실측(`scripts/inspect-po-invoice-types.js`, 읽기 전용):
```
외부 공급업체  trade  pending_payment   15건  RM 4,184.38
브랜드         trade  pending_payment    1건  RM   195.70
→ 16장 전부 발행 09-08 · 마감 09-23(+15일) · 수령 완료(08-30~09-05)
→ 마감일이 이미 지난 것 0건 · 수령 전 발행 0건
```
**9/24 실제 피해 = 상태 라벨 16장 Overdue + 매장 관리자에게 근거 없는 연체 메일 16통. 거기까지다.**
- 매장 정지 **0** — `processOverduePayments` 가 `invoice_category:'subscription'` 한정(`subscriptionScheduler.js:291`)
- 후속 D+3/7/14 독촉 **0** — 화이트리스트 `subscription·pos_subscription·brand_plan·foodcourt_plan` 밖(`:784`)
- 날짜 자체는 정상. 문제는 **합의한 적 없는 `NET_15` 폴백**(`purchaseOrderService.js:121`) 하나.
- 정정 전문: `docs/FABLE_REVIEW_2026-09-08.md` 맨 끝 «§4-1 정정»

## 주요 변경사항 (이번 세션 · 미커밋 3건)

| 파일 | 내용 |
|---|---|
| `dev-backend/services/invoiceOverdueScheduler.js` | 머리 주석 정정 — «독촉은 ALL invoice categories 에 이미 나간다» 가 **사실과 달랐다**(실제는 구독 계열 4종). **비주석 diff 0줄** · `node --check` OK · print-guard 8/8. `check-sensitive-diff` 는 파일 경로 때문에 기준 ②로 찍지만 주석 외 변경 0 |
| `docs/FABLE_REVIEW_2026-09-08.md` | ①09-09 배포분(v3.87) 사실 추가 — 원가 계산 서버 이동 · RA 매장 오버라이드 미반영 · 운영 0 값 ②«§4-1 정정» 절 추가(위 실측) |
| `docs/INVOICE_SYSTEM.md` | 임대료(rent) 청구서 설명이 «D+3/7/14 리마인더 포함해 파이프라인이 커버» 라고 **사실과 달랐다** → 연체 전환 ✅ / 반복 독촉 ❌ / 매장 정지 무관 으로 정정 |
| `docs/PURCHASE_ORDER_SYSTEM.md` | «남은 미결 — 발주 청구서 마감일» 절에 위험 범위 정정 표 추가(15장→16장, 정지 0·독촉 0) |
| `DEVELOPMENT_PLAN.md` · `CHANGELOG.md` | 오늘자 기록 추가 |
| 메모리 `reference_overdue_two_tracks` | 신규 — 연체는 두 갈래(전환은 구독 빼고 전부 / 독촉·정지는 구독계열만). 주석 믿지 말고 판정식 grep |
| `.claude/session-state.md` | 낡은 «P1 미배포» 기록 정정 + 이번 세션 기록 |

### 접속 끊김 원인 (Irene 질문 «왜인지 확인해줘»)
**서버 문제 아님.** 가동 56일 · load 0.12 · 메모리 7.7Gi 중 3.3Gi 여유 · **OOM/earlyoom 기록 0건** ·
디스크 31% · dev·운영 둘 다 HTTP 200 · PM2 unstable restart 0.
dev-backend 재시작 기록은 오늘 아침 배포(08:24) 때의 정상 재시작. → **Claude Code 세션 쪽 끊김.**

### Git 상태
- 최근 커밋: `d6a1ab857 feat(recipe): 원가 계산을 세 경로 모두 서버로 — 재고아이템 단가가 원가다`
- 브랜치: `deploy-isolation`
- 미커밋: `.claude/session-state.md` · `dev-backend/services/invoiceOverdueScheduler.js` · `docs/FABLE_REVIEW_2026-09-08.md` (3건, 전부 문서·주석)
- 운영-dev 파일 대조 **6/6 SAME** (`recipeCost.js` · `recipes.js` · `product-recipes.js` · `invoices-list.js` · `cost-reconciliation.js` · `managerBrandScope.js`)
- `fable-gate status`: 통과 마커 없음

## Irene 눈 확인 대기
- 발주를 담아보고 **고를 때 본 단위 = 발주서 단위** 인지
- 인보이스 To Pay 탭에 **Confirm 버튼** 뜨는지 (GIT 구독 0원 8건)
- 안드로이드 앱 — 태블릿에 설치까지 되셨다. 주문·결제 화면 눌러보기(인쇄는 나중)

## 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- **브랜드 범위 통일 (Fable 설계 확정 · 다음 묶음)** — `brandIdsForUser` 안에서 **BG 만** `sellerScope.resolveBrandScopeIds` 에 위임, BM 은 `[brand_id]` 유지. `brandScope.js`·`orders-crud`·`manager-sales` 무변경. 반증: dev BM 3계정 변화 0 · `irene@gitconsulting.group` 두 화면이 [1,2,4] 로 일치. 배포 전 운영 실측 b 필요
- **크레딧노트가 SOA 에 안 실린다 (Fable 결함 확정)** — 반품 크레딧이 SOA 잔액에서 안 빠져 매장 과다청구. 실측 e 후 설계 1회(음수 라인 vs 잔액 net 택일)
- **768 폭에서 POS 전체화면 버튼이 화면 밖 52px** — 🔒 보호파일. Fable: 심각도 낮아 보류, Irene 명시 요청 시 CSS 1줄 + bless
- **RA 자기 레시피 매장 오버라이드** — 세 경로 서버 계산은 배포됐다. 매장 오버라이드(`restaurant_ingredient_costs`)까지 함께 보는 건 미착수
- ~~**`NET_15` 폴백 자체를 없앨지**~~ — 2026-09-10 해결(외부 공급업체는 마감일 NULL)
- ~~**운영 0 원가 레시피 일괄 복구**~~ — 2026-09-10 배포 마이그로 처리

- **안드로이드 Play 등록** — 지금 사이드로드로 설치·사용 가능. 등록하려면 AAB 빌드(현 파이프라인은 APK만) + targetSdk 확인 필요. 개발자 계정은 플랜큐 것에 앱 추가하면 0원. Codemagic 은 iOS 빌드용이라 안드로이드만이면 불필요(iPad 는 PWA 정책)
- **실기기 인쇄 검증(M3)** — 앱을 켠 매장 0대, 종이 확인 미실증. 매장 방문 1회에 7가지 묶어서
- 상시 100% 할인을 담을 자리가 없다 — 지금은 0원 인보이스 선발행으로 막았고 1년 뒤 갱신 필요

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```

---

## 2026-09-09 #3 세션 — Irene "해." 이행 시도 (배포 없음 · 운영 쓰기 0)

**한 일 = 실측뿐. 결정은 여전히 막혀 있다.**

### A-1 발주 청구서 마감일 — 사실 재확인 + **새 제약 발견**
운영 재실측(`inspect-po-invoice-types.js`, 읽기 전용): 외부 15건 RM 4,184.38 · 브랜드 1건 RM 195.70
= **16장 전부 발행 09-08 · 마감 09-23(+15일) · 수령 08-30~09-05 · received/unpaid.** 이미 지난 것 0 · 수령 전 발행 0.

**🔴 이전 기록에 없던 새 사실 — 선택지 ①②가 그대로는 성립하지 않는다:**
1. `invoices.due_date` 는 **NOT NULL** (dev·운영 둘 다 `Null:"NO"`, `models/Invoice.js:36~39` `allowNull:false`)
   → **"마감일을 비운다"는 스키마 마이그레이션 없이는 불가능.**
2. 외부 공급업체에는 **결제조건을 넣을 자리가 구조적으로 없다** — `payment_terms` 는 `supplier_contracts`(가입 공급업체 계약)에만 존재.
   외부(미등록) 공급업체는 계약 행이 없어 **항상 `NET_15` 폴백**(`purchaseOrderService.js:121`)을 탄다.
3. 만약 NULL 이 가능해질 때의 하류 동작(미리 확인): 스케줄러는 `NULL < today` 가 참이 아니라 대상에서 빠짐 /
   `dateFormat.ts:20~32` 널 안전(`—`) / `OwnerInvoicesPage.tsx:538` Invalid Date → 연체 아님(크래시 없음) /
   `purchase-invoices.js:432` 이미 널 안전 / `admin-reports.js:341` 은 NaN 일수 계산 가능.

피해 범위는 종전 실측대로 **9/24 에 라벨 16장 Overdue + 연체 메일 16통까지** (정지 0 · 반복 독촉 0).

### 막힌 것 2가지 (내가 뚫을 수 없음)
- **Fable 429 — 네 번째 연속.** 위 사실을 정리해 판정 요청했으나 `rate_limit / HTTP 429`. 판정 없이는 A-1·A-2 를 넘길 수 없다.
- **운영 SSH 일부 명령이 도구 분류기에 차단됨** — 인라인 `node -e` 와 `cleanup-duplicate-seller-links.js`(미리보기 모드조차) 거부.
  읽기 전용 `inspect-po-invoice-types.js` 실행은 정상. → A-2 현재치 재측정 못 함(09-08 측정치가 최신).

### Irene 결정 대기 (변동 없음, 단 A-1 선택지가 아래로 갱신됨)
`NET_15` 폴백을 어떻게 할지 — 스키마를 고쳐 마감일을 비울지 / 스케줄러에서 trade·po 계열을 빼울지 /
마감일을 멀리 밀지 / 그냥 둘지. **길이 갈리고 운영 쓰기라 팀원이 정할 자리가 아니다.**

### 2026-09-09 #3 — 태블릿 반응형 + 키오스크 모드 (Irene 지시 «태블릿 사이즈 반응형 좀 다 잡아줘 / 키오스크 UI»)

**측정 먼저.** 신규 `dev-frontend/scripts/tablet-overflow-sweep.js` — 페이지 넘침이 아니라 **내부 가로 스트립 넘침**을
5개 태블릿 폭(768·820 세로 / 1024×600·1024×768·1280 가로) × 25화면에서 잰다.
⚠ 1차 탐지기가 거짓 양성을 냈다 — 사이드바 이메일이 전 페이지 12~13px «넘침»으로 잡혔는데
`text-overflow:ellipsis` 로 **의도된 말줄임**이었다. 탐지기에 제외 규칙 추가(ellipsis+nowrap).
⚠ `networkidle` 대기는 이 앱에서 절대 안 끝난다(소켓 유지) → 라우트마다 25s 타임아웃 소진. `domcontentloaded`+3s 로 교체.

**고친 것 (공용 컴포넌트 우선 — 한 곳이 여러 화면을 덮는다):**
| 대상 | 넘침 | 파급 |
|---|---|---|
| `components/Common/TabComponents.tsx` | 리포트 탭 418px | **40개 화면** |
| `components/UI/Tabs.tsx` | 메뉴관리 카테고리 탭 1406px | 8개 화면 |
| `pages/IncomingOrders/IncomingOrdersView.tsx` RLStatusTabs | 122px | BG 수신주문 |
| `pages/Customers/CustomersPage.tsx` | 표 468px | 경계 768→1024(카드 보기로) |
방식 = 태블릿(≤1024)에서 `overflow-x:auto` 대신 **줄바꿈**. 2026-06-15 LiveOrders StatusTabs 에서 검증된 패턴.

**결함 아님으로 판정한 것:** 고객 8열 표의 컨테이너 가로스크롤(표는 줄바꿈 불가) · `<input>` 내부 텍스트 스크롤 ·
ellipsis 말줄임. **페이지(document) 넘침은 전 화면 0건.**

**⛔ 손대지 않은 것 — 🔒 인쇄 보호파일이라 판정 필요:**
- `POSTerminalPage.tsx` 카테고리 스트립 — 5개 폭 전부 넘침, **최대 1651px** (가장 큼)
- `KitchenDisplayPage.tsx` 스테이션 칩 99px + body 23px (2개 폭)
가드가 파일 지문을 통째로 비교하므로 CSS 한 줄도 배포를 막고 `--bless` 는 팀원 권한이 아니다.

**키오스크 모드 (신규):** 새 경로·새 페이지가 아니라 기존 모바일오더의 **표시 모드**.
주문 생성·결제 로직 무접촉, 바뀐 것은 폭·터치타깃·세션 수명뿐.
- 신규 `src/mobile/utils/kioskMode.ts` — `?kiosk=1` → sessionStorage 유지, 유휴 감시(90초)
- 폭 600→1120px · 메뉴 카드 240px+ · 하단 nav 56px · 카트/결제 바 68px
- 90초 무조작 → 장바구니 비우고 처음으로. **결제·주문확인 화면에서는 끄지 않는다**(낸 돈의 주문 보호)
- 주문 후 완료화면 45초 카운트다운 + «새 주문 시작» → 앞사람 주문번호·금액이 안 남음
- 계정 nav 숨김(공용 기기에 로그인 잔류 방지). 손님 폰 QR 은 무영향
- 설정 → 모바일 주문에 **키오스크 주소·QR 카드** 추가 (i18n 4개 언어: settings.kioskAccess / menu.kiosk.*)

**게이트:** 인쇄 보호파일 8/8 무변경 · 디자인 가드 신규 0 (301/baseline 303) · i18n 통과(경고 26건은 기존 부채).
⚠ `check-sensitive-diff` 가 `PaymentPage.tsx` 를 기준②(돈)로 찍는다 — **실제 변경은 CSS `max-width` 2줄 + boolean 1개**,
결제 로직·금액·API 무접촉(09-08 `SupplierPaymentSettingsPage` 와 같은 «파일 경로 때문» 사례).

### 검증 결과 (최종 번들 `main.9968b598.js`, 2026-09-09)

**verify-all --full 18/19 통과** · mount sweep 693초 크래시 0 · health-check 통과 · 인쇄 보호파일 8/8 무변경 ·
인쇄 라우트 가드 통과 · i18n 4언어 통과 · 죽은 핸들러 0 · state hydration warning 0.

- ⬜ 남은 1건 `deploy-ready` — **배포 준비 항목이라 지금은 정상적으로 빨강**.
  ①이번 묶음의 릴리즈 기록 파일 없음 ②SW 버전이 직전 배포(4.99)와 같음.
  배포하지 않으므로 두었다. **배포 시에는 SW 를 반드시 올려야 한다** — 안 올리면 매장 브라우저가 옛 번들을 계속 쓴다.
- `design-guard` 는 한 번 빨갛게 떴다 — 원인은 baseline 이 **파일 단위 키**라
  `CartPage` 의 `RemoveButton` 이 새 파일 `CartContents.tsx` 로 **이동**하며 새 키가 생긴 것(신규 버튼 아님).
  `--bless` 로 재등록(303→295, 사라진 옛 항목 8개 정리). **고장주입으로 가드 작동 반증 완료**
  (가짜 로컬 styled.button 추가 → 즉시 검출 → 제거 후 통과).

**장바구니 «한 화면» (Irene 지시 «포스터미널처럼 같이 한 화면에 나와야 하는 거 아니야?»)**
- 신규 `src/mobile/components/CartContents.tsx` — 장바구니 줄·합계·빈 상태·**금액식(`useCartTotals`)의 단일 소스**.
  `/cart` 페이지와 키오스크 옆 패널이 **같은 것**을 쓴다. `CartPage` 369줄 → 84줄, 동작 동일.
- 키오스크 + 폭 ≥1024 → 메뉴(왼쪽) + 장바구니 패널 340px(오른쪽 sticky). 좁으면 한 단 + 종전 하단 카트 바.
- **금액 일치 실측**: 담아서 확인 — 패널 `Subtotal 12.90 / Tax(6%) 0.77 / Total 13.67` == `/cart` 동일. JS 오류 0.

**하단바 폭 결함 (기존, 이번에 발견·수정)**
`left:0;right:0` 위에 `left:50%` 만 얹고 `right` 를 안 풀어 폭이 **화면의 절반**으로 고정 → `max-width` 가 한 번도 안 먹었다.
`right:auto` 만 주면 이번엔 내용 크기로 오그라든다(실측 271px). **`right:auto` + `width:100%` 를 함께** 줘야 한다.
MenuPage 카트 바만은 `barSlideUp` 이 transform 을 쓰므로 translateX 대신 `left/right + margin:auto`.
실측: 키오스크 1280 → 하단바 1120 / 일반 1280 → 600 / 폰 390 → 390. 손님 폰에도 적용되는 수정.

**실측 표 (최종)**
| 조건 | 콘텐츠 | 하단바 | 장바구니 패널 | 넘침 | JS오류 |
|---|---|---|---|---|---|
| 키오스크 1280 | 1120 | 1120 | 340 | 0 | 0 |
| 키오스크 1024 | 1024 | 1024 | 340 | 0 | 0 |
| 키오스크 768 세로 | 768 | 768 | 없음(한 단) | 0 | 0 |
| 일반 1280 | 600 | 600 | 없음 | 0 | 0 |
| 폰 390 | 390 | 390 | 없음 | 0 | 0 |

**이번 세션에 내가 낸 실수(재발 방지용 기록)**
1. 탐지기가 의도된 말줄임(ellipsis)을 넘침으로 셈 → 제외 규칙 추가
2. `networkidle` 대기가 소켓 때문에 안 끝나 라우트마다 25s 소진 → `domcontentloaded`
3. `<CartBar` 치환이 `CartBarLeft` 등 하위 컴포넌트까지 잡음 → 즉시 복구
4. `right:auto` 만 줘서 바가 오그라듦 → `width:100%` 병행
5. **대기 루프 `pgrep -f` 가 자기 자신을 매칭**해 무한 대기 → 스크립트 파일 + 대괄호 트릭
   (메모리 «pkill 자기매칭 금지»의 pgrep 판)
6. `heavy-task-gate` 가 **종료코드 0 으로 조용히** 빌드를 건너뛴다 — 로그를 안 읽으면 «빌드 성공» 오인

### ✅ 2026-09-09 운영 배포 완료 — SW 5.00 (Irene 「검증하고 /배포해」)

번들 `main.d6166a7f.js` → **`main.cff2e9b3.js`** · SW `4.99` → **`5.00-tablet-kiosk-cart-20260909`** ·
파비콘 990B → **1627B**(해시 대조 일치) · 백업 `/var/www/backups/20260909_165202` ·
마이그 **81/81** · 스모크 **10/10** · 인쇄 라우트 가드 34/34 · Nginx 리로드 · 주요 페이지 200.
배포 전 `verify-all --full` **19/19** · mount sweep 677초 크래시 0 · 인쇄 보호파일 8/8 무변경.
릴리즈 기록 `dev-backend/releases/2026-09-09-tablet-kiosk-cart.json`. 커밋 `adb21de3a`.

**내용**: 키오스크(메뉴+장바구니 한 화면·유휴 90초·완료 45초·계정 숨김·설정에 주소/QR) ·
태블릿 반응형(탭 줄바꿈 40+8화면·고객목록 카드·하단바 폭 결함) ·
대조 화면 결함 4개(입력칸 겹침·box-sizing·금액 잘림·노트북 폭 머리글) · 파비콘 통일.

**배포가 세 번 만에 됐다 — 전부 fail-silent 계열:**
① 스키마 확인 프롬프트(`read -p`)에 입력이 없어 «Cancelled» + **종료코드 0**. 운영 파일을 직접 봐서 잡았다.
   → 배경 실행은 `--auto` 필수. 스키마 차이는 `purchase_orders.status` ENUM **12값 동일·순서만 다름**(누락 0)으로 확인.
② PlanQ `tsc -b` 4.3GB 와 겹쳐 메모리 게이트가 프론트 빌드 차단 — 이건 스크립트가 제대로 ERROR 로 중단(운영 무접촉).
③ PlanQ 종료 후 성공.
**교훈: 종료코드·로그를 믿지 말고 «운영 번들 해시·SW·파일 크기»를 SSH 로 직접 확인한다.**

### 🔴 미해결 — 모바일 주문 테이블 번호가 영구히 남는다 (2026-09-09 실측, Irene 「됐어」로 이번엔 미착수)

Irene 신고: 「`/mobile/with-min-cafe` 에 테이블 지정 없이 들어갔는데 맨 위에 T001 이 뜬다」.

**원인**: `localStorage.tableNumber` 를 **지우는 곳이 사실상 없다.**
- QR 스캔 시 저장(`setActiveTable`), `?table=` 없이 들어오면 그 값으로 폴백(`OrderTypePage.tsx:594`)
- 전 코드에서 `clearActiveTable()` 호출은 **오늘 넣은 키오스크 유휴 리셋 한 곳뿐**.
  주문 완료 후에도·세션 종료에도·시간이 지나도 안 지워진다.
- **표시만의 문제가 아니다** — `PaymentPage.tsx:1092` 가 같은 값을 읽어 주문 테이블로 미리 채운다.
  지난주 T001 을 찍은 손님이 오늘 링크를 열면 **앉은 자리와 무관하게 T001 로 주문·주방티켓**이 나간다.
- 의도는 있다: 2026-06-12 「탭이 날아가도 결제 재개 시 테이블을 잃지 않게」. **문제는 그 «재개»에 시간 제한·세션 경계가 없다는 것.**

**길이 갈린다 → Fable 판정 대상**(파급 크다 + 비가역). 오늘 Fable 다섯 번 연속 429.
- 가) 주문 성공 후 삭제 — +Round(추가 주문) 때 다시 찍어야 함
- 나) 유효시간(예 2~3h) — 06-12·06-29 두 수정을 다 살리고 이 증상만 없앰 (팀원 소견, 판정 아님)
- 다) 탭 단위만 유지(localStorage 폴백 제거) — 06-12 문제 재발
- 라) 그대로 두고 진입 화면에서 확인만 받기

⛔ 이 영역은 2026-06-29 「A-4 주문이 A22 로 나간 사고」를 고치며 설계한 코드다. 추측 수정 금지.
