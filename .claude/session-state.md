## 현재 작업 상태
**마지막 업데이트:** 2026-08-25
**버전:** v3.78 (배포 1회 — 발주 수량 step. 버전 상승 여부 Irene 확인 대기)
**작업 상태:** 완료

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션 · 2026-08-25)

**운영 배포 1건** — 발주 수량 화살표가 0.01 씩 오르던 것(팩·개는 1씩, 무게·부피만 소수).
카트·수령·반품 3곳, 공용 `qtyStepForUnit()` 로 통일. 백업 `20260825_193001`,
`verify-all --full` 16/16(mount sweep 679초), 안전게이트 9/9, 마이그 56/56, 스모크 10/10.

**⚠️ 어제(8/24) 내가 만든 결함 수리 (미배포)** — 재고관리에서 **주문 기능이 사라져 있었다.**
공급처 연결 여부를 '재고부족 발주제안'으로 판정한 것이 원인. 최소치를 안 정한 품목은 제안에 안 나와
**운영 BG 285건·매장 77건이 연결돼 있는데 "No supplier linked" 로 표시**됐다.
실제 연결(`has_seller_source` / `sellers`)로 판정하도록 수리. 일괄발주 체크박스는 제안 기반 유지(분리).
고장주입으로 증상 재현(11건 전부 미연결) → 원복 후 통과. 단일 진실 [[reference_link_vs_lowstock_confusion]].

**화면 개선 (미배포)** — 재고관리: 빈 회색 자리표시 제거, 이름칸 확대, 품목명 줄바꿈.
발주 카트: 이름 전체 표시, 현재 재고 병기, 우측 패널 드래그 리사이즈(320~760px 기억).
**포장 표기 노출**: 공급업체 상품명의 `(50SETS X 5PKTS/CTN)` 을 목록·카트에 그대로(해석 안 함).

**운영 데이터** — GIT Consulting 재고 실사 21건 대조 후 어긋난 8건만 수정(313→306) ·
옛 표기 중복 상품 21건 삭제로 한글 병기 통일(상품 109→90, 카테고리 19건 이관) ·
신규 2건 등록(투명컵 RM14.70 / 뚜껑 RM5.25 = 원가+40%) + 재고 18팩씩(→342) + UGS 연결 ·
TK 1000 매입단가 RM16.00/팩(1 CTN RM80 ÷ 5 PKT).

**⚠️ 내가 만들고 고친 것 2건**
1. 채번이 건수 기반이라 상품 21건 삭제 후 등록 시 **이미 쓰인 SKU 재발급**(PRD-094/095) → 114/115 로 정정.
   [[reference_count_based_code_numbering]]
2. **상품 삭제가 발주 연결(`ingredient_seller_products`)을 안 치운다** → 실매장 2곳 4건이 없어진 상품을
   가리킨 채 남음 → 남은 한글 상품으로 재연결(계약가 보존). **코드는 미수정 — 후속 후보.**

**검증** — 실브라우저 회귀 2종 신규(`po-qty-step` / `inventory-seller-link`) 3회 연속 통과 ·
고장주입 2건(양쪽 다 증상 재현 후 원복) · 헬퍼 단위테스트 21/21 + 10/10 ·
print-guard 8/8 무접촉 · design-guard·route-guard 신규 0 · health-check 171/171 · 민감도 판정 비대상.

### 다음 확정 작업

**발주서에 공급업체 상품명·SKU 표시 (Irene 지시 2026-08-25, 다음 세션)**
> Irene 원문: **"발주할 때 발주관리에서는 한글이름 들어간 우리 재고표시 말고 공급업체 상품 이름으로
> 표시해야 하는 거 아니야?"** → 이어서 **"기존에 발주한 발주리스트에서도 바꿔줘."**
> **"일단 오늘은 그만 자게 저장하고 /개발완료 하자. 다음 섹션에서 할게."**

**실측 결과 (이번 세션에 조사 완료 — 다시 조사할 필요 없음):**

| 화면 | 지금 나오는 이름 | 공급업체가 보나 |
|---|---|---|
| 발주 상세 `PurchaseOrderDetailPage.tsx:1114-1118` | 내부명 굵게 + 아래 회색 공급업체명·SKU | 아니오 |
| 발주 카트·품목 목록 `NewPurchaseOrderPage.tsx` | 내부명 (+포장 표기) | 아니오 |
| staging `PurchaseOrderStagingPage.tsx:374` | 내부명 | 아니오 |
| **인쇄본 `PurchaseOrderPrintPage.tsx:284`** | `ingredient?.name \|\| description` = **내부명만** | **예** |
| **WhatsApp/이메일 `utils/poShare.ts:33`** | `product_name \|\| ingredient_name` = **내부명만** | **예** |

**핵심 결함**: 두 이름이 같이 나오는 건 내부 화면뿐이고, **공급업체에게 실제로 나가는 문서
(인쇄본·WhatsApp·이메일)에는 공급업체 상품명도 SKU도 없다.** 받는 쪽이 자기 상품을 대조할 수 없다.

**할 일 (Irene 이 1번 권고를 받은 상태에서 "기존 발주리스트에도" 를 덧붙임):**
1. 인쇄본·WhatsApp·이메일에 **공급업체 상품명 + SKU 를 주(主)로**, 내부명은 참고로 작게
2. **기존 발주 건에도 적용** — 데이터 마이그 불필요할 가능성이 높다:
   상세 라우트가 `sellerSource → SupplierProduct` 조인으로 `seller_product_name`/`seller_product_sku`
   를 이미 붙여준다(`routes/purchase-orders-crud.js:506-526`). 인쇄·공유 경로에도 같은 값을
   내려주면 옛 발주도 그대로 바뀐다. **단 `purchase_order_items.description` 스냅샷은 내부명이므로
   (`:743`,`:797`,`:1114`) 연결이 끊긴 옛 건은 폴백이 필요하다 — 실측 후 판단.**
3. ⚠️ **공급업체에게 나가는 문서 내용 변경 = 매장 밖으로 나가는 결과.** 배포 전 신중히.
   내부 화면까지 주/부를 뒤집는 것(2안)은 Irene 이 아직 고르지 않았다 — 1번만 확정.

  (Irene 언급: Fable 은 **목요일(8/27~28)** 사용 가능 → 아래 구조 사안을 그때 설계)

### 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- **[Fable 설계 대기] 브랜드 재고 목록 이원화** — `ingredients`(브랜드 88) vs `product_ingredients`(286).
  상품 등록 시 양쪽에 하나씩 생긴다. 단일 진실 = 메모리 [[project_brand_stock_two_lists_split]]
- **[Fable 설계 대기] `linked_ingredient_id` 반쪽 구현** — 읽기만 연결을 따르고 쓰기는 전부 무시.
  **채우면 숫자가 안 움직인다** — 쓰기도 따르게 할지, 링크 개념을 버릴지 결정 필요
- **[Fable 설계 대기] FG(푸드코트) 같은 어긋남** — 입고는 `ingredients`, 화면은 `foodcourt_products`.
  사오면 안 보이고 팔면 음수로 내려간다(역할 전수 조사 결과)
- **미배포 코드 1건** — `useInventoryData.ts` 가 브랜드 수량에 매장 값을 더하던 계산 제거.
  현재 연결이 비어 있어 운영 영향 없으나 누가 연결을 채우면 두 배로 보인다. 다음 배포에 포함
- **이름 표기 통일 남은 것 — Irene 이 나중에 직접 본다(8/25 지시, 개발 대상 아님).**
  다시 물어보지 말 것. ①한글명 없는 34건(K-소스·K-DINE 의류 등) ②가격이 다른 옛 상품
  `OMIJA/PEACH/STRAWBERRY BASE 1btl` 3건(비활성 그대로 둔다) ③`RECLOSABLE LID 12OZ BLACK`
  레시피·소스 연결은 Irene 이 직접 한다.
  Irene 원문: **"한글 안보이는 건 두고 가격 다른 거 둬. 내가 나중에 볼게. 레시피나 소스랑
  연결하는 거나 알아서 할게. 개발문제 아니면 넘어가. 비활성 두고."**
- **[코드 결함] 상품 삭제가 발주 연결을 안 치운다** — `DELETE /api/brand-products/:id`
  (`routes/brand-products.js:1133`)가 식자재·분배·옵션그룹은 지우면서 `ingredient_seller_products`
  는 그대로 둔다. 오늘 실매장 4건이 끊겼다(데이터는 복구 완료). 코드 수정은 배포 필요 — 별건.
- **채번을 max 기반으로** — SKU/코드가 건수 기반이라 삭제 뒤 등록하면 번호가 재발급된다.
  운영 재고 코드에 이미 중복 12종. 메모리 [[reference_count_based_code_numbering]]
- 식자재 쪽 이름 중복(id 384 / 385) 정리
- Brand Manager 는 재고 목록이 전부 비어 보인다(읽기는 `owner_user_id`, 쓰기는 `brand_id` 기준)
- 데이터 작업(매장 몫): 재료 수량 입력 → 그 뒤 메뉴↔레시피 연결 84건. 순서 반대면 품절 알림 폭주
- 프로필·구독 통합 / POS 기기 고정 해제 옵션 / 흩어진 계정 통합

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```
