## 현재 작업 상태
**마지막 업데이트:** 2026-09-07 18:30 UTC
**버전:** **v3.85** (버전 미상승 · SW `4.89-upload-touch-gates-20260907`)
**작업 상태:** 완료 — 오늘 **세 번 배포**. 남은 일은 Irene 지시로 **다음 세션**.

---

## 완료된 작업 (이번 세션)

### 배포 1 (17:42 UTC 이전, SW 4.87) — 판매자 발주 가시성 + 상태 표시
- BG 가 소유·소속·형제 브랜드 앞으로 온 발주를 전부 본다(`resolveBrandScopeIds`). 운영 실측 범위 `[1,2]` — K-DINE 주문이 화면에 나옴을 확인
- 판매자 관점 라벨: `received`→완료. **주문상태·결제상태 열 분리**(운영 발주 25건 전부 `unpaid` 표시)
- 거래 청구서 원장(수령 3경로 전부 발행 · 발주↔청구서 결제 양방향 거울 · SOA 이중청구 차단)

### 배포 2 (17:42 UTC, SW 4.88) — 발주 단위를 공급업체 기준으로
> Irene: "발주정보는 공급업체 기준. 발주페이지는 다 포장단위여야 하네" / "소스 다 kg" / "패키지는 다 pack"
- 서버가 라인 단위를 정한다 — `resolveOrderUnit` = 판매자 상품 단위 → 재료 포장단위 → 취급단위. 라인 생성 4곳 전부. **수량·단가·환산·금액 무변경(라벨만)**
- 발주 담는 화면도 같은 규칙(`orderUnitOf`). 가격도 **주문 단위당**으로(0.0279/g → 27.90/kg). **재고 칸은 취급단위 유지**
- 🔴 **소스 재료의 취급단위(g)는 못 바꾼다** — 레시피가 g 으로 쓰고 `inventoryDeductionService.js:292` 가 단위 변환 없이 그대로 차감한다(50 → 50kg 사고). 정지 조건이 막았다. 그래서 데이터가 아니라 표시 규칙을 고쳤다
- 검증: verify-all --full **18/18**(새 번들 `main.182ab12e.js` mount sweep 670초 실행) · health-check **227/227** · 고장주입 1건 반증(판매자 단위 무시 → 4/5 실패)

### 배포 3 (18:21 UTC, SW 4.89) — 인보이스 업로드 모바일 + 게이트 보강
- **인보이스 올리기 버튼** 29px → 35px (같은 줄 View·Invoice 와 동일). 내일부터 직원이 휴대폰에서 쓴다
  - 실브라우저(iPhone 12) 실제 업로드 성공 확인 · console.error 0 · 시험 잔재 0
  - 운영 대상 **15건**(with MIN Cafe 외부 공급업체). 공급업체 전부 살아 있어 버튼 뜬다. **지금까지 업로드 0건**
  - 처음 안 보였던 건 화면이 아니라 내 시험 데이터(삭제된 공급업체 → is_external=false)였다
- **배포 백업 보호가 실제로는 0건이었다** — `head -N`(줄바꿈) 결과를 `case " $KEEP "` 로 비교해 하나도 안 맞았다.
  시뮬레이션 재현(옛 0건 / 새 10건) 후 `| tr '\n' ' '` 로 수정
- **신규 게이트 `bundle-fresh`** — 프론트 소스가 서빙 번들보다 새로우면 fail-closed.
  빌드 실패를 sweep 캐시가 못 잡던 구멍(2026-09-06·09-07 두 번). 만들자마자 미빌드 변경을 실제로 잡았다
- 주간 리포트 생성기 2건: 디스크 원인을 실제 폴더 3개로 · 자동 보안 업데이트 판정을 apt 스탬프로 (**운영 미적용**)
- 검증: verify-all --full **19/19** · mount sweep 680초 실제 실행 · 스모크 10/10 · 번들 `main.d8cefc7d.js` 서빙 확인

### 운영 데이터 적용 (오늘)
- **발주 단위**: 판매자 상품 단위 1건(K-Bulgogi 단위 칸이 비어 있었다) · 재료 포장단위 18건 · 팩당 낱개 수 4건(3.25OZ 소스통이 1 → 50, 낱개 원가 50배였다) · 수령 전 발주 라인 라벨 5건. **금액 이동 0건 · 헤더=라인합 · 수령된 발주 무접촉**
- **1kg 소스 19건**: 취급단위 kg·기준 1·포장 kg (레시피 0·재고 0 인 행만)
- **GIT Consulting 구독**: 미결제 8건 → 0원 + 100% 할인(상태 유지 = Confirm 버튼) · Expired 해제(`subscription_end` 2026-04-01 → 2027-09-01) · **2026-10 ~ 2027-09 12개월치 0원 선발행**(스케줄러 중복검사를 이용해 RM799 재발 차단)

---

## 진행 중인 작업
- 없음

## 다음 확정 작업 (Irene 2026-09-07 지시 — "남은 거 다 다음에 하자")

1. **거래 청구서 백필 14건 (RM 4,020.57)** — 가장 급하다. 수령이 끝났는데 청구서가 안 나갔다. 운영 거래 청구서 현재 **0건**. 코드는 배포됐고 과거 건 백필만 남았다. `scripts/backfill-trade-invoices.js`
2. **통계 수치 연동 점검** — 통계 정리와 함께. `with MIN Cafe` 가 Revenue/Orders/Customers 전부 0 인데 Growth −100%. 0 을 "데이터 없음"이 아니라 실적 0 으로 계산 중. 메모리 [[project_stats_figures_not_linked]]
3. **브랜드제너럴 대시보드 매출** — 브랜드들 합계가 아니라 BG 자기 매출. 리포트는 만들었고 대시보드는 미착수
4. **중복 정리 미실행분** — `--apply` · `--deactivate`(상품 10건 비활성)
5. **주간 리포트 생성기 운영 적용** — `dev-backend/scripts/ops/fix-weekly-report.sh` 는 고쳤지만 운영에 **1회 실행**이 남았다
6. **운영서버 리부트** — Irene 승인 완료, sudo 비번이 필요해 Irene 이 직접: `ssh irene@87.106.78.146 "sudo shutdown -r 16:00"` (서버 UTC 16:00 = 말레이시아 자정)

## Irene 눈 확인 대기 (2건)
- 발주를 담아보고 **고를 때 본 단위 = 발주서 단위** 인지 (새로고침 Ctrl+Shift+R 후)
- 인보이스 To Pay 탭에 **Confirm 버튼**이 뜨는지, 눌러서 결제완료 되는지

## 후속 후보 (아이디어 메모, 확정 X)
> 다음 사이클 결정은 Irene 지시 기준. /개발시작 에서 자동 추천 대상 아님.

- 상시 100% 할인을 담을 자리가 구조에 없다 — 지금은 0원 인보이스를 미리 깔아 막았고, 1년 뒤 갱신 필요
- `fix-weekly-report.sh` (주간 보안 리포트 생성기 수정본, 운영 1회 실행용 · 미적용)

---

## 서버 재시작 후 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션에서 진행하던 작업을 이어서 하고 싶어.
/var/www/.claude/session-state.md 파일 읽어줘.
```
