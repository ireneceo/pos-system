# Fable 사후 검토 요청 — 2026-09-08 배포분

> 이 문서는 **팀원(Opus)이 사실만 모아 둔 것**이다. 판정·우선순위·수용 여부는 Fable 이 정한다.
> 작성 이유: 이날 배포 8묶음이 **Fable 사용 한도 초과(429)로 판정 없이** 나갔다.
> Irene 지시(원문): *"다음에 fable이 검토하게 저장해놔."*

---

## 0. 왜 판정 없이 나갔나 (사실)

- Fable 게이트 판정을 **두 번 요청**했고 둘 다 `rate_limit / HTTP 429 (You've reached your Fable limit)` 로 끊겼다.
  첫 요청 때 Fable 이 남긴 마지막 말: *"판정을 위해 설계 문서와 실제 diff 를 직접 대조하겠습니다."*
- 그 상태에서 Irene 이 `"3번으로 해. 다음에 판정받고 배포"` → 이후 `"다해"` → `/배포` 로 지시를 바꿨다.
- `.claude/.fable-gate-skip` 파일은 **이전부터 존재**했다(내가 만들지 않았다). 그래서 훅이 정지를 막지 않았다.
- `check-sensitive-diff` 는 매 묶음 **기준 ①②③⑤ 접촉**으로 «FABLE 게이트 대상» 판정을 냈다.

---

## 1. 배포 목록 (운영 반영 완료 · 8묶음)

| SW | 릴리즈 기록 | 핵심 |
|---|---|---|
| v3.86 | `2026-09-08-header-two-column.json` | 헤더 2열 반응형 56화면 + **P0** 외부 공급업체 가격→원가 전파 |
| 4.91 | `2026-09-08-invoice-cost-reconcile.json` | **P1 §1·2·3·5·6·7** + 크로스테넌트 가격쓰기 차단 + 브랜드 범위(주문) + regional-stats 난수 제거 |
| 4.92 | `2026-09-08-reconcile-visibility.json` | 청구서 발행자에 `supplier` 분기 + 인보이스↔발주 연결 표시 |
| 4.93 | `2026-09-08-reconcile-ux.json` | `/uploads` 만 X-Frame-Options SAMEORIGIN + 반응형 + 목록 차이 표시 |
| 4.94 | `2026-09-08-external-return-send.json` | 외부 반품 왓츠앱·메일·PDF 발송 + **구매자가 반품 닫기(재고 반영)** |
| 4.95 | `2026-09-08-reconcile-retro-invoice.json` | **P1 §5-3** 청구서 금액 정정 + **§4** 과거 발주 소급 |
| 4.97 | `2026-09-08-bg-dashboard-and-cost-report.json` | BG 대시보드 자기매출 + **requireBrandScope 소유∪배정** + **P2** 구매·원가 리포트 |
| 4.98 | `2026-09-08-reconcile-input-first.json` | **미배포(검증 중)** — 대조 화면 입력 우선 배치 + 청구서에 주문·수령일 |

각 묶음의 실호출·고장주입·게이트 결과는 위 릴리즈 기록 JSON 의 `verification` 절에 그대로 있다.

---

## 2. Fable 이 특히 봐야 할 지점 (내가 위험하다고 보는 순서)

### ① 권한 경계를 두 번 넓혔다 — «소유 ∪ 배정»
- `utils/managerBrandScope.js` 신설 → `routes/orders-crud.js` · `routes/manager-sales.js` · `middleware/brandScope.js` 가 함께 쓴다.
- 근거: 운영 실측에서 **같은 브랜드·같은 역할인데 계정마다 결과가 달랐다** —
  `help@gitconsulting.group`(BG, 소유자) 25건 / `irene@gitconsulting.group`(BG, 배정만) **0건**.
  `requireBrandScope` 는 배정 BG 에게 **403** 을 주고 있었다.
- 반증: 남의 브랜드 3곳 접근이 모두 404, 자기 브랜드만 200 (4/4). 고장주입으로 제거 시 403 재현.
- **판정 필요**: 형제 브랜드를 넣지 않은 선택, 그리고 `brandScope` 가 브랜드 재고·상품·레시피까지 함께 여는 파급.

### ② 반품 승인 권한을 구매자에게 열었다
- `routes/po-returns.js` — **외부(미가입) 공급업체 발주에 한해** 구매자가 자기 반품을 승인/거절할 수 있다.
- 근거: 외부 업체는 로그인이 없어 승인 주체가 없다. 운영에 반품 1건이 `requested` 로 멈춰 있었고
  **그동안 구매자 재고가 안 줄어 실제보다 많게** 남아 있었다. 반품 버튼이 뜨는 외부 발주 15건.
- Irene 지시: *"외부공급업체는 발주처럼 왓츠앱 메일, pdf 로 보내게 해줘야지."*
- 승인은 **재고를 되돌리고 크레딧노트를 만든다**. 대조군(가입 공급업체)은 404/400 으로 막히는 것을 확인했다.
- **판정 필요**: 자가승인의 타당성, 크레딧노트가 외부 건에도 발행되는 것이 맞는지.

### ③ 돈 문서를 다시 쓴다 — 청구서 금액 정정(§5-3)과 과거 발주 소급(§4)
- `services/reconcileInvoiceSync.js` — 대조 확정 시 청구서 라인·총액을 인보이스 값으로 덮어쓴다.
  안전선: **외부 공급업체만 · 미결제 청구서만**(결제·부분수금 건은 사유 반환 후 미변경).
- `services/retroApplyPrice.js` — 같은 매핑의 **미결제·미수령·미대조** 라인만, 기본 꺼짐, `batch_id` 로 되돌리기.
- **판정 필요**: 「이미 결제된 청구서는 안 고친다」를 내가 정한 것(설계에 명시 없음). 그리고 소급이 발주 헤더 금액을 다시 계산하는 범위.

### ④ 보안 헤더 완화
- `middleware/security.js` — `/uploads/` 경로만 `X-Frame-Options: SAMEORIGIN`, 나머지는 `DENY` 유지.
- 이유: 대조 화면이 자기 사이트 파일조차 못 띄웠다(미리보기 빈칸).
- 확인: `/uploads` SAMEORIGIN · `/api` DENY 각각 응답으로 검증.

### ⑤ 🔒 인쇄 보호파일 지문 재등록(bless)
- `routes/orders-crud.js` 의 GET 목록 핸들러 11줄(브랜드 범위)만 바뀌었고 인쇄 경로는 무접촉이다.
- Irene 승인 원문: *"인쇄는 인쇄방식만 함부로 안건드리면 문제없어."* → 그 근거로 `--bless` 실행.
- **판정 필요**: 이 bless 가 타당한지, 앞으로 같은 상황의 기준.

---

## 3. 내가 만든 결함을 스스로 찾아 고친 것 (참고)

1. **크로스테넌트 쓰기** — 대조의 «이 가격으로»가 공급업체 종류를 안 보고 판매가를 덮었다.
   dev 실측 발주 18건·라인 56건 도달, 한 상품에 매핑 4개. → 내가 등록한 외부 공급업체만 허용.
2. **원가 이력 조회 누출(잠재)** — `/cost-changes` 가 `entity` 없는 행을 무조건 열어줬다. 0건일 때 닫음.
3. **세금이 사라짐** — `invoices` 에 `tax_amount` 칸이 없는데 거기 썼다. 총액 114 → 정정 후 120.
   이 시스템은 세금이 `additional_charges` 한 곳에만 산다(Path B).

게이트가 잡아준 것: 인쇄 보호파일 지문 · 타임존 가드 · 인스펙션(중단된 테스트가 남긴 잔재).

---

## 4. 아직 판정이 필요한 미결 사안 (배포 안 됨)

### 4-1. 발주 청구서의 날짜·마감일 (2026-09-08 검토, Irene 요청)
운영 실측 16장(외부 15 · 브랜드 1, 전부 `trade`/`pending_payment`):

| 청구서 | 발행 | 마감 | 주문 | 수령 |
|---|---|---|---|---|
| TRD-SUP5-…001 | 09-08 | 09-23 (+15일) | 08-31 | 09-02 |
| TRD-SUP17-…001 | 09-08 | 09-23 (+15일) | 08-31 | 09-05 |
| TRD-SUP7-…001 | 09-08 | 09-23 (+15일) | 08-29 | 08-30 |

- **Issued 는 «만든 날»** 이라 백필분 14장이 전부 09-08 이다. 실제 거래일과 최대 10일 벌어진다.
- **Due 는 지어낸 값**이다. 15장 전부 정확히 +15일 = 계약 없을 때 붙는 `NET_15` 기본값인데,
  외부 공급업체와는 결제조건을 합의한 적이 없다.
- **그 지어낸 날짜가 자동 연체를 만든다** — `services/invoiceOverdueScheduler.js` 가 매일 돌며
  `pending_payment` + 마감일 경과 → `overdue` 전환 + 매장 메일. **9월 24일에 15장이 한꺼번에 연체가 된다.**
- 발행 시점에 이미 지난 마감일 0건, 수령 전 발행 0건(그 둘은 정상).

**내 의견(판정 아님)**: ①화면은 주문일·수령일을 앞세운다 ②결제조건 없는 판매자는 **마감일을 비운다**
③`issued_at` 은 데이터로 두되 표시 이름을 바꾼다(소급하면 지난달 정산서가 흔들린다).
Irene 원문: *"Issued 는 발주일. Due는 뭐야? 받은 날짜면 되지. 외부공급업체가 왜 마감일 관리가 필요해?"*

### 4-2. 운영 실행 대기
- 중복 링크 정리 — 미리보기 36건 확인됨(같은 상품 중복 · 발주 사용 0건). `--apply` 만 남음.
  ⚠ 실측: 중복 그룹 54개 중 **18개는 서로 다른 프로덕트 = 복수 공급처(정상)**, 비선호인데
  **발주에 쓰인 링크 3개**(재료 800/834/894). 그 둘은 대상에서 제외했다.
- 주간 서버 리포트 — `sudo` 필요라 Irene 이 터미널에서 직접.

---

## 5. 기계 게이트 상태 (마지막 실행 기준)

- `verify-all --full` **19/19** (묶음마다 실행)
- health-check **227/227** · 인쇄 보호파일 **8/8** · 인쇄 라우트 **34/34** · i18n 신규 하드코딩 **0**
- 실브라우저 mount sweep 8역할 크래시 **0**
- 고장주입 **총 12건** 반증 후 원복 (외부결제 가드 · 원가 전파 · 매칭기 · 소유 확인 · 이력 조회 · 배정 브랜드 ×2 · regional-stats 난수 · 반품 대조군 · 청구서 정정 대조군 2 · 소급 제외 대상)

---

## 6. 운영 데이터 변경 (Irene 이 직접 실행)

- 거래 청구서 백필 **14건 / RM 4,020.57** — 금액 검산 일치, 실행 후 누락 **0건** 확인.
  발행일은 «만든 날» 규칙을 따랐다(위 4-1 의 발단).

---

# 추가 — 2026-09-09 배포분 (v3.87 · SW 4.99) · 같은 사유로 판정 없이 나감

2026-09-09 에도 Fable 판정을 2회 시도했으나 **429(사용 한도)** 로 못 받고 배포했다. 아래는 사실만이다.

## A. 배포 내용
1. **레시피 재료명·줄 원가 표시** (프론트 2파일, 커밋 `90f88cd82`)
   화면이 이름·원가를 API 값이 아니라 «선택기 목록»에서 다시 찾던 것. 그 목록은 거울 재료를 뺀 목록이라
   저장된 줄의 재료를 못 찾아 «Ingredient #123 · RM 0.00» 이 됐다.
   운영 실측: 브랜드 레시피 54개 · **337줄 중 334줄(99%)이 거울 재료** · cost=0 21줄 · 합계 0 인 레시피 27개.
2. **원가 계산을 서버로** (커밋 `194d77e4f`, `d6a1ab857`)
   신규 `dev-backend/utils/recipeCost.js`(단위 환산표 + `computeLineCost` + `resolveLineCost`).
   `routes/recipes.js` · `routes/product-recipes.js` 의 저장 경로가 `item.cost || 0` 대신 서버 계산값을 쓴다.
   환산 불가 줄만 화면 값 → 저장돼 있던 값 순으로 폴백. **어느 경우에도 0 으로 덮지 않는다.**
3. **e2e 안전망이 실제로 돌게 고침** — 지난 «3/3» 기록이 오늘 3건 전부 실패했는데 코드 회귀가 아니라
   테스트가 브랜드를 잘못 고른 것이었다. dev 전용 손상 주입 `scripts/e2e-damage-recipe-cost.js` 신설
   (운영 DB 거부 · `E2E-`/`TMP-` 이름 아니면 거부 — 둘 다 실제 거부 확인).

## B. Fable 이 봐야 할 지점 (내가 위험하다고 보는 순서)
### B-1. 돈 값을 서버가 다시 계산해 덮어쓴다
저장 시 화면이 보낸 원가를 버리고 재고아이템 단가에서 계산한 값을 쓴다. 세 경로 전부(브랜드 레시피 ·
프로덕트 레시피 · RA 레시피). **단위 환산표가 프론트 `unitConversion.ts` 와 같은 값이라는 전제**에 기대고 있고,
환산 불가일 때만 폴백한다. 환산표가 틀리면 원가가 조용히 틀린 값으로 저장된다.

### B-2. RA 매장 오버라이드를 아직 안 본다
매장은 자기 재고아이템 원가(공급업체 연결)를 써야 하고 `restaurant_ingredient_costs` 오버라이드가 있다.
현재 서버 계산은 그것을 보지 않는다. 운영 실측상 지금 망가진 값은 없다(매장 줄 10개 · zerocost 0)지만
**구조적으로 매장 원가가 브랜드 단가로 계산될 수 있다.**

### B-3. 운영에 이미 0 으로 박힌 값
화면에서는 `recipeTotalCost()` 로 제 값이 보이고, 한 번 저장하면 DB 도 복구된다.
**일괄 복구는 운영 쓰기라 안 했다** — 판정 필요.

## C. 게이트 결과 (2026-09-09)
verify-all --full **19/19** · health-check **227/227** · 인쇄 보호파일 **8/8 무변경** ·
e2e 3건 × 3회 **9/9** · 고장주입 2방향 반증(서버 계산 끄면 0.0000 재현 → 원복 5.0000 / 해석 순서 되돌리면
화면에 `Ingredient #3943 · RM 0.00` 재현 → 원복) · 실브라우저에서 손상 레시피 열어 저장 → DB **0.00 → 5.00**.
배포 백업 `/var/www/backups/20260909_080518` · 마이그 81/81 · 스모크 10/10.

## D. 2026-09-09 09:00 현재 상태 (실측)
- 워킹트리 clean · 운영 SW `4.99-recipe-ingredient-display-20260909`
- 운영-dev 파일 대조 6/6 SAME (`recipeCost.js` · `recipes.js` · `product-recipes.js` ·
  `invoices-list.js` · `cost-reconciliation.js` · `managerBrandScope.js`)
- `fable-gate status`: 민감 판정 **비대상** · 통과 마커 무효(지문 불일치)
- `invoices-list.js` 의 «청구서에 발주 주문일·수령일» 은 운영본과 바이트 단위로 동일하게 두었다(§4-1 미결과 연결)

---

## §4-1 정정 — 9/24 에 실제로 일어나는 일 (2026-09-09 운영 실측 + 코드 대조)

기존 기록은 «9월 24일에 15장이 한꺼번에 연체 처리된다» 였다. **범위를 다시 재서 정정한다.**

### 운영 실측 (`scripts/inspect-po-invoice-types.js`, 읽기 전용, 운영 DB)
```
supplier  외부 공급업체   trade  pending_payment   15건  RM 4,184.38
brand     (공급업체 아님) trade  pending_payment    1건  RM   195.70
→ 16장 전부 발행 09-08 · 마감 09-23(+15일) · 주문 08-29~09-04 · 수령 08-30~09-05
→ 마감일이 이미 지난 청구서 0건 · 수령 전 발행 0건
```
날짜 자체는 **틀린 게 아니다** — 수령은 다 끝났고 발행일도 정상이다.
문제는 **마감일 09-23 을 외부 공급업체와 합의한 적이 없다**는 것 하나다
(`purchaseOrderService.js:121` 의 계약 없을 때 폴백 `NET_15`).

### 9/24 에 실제로 일어나는 일 — 코드 3곳 대조
| 경로 | 판정식 | trade 청구서에 걸리나 |
|---|---|---|
| `invoiceOverdueScheduler` 상태 전환 | `invoice_category ≠ 'subscription'` | **걸린다** → 16장 `pending_payment` → `overdue` |
| 같은 스케줄러의 메일 | 매장 admin 에게 `invoice_overdue` **1통씩** | **걸린다** (개인 관문은 `sendNotification` 이 봄) |
| `processOverduePayments` 매장 정지 | `invoice_category: 'subscription'` (`subscriptionScheduler.js:291`) | **안 걸린다** — 매장 정지·구독 영향 **0** |
| `processOverdueReminders` D+3/7/14 독촉 | 화이트리스트 `subscription·pos_subscription·brand_plan·foodcourt_plan` (`:784`) | **안 걸린다** — 후속 독촉 **0** |

**결론: 피해는 «상태 라벨 16장이 Overdue 로 바뀌고, 매장 관리자에게 근거 없는 연체 메일 16통» 까지다.**
매장이 정지되거나 반복 독촉이 가는 일은 없다. 기존 기록보다 좁다.

### 부수 적발 — 주석이 사실과 달랐다 (2026-09-09 수정함)
`invoiceOverdueScheduler.js` 머리 주석이 «D+3/D+7/D+14 는 이미 subscriptionScheduler 가
**ALL invoice categories** 에 보내므로 여기서 중복하지 않는다» 라고 돼 있었는데,
실제 화이트리스트는 구독 계열 4종뿐이라 **trade 는 아예 대상이 아니었다.**
주석만 실측대로 고쳤다(동작 무변경). → 이 주석을 믿고 «독촉이 반복된다» 고 판단하면 과대평가가 된다.

### ✅ 판정 완료 (2026-09-10 Fable)

- **마감일**: 외부(미가입) 공급업체 청구서는 `due_date = NULL`. 브랜드 판매자 1장은 그대로(내부 정책).
- **`issued_at` 소급 — 철회.** 2026-09-10 판정은 «수령일로 소급» 이었으나, `scripts/backfill-trade-invoices.js`
  머리의 **2026-09-07 판정 · Irene 승인** 「발행일은 실행하는 날, 소급하지 않는다」가 선다(게이트 F3에서 Fable 이 자기 판정 철회).
  수령일은 청구서 비고와 4.98 화면의 주문일·수령일 칸에 이미 있고, 구매·원가 리포트는 `po.received_at` 기준이라
  발행일이 리포트를 흔들지 않는다. 마이그에서 소급 스위치·주석 SQL 삭제.

### 판정이 필요한 지점 (내 의견 아님 — Fable 몫)
1. 결제조건이 없는 판매자의 청구서에 **마감일을 아예 비울 것인가**, 아니면 수령일 기준 다른 기본값을 둘 것인가
2. 이미 발행된 16장의 마감일을 **소급 수정할 것인가** (운영 쓰기 · 지난달 SOA 영향 검토 필요)
3. 아무것도 안 하고 9/24 에 16통이 나가게 둘 것인가

⚠ 2026-09-09 에도 Fable 판정 시도가 **429(사용 한도)** 로 실패했다. 세 세션 연속이다.

---

# 【E】 2026-09-09 세션 #3 — 태블릿 반응형 + 키오스크 + 장바구니 한 화면 (**미배포 · 미커밋**)

> Irene 지시: ①"fable없이 할 수 있는 거 해줘. 태블릿 사이즈 반응형 좀 다 잡아줘. 그리고 모바일오더 형태를
> 태블릿 버전도 해줘서 고객이 스스로 키오스크 주문하듯이 UI해줄 수 있어? 우리 장기적으로 고객이 주문하게도
> 해야 해서 태블릿 배치하려고 해." ②"검증했어? 모바일 아니면 결제할 내용이 포스터미널처럼 같이 한화면에
> 나와야 하는 거 아니야? 장바구니 말이야." ③"fable이 검토하고 하도록 해줘. 저장해. 내일할게."
>
> ⚠ **Fable 판정 시도가 또 429 로 실패했다 — 이번이 다섯 번째 연속**(09-08 · 09-09 오전 · 09-09 재접속 ·
> 09-09 마감일 건 · 09-09 이 묶음). 판정 없이 배포하지 않았다.

## E-1. 무엇을 바꿨나

### (가) 태블릿 폭 가로넘침 — 실측 후 공용 컴포넌트 우선 수정
측정 도구를 새로 만들었다: `dev-frontend/scripts/tablet-overflow-sweep.js` (읽기 전용).
페이지(document) 넘침이 아니라 **내부 가로 스트립 넘침**을 잰다 — 매장이 말하는 "좌우 흔들림"의 정체.
5개 폭(768·820 세로 / 1024×600 · 1024×768 · 1280 가로) × 25화면. **페이지 넘침은 전 화면 0건.**

| 파일 | 증상 | 소비처 |
|---|---|---|
| `components/Common/TabComponents.tsx` | 리포트 탭 최대 418px | **40개 화면** |
| `components/UI/Tabs.tsx` | 메뉴관리 카테고리 탭 최대 1406px | 8개 화면 |
| `pages/IncomingOrders/IncomingOrdersView.tsx` RLStatusTabs | 122px | BG 수신주문 |
| `pages/Customers/CustomersPage.tsx` | 데스크톱 표(min-width 960px)가 태블릿 세로에서 밀림 | 경계 768→1024, 기존 카드 보기 사용 |

방식 = 태블릿(≤1024)에서 `overflow-x:auto` 대신 `flex-wrap:wrap`.
2026-06-15 LiveOrders StatusTabs 를 같은 방식으로 고쳐 검증된 패턴이다.

**결함 아님으로 판정한 것**(Fable 이 뒤집을 수 있음):
고객 8열 표의 컨테이너 가로스크롤(표는 줄바꿈 불가) · `<input>` 내부 텍스트 스크롤 ·
`text-overflow:ellipsis` 말줄임(내 탐지기의 1차 오검출이었고 제외 규칙을 추가했다).

### (나) 🔒 손대지 않은 것 — 보호파일이라 팀원 권한 밖
- `pages/POSTerminal/POSTerminalPage.tsx` 카테고리 스트립 — **5개 폭 전부 넘침, 최대 1651px**
  (이번 측정에서 가장 큰 값). 해당 요소는 `overflow-x:auto` 스트립.
- `pages/KitchenDisplay/KitchenDisplayPage.tsx` 스테이션 칩 — 99px(2개 폭).
  코드는 `<ViewToggle style={{ maxWidth:'min(440px, 36vw)', overflowX:'auto', flexWrap:'nowrap' }}>` 이고
  주석이 "many stations on small tablets: overflow-x scroll" 이라 **의도된 설계로 보인다**.
  같은 화면에서 body 23px 넘침도 1개 폭에서 관측.

`check-print-guard.js` 가 파일 전체 sha256 을 비교하므로 CSS 한 줄만 고쳐도 배포가 fail-closed 로 막힌다.
`--bless` 는 CLAUDE.md 상 Fable/Irene 몫이다. **판정 필요.**

### (다) 키오스크 모드 (신규)
**새 경로·새 페이지를 만들지 않았다** — 기존 모바일오더의 **표시 모드**다.
주문 생성·결제 로직 무접촉이고 바뀐 것은 폭·터치타깃·세션 수명뿐.
- 신규 `src/mobile/utils/kioskMode.ts` — `?kiosk=1` → sessionStorage 유지, 유휴 감시 90초
- `MobileLayout` 폭 600→1120px · 하단 nav 56px · 메뉴 카드 240px+
- 90초 무조작 → `clearCart()` + `clearActiveTable()` + 처음 화면.
  **경로가 `/payment` 또는 `/order/` 를 포함하면 감시하지 않는다**(결제 중·주문확인 중 리셋 방지)
- 주문 완료 화면(`OrderTrackingPage`)에 45초 카운트다운 + "새 주문 시작" 버튼
- 키오스크에서 Account nav 숨김(공용 기기 로그인 잔류 방지)
- 설정 → 모바일 주문에 키오스크 주소·QR 카드. i18n 4개 언어(`settings.kioskAccess*`, `menu.kiosk.*`)

### (라) 장바구니 «한 화면» (Irene 원문 ②)
- 신규 `src/mobile/components/CartContents.tsx` — 장바구니 줄·합계·빈 상태 +
  **금액식 `useCartTotals` 의 단일 소스**. `/cart` 페이지와 키오스크 옆 패널이 같은 것을 쓴다.
  금액식은 CartPage 에 인라인이던 것을 **그대로 옮겼고 계산 내용은 바꾸지 않았다**
  (소계 → 서비스차지(takeaway 제외 옵션 기본 true) → 세금 → 합계).
- `CartPage` 369줄 → 84줄.
- `MenuPage`: 키오스크 + `window.innerWidth >= 1024` 일 때만 2단(메뉴 + 340px sticky 패널).
  좁으면 한 단 + 종전 하단 카트 바(768 에서는 메뉴 2열 + 패널 340px 이 안 들어가 카드가 눌린다).
- **금액 일치 실측**: 실제로 담아서 —
  패널 `Subtotal RM 12.90 / Tax(6%) RM 0.77 / Total RM 13.67` == `/cart` 동일. JS 오류 0.

### (마) 기존 결함 발견·수정 (키오스크와 무관 — 손님 폰에도 적용)
하단 고정 바 5곳이 `left:0;right:0` 위에 `left:50%` 만 얹고 `right` 를 해제하지 않아
폭이 **화면의 절반**으로 고정 → `max-width` 가 **한 번도 적용된 적이 없었다.**
`right:auto` 만 주면 이번엔 내용 크기로 오그라든다(실측 271px).
→ `right:auto` + `width:100%` 를 **함께** 줘야 한다.
`MenuPage` 카트 바만은 `barSlideUp` 이 transform 을 쓰므로 translateX 대신 `left/right + margin:auto`.
대상: `MobileLayout` BottomNav · `CartPage` CheckoutButton · `ItemDetailPage` AddToCartButton ·
`PaymentPage` PayHint/PayButton · `MenuPage` CartBar.

## E-2. Fable 이 특히 봐야 할 지점 (내가 위험하다고 보는 순서)

1. **금액식을 파일 밖으로 옮긴 것** — `CartContents.tsx` 의 `useCartTotals`.
   내용은 그대로 옮겼다고 판단했고 실측으로 두 화면 값이 같음을 확인했지만,
   **돈 계산을 옮긴 것 자체**가 검토 대상이다. (`check-sensitive-diff` 는 이 파일을 찍지 않았다.)
2. **`PaymentPage.tsx` 접촉** — 기계 판정이 기준②(돈)로 찍는다.
   실제 diff 는 CSS(`max-width`/`right`/`width`)와 boolean prop 하나뿐이고 결제 로직·금액·API 무접촉.
   09-08 `SupplierPaymentSettingsPage` 와 같은 «파일 경로 때문» 사례로 보이는데 확인이 필요하다.
3. **`CustomersPage` 표→카드 경계 상향(768→1024)** — 태블릿에서 직원이 보는 것이 바뀐다.
   되돌리기 쉽지만 «표를 유지하고 스크롤» 과 «카드로 전환» 중 어느 쪽이 맞는지는 판단이 갈린다.
4. **키오스크 유휴 리셋 90초 / 완료화면 45초** — 숫자 근거는 업계 관행이고 실측 근거는 없다.
5. **`design-guard --bless`** — 팀원이 실행했다. 근거는 baseline 이 **파일 단위 키**라
   `CartPage` 의 `RemoveButton` 이 새 파일로 **이동**해 새 키가 생긴 것(신규 버튼 아님).
   재등록 303→295(사라진 옛 항목 8개 정리). 고장주입으로 가드 작동은 반증했다.

## E-3. 게이트 결과 (최종 번들 `main.9968b598.js`)
- **`verify-all --full` 18/19 통과**
- 실브라우저 mount sweep 693초 **크래시 0** (8역할 + `/pos/manager/*`)
- health-check 통과 · 인쇄 라우트 가드 통과 · **인쇄 보호파일 8/8 무변경**
- i18n 4언어 통과 · 죽은 핸들러 0 · state hydration warning 0
- ⬜ 실패 1건 `deploy-ready` — ①이번 묶음 릴리즈 기록 파일 없음 ②SW 버전이 직전 배포(4.99)와 동일.
  **배포하지 않아서 그대로 두었다.** 배포 시 SW 를 올리지 않으면 매장이 옛 번들을 계속 쓴다.
- 고장주입 1건: `design-guard` 에 가짜 로컬 styled.button 주입 → 즉시 검출 → 제거 후 통과

실브라우저 실측표:
| 조건 | 콘텐츠 | 하단바 | 장바구니 패널 | 페이지 넘침 | 내부 넘침 | JS오류 |
|---|---|---|---|---|---|---|
| 키오스크 1280 | 1120 | 1120 | 340 | 0 | 0 | 0 |
| 키오스크 1024 | 1024 | 1024 | 340 | 0 | 0 | 0 |
| 키오스크 768 세로 | 768 | 768 | 없음(한 단) | 0 | 0 | 0 |
| 일반 1280 | 600 | 600 | 없음 | 0 | 0 | 0 |
| 폰 390 | 390 | 390 | 없음 | 0 | 0 | 0 |

## E-4. 팀원이 이번 세션에 스스로 낸 실수 (숨기지 않고 적음)
1. 탐지기가 의도된 말줄임(ellipsis)을 넘침으로 셈 → 제외 규칙 추가
2. `networkidle` 대기가 소켓 때문에 안 끝나 라우트마다 25s 소진 → `domcontentloaded` 로 교체
3. `<CartBar` 문자열 치환이 `CartBarLeft` 등 하위 컴포넌트까지 잡음 → 즉시 복구
4. `right:auto` 만 줘서 바가 오그라듦 → `width:100%` 병행
5. 대기 루프 `pgrep -f` 가 **자기 명령줄을 매칭**해 무한 대기(54분 허비) → 스크립트 파일 + 대괄호 트릭
6. `heavy-task-gate` 가 **종료코드 0 으로 조용히** 빌드를 건너뛴다 — 로그 미확인 시 "빌드 성공" 오인

## E-5. 물어보는 것
①이대로 배포 가능한지 ②POS Terminal(1651px)·KDS(99px) 보호파일 건을 어떻게 할지
③`CartContents.tsx` 로 금액식을 옮긴 것이 안전한지 ④놓친 것이 있는지.
