# Fable 사후 검토 요청 — 2026-09-08 배포분

> 이 문서는 **팀원(Opus)이 사실만 모아 둔 것**이다. 판정·우선순위·수용 여부는 Fable 이 정한다.
> 작성 이유: 이날 배포 8묶음이 **Fable 사용 한도 초과(429)로 판정 없이** 나갔다.
> Irene 지시(원문): *"다음에 fable이 검토하게 저장해놔."*

---

## 0. 왜 판정 없이 나갔나 (사실)

- Fable 게이트 판정을 **두 번 요청**했고 둘 다 `rate_limit / HTTP 429 (You've reached your Fable limit)` 로 끊겼다.
  첫 요청 때 Fable 이 남긴 마지막 말: *"판정을 위해 설계 문서와 실제 diff 를 직접 대조하겠습니다."*
- 그 상태에서 Irene 이 `"3번으로 해. 다음에 판정받고 배포"` → 이후 `"다해"` → `/배포` 로 지시를 바꿨다.
- `.claude/.fable-gate-skip` 파일은 **이전부터 존재**했다(내가 만들지 않았다). 그래서 훅이 정지를 막지 않았다.
- `check-sensitive-diff` 는 매 묶음 **기준 ①②③⑤ 접촉**으로 «FABLE 게이트 대상» 판정을 냈다.

---

## 1. 배포 목록 (운영 반영 완료 · 8묶음)

| SW | 릴리즈 기록 | 핵심 |
|---|---|---|
| v3.86 | `2026-09-08-header-two-column.json` | 헤더 2열 반응형 56화면 + **P0** 외부 공급업체 가격→원가 전파 |
| 4.91 | `2026-09-08-invoice-cost-reconcile.json` | **P1 §1·2·3·5·6·7** + 크로스테넌트 가격쓰기 차단 + 브랜드 범위(주문) + regional-stats 난수 제거 |
| 4.92 | `2026-09-08-reconcile-visibility.json` | 청구서 발행자에 `supplier` 분기 + 인보이스↔발주 연결 표시 |
| 4.93 | `2026-09-08-reconcile-ux.json` | `/uploads` 만 X-Frame-Options SAMEORIGIN + 반응형 + 목록 차이 표시 |
| 4.94 | `2026-09-08-external-return-send.json` | 외부 반품 왓츠앱·메일·PDF 발송 + **구매자가 반품 닫기(재고 반영)** |
| 4.95 | `2026-09-08-reconcile-retro-invoice.json` | **P1 §5-3** 청구서 금액 정정 + **§4** 과거 발주 소급 |
| 4.97 | `2026-09-08-bg-dashboard-and-cost-report.json` | BG 대시보드 자기매출 + **requireBrandScope 소유∪배정** + **P2** 구매·원가 리포트 |
| 4.98 | `2026-09-08-reconcile-input-first.json` | **미배포(검증 중)** — 대조 화면 입력 우선 배치 + 청구서에 주문·수령일 |

각 묶음의 실호출·고장주입·게이트 결과는 위 릴리즈 기록 JSON 의 `verification` 절에 그대로 있다.

---

## 2. Fable 이 특히 봐야 할 지점 (내가 위험하다고 보는 순서)

### ① 권한 경계를 두 번 넓혔다 — «소유 ∪ 배정»
- `utils/managerBrandScope.js` 신설 → `routes/orders-crud.js` · `routes/manager-sales.js` · `middleware/brandScope.js` 가 함께 쓴다.
- 근거: 운영 실측에서 **같은 브랜드·같은 역할인데 계정마다 결과가 달랐다** —
  `help@gitconsulting.group`(BG, 소유자) 25건 / `irene@gitconsulting.group`(BG, 배정만) **0건**.
  `requireBrandScope` 는 배정 BG 에게 **403** 을 주고 있었다.
- 반증: 남의 브랜드 3곳 접근이 모두 404, 자기 브랜드만 200 (4/4). 고장주입으로 제거 시 403 재현.
- **판정 필요**: 형제 브랜드를 넣지 않은 선택, 그리고 `brandScope` 가 브랜드 재고·상품·레시피까지 함께 여는 파급.

### ② 반품 승인 권한을 구매자에게 열었다
- `routes/po-returns.js` — **외부(미가입) 공급업체 발주에 한해** 구매자가 자기 반품을 승인/거절할 수 있다.
- 근거: 외부 업체는 로그인이 없어 승인 주체가 없다. 운영에 반품 1건이 `requested` 로 멈춰 있었고
  **그동안 구매자 재고가 안 줄어 실제보다 많게** 남아 있었다. 반품 버튼이 뜨는 외부 발주 15건.
- Irene 지시: *"외부공급업체는 발주처럼 왓츠앱 메일, pdf 로 보내게 해줘야지."*
- 승인은 **재고를 되돌리고 크레딧노트를 만든다**. 대조군(가입 공급업체)은 404/400 으로 막히는 것을 확인했다.
- **판정 필요**: 자가승인의 타당성, 크레딧노트가 외부 건에도 발행되는 것이 맞는지.

### ③ 돈 문서를 다시 쓴다 — 청구서 금액 정정(§5-3)과 과거 발주 소급(§4)
- `services/reconcileInvoiceSync.js` — 대조 확정 시 청구서 라인·총액을 인보이스 값으로 덮어쓴다.
  안전선: **외부 공급업체만 · 미결제 청구서만**(결제·부분수금 건은 사유 반환 후 미변경).
- `services/retroApplyPrice.js` — 같은 매핑의 **미결제·미수령·미대조** 라인만, 기본 꺼짐, `batch_id` 로 되돌리기.
- **판정 필요**: 「이미 결제된 청구서는 안 고친다」를 내가 정한 것(설계에 명시 없음). 그리고 소급이 발주 헤더 금액을 다시 계산하는 범위.

### ④ 보안 헤더 완화
- `middleware/security.js` — `/uploads/` 경로만 `X-Frame-Options: SAMEORIGIN`, 나머지는 `DENY` 유지.
- 이유: 대조 화면이 자기 사이트 파일조차 못 띄웠다(미리보기 빈칸).
- 확인: `/uploads` SAMEORIGIN · `/api` DENY 각각 응답으로 검증.

### ⑤ 🔒 인쇄 보호파일 지문 재등록(bless)
- `routes/orders-crud.js` 의 GET 목록 핸들러 11줄(브랜드 범위)만 바뀌었고 인쇄 경로는 무접촉이다.
- Irene 승인 원문: *"인쇄는 인쇄방식만 함부로 안건드리면 문제없어."* → 그 근거로 `--bless` 실행.
- **판정 필요**: 이 bless 가 타당한지, 앞으로 같은 상황의 기준.

---

## 3. 내가 만든 결함을 스스로 찾아 고친 것 (참고)

1. **크로스테넌트 쓰기** — 대조의 «이 가격으로»가 공급업체 종류를 안 보고 판매가를 덮었다.
   dev 실측 발주 18건·라인 56건 도달, 한 상품에 매핑 4개. → 내가 등록한 외부 공급업체만 허용.
2. **원가 이력 조회 누출(잠재)** — `/cost-changes` 가 `entity` 없는 행을 무조건 열어줬다. 0건일 때 닫음.
3. **세금이 사라짐** — `invoices` 에 `tax_amount` 칸이 없는데 거기 썼다. 총액 114 → 정정 후 120.
   이 시스템은 세금이 `additional_charges` 한 곳에만 산다(Path B).

게이트가 잡아준 것: 인쇄 보호파일 지문 · 타임존 가드 · 인스펙션(중단된 테스트가 남긴 잔재).

---

## 4. 아직 판정이 필요한 미결 사안 (배포 안 됨)

### 4-1. 발주 청구서의 날짜·마감일 (2026-09-08 검토, Irene 요청)
운영 실측 16장(외부 15 · 브랜드 1, 전부 `trade`/`pending_payment`):

| 청구서 | 발행 | 마감 | 주문 | 수령 |
|---|---|---|---|---|
| TRD-SUP5-…001 | 09-08 | 09-23 (+15일) | 08-31 | 09-02 |
| TRD-SUP17-…001 | 09-08 | 09-23 (+15일) | 08-31 | 09-05 |
| TRD-SUP7-…001 | 09-08 | 09-23 (+15일) | 08-29 | 08-30 |

- **Issued 는 «만든 날»** 이라 백필분 14장이 전부 09-08 이다. 실제 거래일과 최대 10일 벌어진다.
- **Due 는 지어낸 값**이다. 15장 전부 정확히 +15일 = 계약 없을 때 붙는 `NET_15` 기본값인데,
  외부 공급업체와는 결제조건을 합의한 적이 없다.
- **그 지어낸 날짜가 자동 연체를 만든다** — `services/invoiceOverdueScheduler.js` 가 매일 돌며
  `pending_payment` + 마감일 경과 → `overdue` 전환 + 매장 메일. **9월 24일에 15장이 한꺼번에 연체가 된다.**
- 발행 시점에 이미 지난 마감일 0건, 수령 전 발행 0건(그 둘은 정상).

**내 의견(판정 아님)**: ①화면은 주문일·수령일을 앞세운다 ②결제조건 없는 판매자는 **마감일을 비운다**
③`issued_at` 은 데이터로 두되 표시 이름을 바꾼다(소급하면 지난달 정산서가 흔들린다).
Irene 원문: *"Issued 는 발주일. Due는 뭐야? 받은 날짜면 되지. 외부공급업체가 왜 마감일 관리가 필요해?"*

### 4-2. 운영 실행 대기
- 중복 링크 정리 — 미리보기 36건 확인됨(같은 상품 중복 · 발주 사용 0건). `--apply` 만 남음.
  ⚠ 실측: 중복 그룹 54개 중 **18개는 서로 다른 프로덕트 = 복수 공급처(정상)**, 비선호인데
  **발주에 쓰인 링크 3개**(재료 800/834/894). 그 둘은 대상에서 제외했다.
- 주간 서버 리포트 — `sudo` 필요라 Irene 이 터미널에서 직접.

---

## 5. 기계 게이트 상태 (마지막 실행 기준)

- `verify-all --full` **19/19** (묶음마다 실행)
- health-check **227/227** · 인쇄 보호파일 **8/8** · 인쇄 라우트 **34/34** · i18n 신규 하드코딩 **0**
- 실브라우저 mount sweep 8역할 크래시 **0**
- 고장주입 **총 12건** 반증 후 원복 (외부결제 가드 · 원가 전파 · 매칭기 · 소유 확인 · 이력 조회 · 배정 브랜드 ×2 · regional-stats 난수 · 반품 대조군 · 청구서 정정 대조군 2 · 소급 제외 대상)

---

## 6. 운영 데이터 변경 (Irene 이 직접 실행)

- 거래 청구서 백필 **14건 / RM 4,020.57** — 금액 검산 일치, 실행 후 누락 **0건** 확인.
  발행일은 «만든 날» 규칙을 따랐다(위 4-1 의 발단).
