{
  "name": "purple-pos-mobile",
  "version": "0.1.0",
  "description": "Purple POS Android app (Capacitor) — RawBT/per-device print-app replacement. Loads the remote web app and injects window.__NATIVE_PRINT natively (same §4 contract as the Windows Electron app).",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "add:android": "cap add android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
